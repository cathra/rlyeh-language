[package]
name = "rlyeh-gc-runtime"
version.workspace = true
edition.workspace = true
description = "Rlyeh 追踪 GC 运行时（K4 Gc<T> 保守标记-清除）"

[lib]
path = "src/lib.rs"
crate-type = ["rlib", "staticlib"]

[dependencies]
libc = "0.2"
