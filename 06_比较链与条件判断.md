# 06 比较链与条件判断

## 相关文档

| 类型 | 文档 | 说明 |
|------|------|------|
| 语法规范 | [docs/grammar.md](./docs/grammar.md) | 比较链语法（EBNF） |
| 实现任务 | [prompts/P003_比较链语义分析.md](./prompts/P003_比较链语义分析.md) | 比较链实现 |

## 目标

实现 Zeta 的数学式比较链和 `in` 集合判断，确保编译期展开、零运行时开销。

## 语法回顾

```zeta
// 区间内（正向）
if 0 < i < 10 {}
if 0 <= i <= 10 {}
if 0 < i <= 10 {}
if 0 <= i < 10 {}

// 区间外（反向）
if 0 > i > 10 {}      // i < 0 || i > 10
if 0 >= i >= 10 {}    // i <= 0 || i >= 10

// 多变量链
if a < b < c {}
if 0 <= x < y <= 100 {}

// in 集合
if i in (1, 3, 5) {}
if ch in ('a'..'z', 'A'..'Z') {}

// 区间字面量
if i in (0, 10) {}    // 开区间
if i in [0, 10] {}    // 闭区间
if i in (0, 10] {}    // 左开右闭

// 补集
if i not in (0, 10) {} // 等价于 0 > i > 10
```

## 编译期展开规则

### 比较链展开

```rust
/// 将比较链展开为逻辑与/或的组合
fn expand_comparison_chain(
    exprs: &[Expr],
    ops: &[CompOp],
) -> Expr {
    let direction = check_direction(ops);
    
    // 生成两两比较的列表
    let mut comparisons = Vec::new();
    for i in 0..ops.len() {
        comparisons.push(Expr::Binary(
            Box::new(exprs[i].clone()),
            ops[i].clone(),
            Box::new(exprs[i + 1].clone()),
        ));
    }
    
    // 用 && 连接所有比较
    // 正向和反向都是 && 连接
    // 区别在于比较运算符本身的方向
    fold_with_and(comparisons)
}

fn check_direction(ops: &[CompOp]) -> ChainDirection {
    let first_is_less = matches!(ops[0], CompOp::Lt | CompOp::Le);
    
    for op in &ops[1..] {
        let is_less = matches!(op, CompOp::Lt | CompOp::Le);
        if is_less != first_is_less {
            // 方向不一致 → 编译错误
            panic!("comparison chain direction is inconsistent");
        }
    }
    
    if first_is_less { Forward } else { Backward }
}
```

### 展开示例

```zeta
// 源码
if 0 < i < 10 {}

// 展开后（AST 层面）
if (0 < i) && (i < 10) {}

// 源码
if 0 > i > 10 {}

// 展开后
if (0 > i) && (i > 10) {}
// 即：i < 0 && i > 10 → 逻辑上等价于 "i 在区间外"
// 编译器可以进一步优化为：i < 0 || i > 10
// 但实际上 (0 > i) && (i > 10) 在逻辑上就是 "i < 0 且 i > 10"
// 这在数学上是一个空集（除非用 ||）
```

**重要修正**：`0 > i > 10` 的语义需要重新思考。

### 反向比较链的正确语义

`0 > i > 10` 按数学理解是 "0 大于 i 且 i 大于 10"，这在逻辑上是一个矛盾（i 不可能同时小于 0 且大于 10）。

**正确设计**：反向比较链应该用 `||` 连接，而不是 `&&`。

```rust
/// 正确的展开逻辑
fn expand_comparison_chain(
    exprs: &[Expr],
    ops: &[CompOp],
) -> Expr {
    let direction = check_direction(ops);
    
    let mut comparisons = Vec::new();
    for i in 0..ops.len() {
        comparisons.push(Expr::Binary(
            Box::new(exprs[i].clone()),
            ops[i].clone(),
            Box::new(exprs[i + 1].clone()),
        ));
    }
    
    match direction {
        Forward => fold_with_and(comparisons),   // a < b && b < c
        Backward => fold_with_or(comparisons),   // a > b || b > c
    }
}
```

### 最终展开规则

```zeta
// 正向：区间内 → &&
0 < i < 10  →  (0 < i) && (i < 10)     →  i ∈ (0, 10)

// 反向：区间外 → ||
0 > i > 10  →  (0 > i) || (i > 10)     →  i ∉ [0, 10]
            →  (i < 0) || (i > 10)

// 混合边界
0 >= i >= 10  →  (0 >= i) || (i >= 10)  →  i <= 0 || i >= 10
```

### 编译器优化

```rust
/// 优化展开后的表达式
fn optimize_chain(expr: Expr) -> Expr {
    match expr {
        // (0 > i) || (i > 10) 已经是较优形式
        // 但可以重排为更直观的形式
        Expr::Binary(left, Or, right) => {
            // 如果两侧都是比较，尝试合并
            if let (Expr::Binary(a, Gt, b), Expr::Binary(c, Gt, d)) = (&*left, &*right) {
                // (0 > i) || (i > 10) → i < 0 || i > 10
                // 已经是清晰的形式
            }
            Expr::Binary(left, Or, right)
        }
        other => other,
    }
}
```

## `in` 表达式展开

### 小集合 → `||` 链

```zeta
// 源码
if i in (1, 3, 5) {}

// 展开后
if i == 1 || i == 3 || i == 5 {}
```

### 范围 → 比较链

```zeta
// 源码
if i in (0..10) {}

// 展开后
if 0 <= i < 10 {}

// 源码
if i in [0, 10] {}

// 展开后
if 0 <= i <= 10 {}
```

### 大集合 → 哈希集合（编译期决策）

```zeta
// 源码（超过 8 个元素）
if code in (200, 201, 202, 301, 302, 400, 404, 500, 501, 502, 503) {}

// 编译器生成（伪代码）
static LOOKUP: [i32; 11] = [200, 201, 202, 301, 302, 400, 404, 500, 501, 502, 503];

// 展开为二分查找
if binary_search(&LOOKUP, code).is_some() {}
```

### `not in` 展开

```zeta
// 源码
if x not in (0, 10) {}

// 展开后
if !(x in (0, 10)) {}
// 进一步展开
if !(0 < x < 10) {}
// 即
if 0 >= x || x >= 10 {}
```

## 类型检查

```rust
impl TypeChecker {
    fn check_comparison_chain(
        &mut self,
        exprs: &[Expr],
        ops: &[CompOp],
    ) -> Result<Type> {
        // 1. 所有表达式必须是同一类型（或可统一）
        let types: Vec<Type> = exprs.iter()
            .map(|e| self.infer(e))
            .collect();
        
        for i in 0..types.len() - 1 {
            self.unify(&types[i], &types[i + 1])?;
        }
        
        // 2. 类型必须实现 PartialOrd
        let t = self.substitute(&types[0]);
        if !self.implements(&t, "PartialOrd") {
            return Err(TypeError::NoPartialOrd(t));
        }
        
        // 3. 检查方向一致性
        let first_is_less = matches!(ops[0], CompOp::Lt | CompOp::Le);
        for op in &ops[1..] {
            let is_less = matches!(op, CompOp::Lt | CompOp::Le);
            if is_less != first_is_less {
                return Err(TypeError::InconsistentChainDirection);
            }
        }
        
        Ok(Type::Bool)
    }
    
    fn check_in_expr(
        &mut self,
        expr: &Expr,
        patterns: &[InPattern],
    ) -> Result<Type> {
        let expr_type = self.infer(expr);
        
        for pat in patterns {
            let pat_type = match pat {
                InPattern::Single(e) => self.infer(e),
                InPattern::Range(lo, hi) => {
                    let t1 = self.infer(lo);
                    let t2 = self.infer(hi);
                    self.unify(&t1, &t2)?;
                    t1
                }
            };
            
            self.unify(&expr_type, &pat_type)?;
        }
        
        // 检查 PartialEq
        if !self.implements(&expr_type, "PartialEq") {
            return Err(TypeError::NoPartialEq(expr_type));
        }
        
        Ok(Type::Bool)
    }
}
```

## 边界情况与错误

### 编译错误

```zeta
// 方向不一致
if 0 < i > 10 {}  
// error: comparison chain direction is inconsistent
// help: did you mean `0 < i < 10` or `0 > i > 10`?

// 连续相等（歧义）
if x == y == z {}
// error: chained equality is ambiguous
// help: use `x == y && y == z`

// 类型不一致
if x in (1, "hello") {}
// error: type mismatch in `in` pattern: expected i32, found &str

// 不支持的类型
if f not in (1.0, 2.0) {}
// error: f64 does not implement Eq (NaN 问题)
// help: use explicit comparisons
```

### 警告

```zeta
// 冗余比较
if 0 < x < 100 && x > 50 {}
// warning: redundant condition, consider `50 < x < 100`

// 永远为真/假
if 0 < x < 0 {}
// warning: condition is always false (empty range)

// 可以简化为单边
if 0 <= x && x <= 100 && x >= 0 {}
// warning: redundant lower bound, `x <= 100` is sufficient
```

## 代码生成

### 比较链 → LLVM IR

```llvm
; 源码: if 0 < i < 10
; 生成:
  %i = load i32, i32* %i_ptr
  %cmp1 = icmp slt i32 0, %i
  %cmp2 = icmp slt i32 %i, 10
  %and = and i1 %cmp1, %cmp2
  br i1 %and, label %then, label %else

; 源码: if 0 > i > 10
; 生成:
  %i = load i32, i32* %i_ptr
  %cmp1 = icmp slt i32 %i, 0
  %cmp2 = icmp sgt i32 %i, 10
  %or = or i1 %cmp1, %cmp2
  br i1 %or, label %then, label %else
```

### `in` 小集合 → LLVM IR

```llvm
; 源码: if x in (1, 3, 5)
; 生成:
  %x = load i32, i32* %x_ptr
  %cmp1 = icmp eq i32 %x, 1
  %cmp2 = icmp eq i32 %x, 3
  %cmp3 = icmp eq i32 %x, 5
  %or1 = or i1 %cmp1, %cmp2
  %or2 = or i1 %or1, %cmp3
  br i1 %or2, label %then, label %else
```

## 交付物

- `comparison_chain.rs`：比较链解析与展开
- `in_expr.rs`：`in` 表达式处理
- `expand.rs`：AST 展开规则
- `optimize.rs`：展开后优化
- `codegen_chain.rs`：LLVM IR 生成
- `test_chain.rs`：单元测试
- `bench_chain.rs`：性能基准

## 验收标准

1. 所有正向比较链正确展开为 `&&` 链
2. 所有反向比较链正确展开为 `||` 链
3. 方向不一致时报编译错误
4. `in` 表达式正确展开（小集合/大集合/范围）
5. 零运行时开销（展开后无额外函数调用）
6. 与模式匹配统一（`match` 中可使用比较链）
7. 错误信息清晰，附带修复建议
