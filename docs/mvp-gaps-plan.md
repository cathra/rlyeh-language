# 开发计划 — 剩余任务消解（阶段 G–L：编译器能力补齐；阶段 M–T：标准库深度完善）

> **性质**：本文档为 [`guide.md`](./guide.md) §13「已知限制（MVP）」消解计划与 [`std-lib.md`](./std-lib.md) 剩余任务开发计划的权威副本，承接 [`development-plan.md`](./development-plan.md)（阶段 A–F 全部完成后）的续作。
> **需求来源**：① `docs/guide.md` §13 已知限制（MVP）列出的 7 项规划特性 + 5 项实现约束（阶段 G–L）；② `docs/std-lib.md` 状态总览中 📋 规划 / 🔧 部分章节（阶段 M–T）。
> **总纲与进度速览**：见根目录 [`CODEBUDDY.md`](../CODEBUDDY.md)（§3.9 限制速览表、§5.5 执行记录）。
> **维护规则**：每完成一项任务，需同步更新本文档状态标识 + `CODEBUDDY.md` §3.9 / §5.5 + `guide.md` §13（勾销对应限制条目）/ `std-lib.md` 状态总览（勾销对应规划章节）。

---

## 1. 背景：MVP 已知限制盘点

### 1.1 规划中 / 未实现特性（7 项）

| # | 限制（guide.md §13） | 编译器现状依据 | 归属阶段 |
|---|---------------------|---------------|---------|
| 1 | **宏系统**：✅ 已解决——I1 声明式宏（`macro_rules!` + `$x:expr`/`ident`/`ty`/`tt` + 重复 `*`/`+`/`?`，parse 期展开）、I2 内置格式化宏（`println!`/`print!`/`format!`/`dbg!`，`{}`/`{:?}` 占位）与 I3 集合宏（`arr!`/`vec!`/`map!`）均已实现（见 §4 执行记录） | grammar.md §2.14 `macro_rules!` EBNF 已实现（新 crate `rlyeh-macro`）；lexer `Dollar`/`Question` token；typecheck `check_format_macro`/`check_dbg_macro`；parser `parse_collection_macro`（`arr!` → `ArrayLit`，`vec!`/`map!` → 块表达式 `with_capacity` + `push`/`insert`）；std-lib.md §8 `Display`/`Debug` 仍为规划 API | **I**（I1/I2/I3 ✅） |
| 2 | **引用与借用**：`&x` 表达式、`&T` 参数类型、`str` 类型、解引用 `*`、裸指针均未实现；仅方法接收者 `&self`/`&mut self` 可用 | grammar.md Type 规则含 `'&' Lifetime? 'mut'? Type`、UnaryExpr 含 `'*' | '&' 'mut'?`、Pattern 含 `'ref'`（均已定义未实现）；typecheck `UnaryOp::Deref/AddrOf/AddrOfMut` → Unsupported（check_expr.rs）；borrowck crate 仅服务 `&self` 接收者 | **G**（G1–G4 ✅） |
| 3 | **闭包**：`|x| x + 1` 语法可解析，typecheck 报 Unsupported | parser 已产出 `AstExpr::Closure`；typecheck 报 Unsupported（check_expr.rs） | **H**（H1–H5 ✅） |
| 4 | **运算符**：✅ 已解决——K1 `?` 错误传播、H1 函数指针、H4 `dyn Trait` 均已实现（见 §4 执行记录） | grammar.md 含 `'dyn' TraitBound` 与后缀 `'?'`（已实现）；`Option`/`Result` + `expect`/`unwrap_or` 已实现 | ✅ |
| 5 | **所有权层级**：✅ 已解决——K2 `Box<T>` / K3 `Rc<T>`/`Arc<T>`/`Weak<T>`（引用计数）/ K4 `Gc<T>`（MVP 保守标记-清除 + `gc_region` 生命周期）均已实现（见 §4 执行记录） | memory-model.md §4（Rc/Arc）/§5（Gc）规范完备（含布局、转换矩阵、开销表）；`Box<T>` 亦为 §3 目标 API | **K** |
| 6 | **并发**：`serde`/`fmt` 模块为规划；actor 的 `async` 方法 + `.await`/`send` 已实现；普通函数 `async fn`/`await` 已支持（L1 ✅，MVP 同步语义）；`json` 序列化已实现（L2 ✅：stringify/parse 支持标量/数组/struct/Vec/**HashMap**，见 §4 执行记录） | std-lib.md §8（fmt）规划标注 / §9（serde）已部分实现（`json::stringify`/`json::parse::<T>` 内建，§9.1，含 HashMap 序列化 L2f / 反序列化 L2g）；§10（async 运行时）已部分实现；actor 异步为独立机制，L1 抽象复用其同步语义 | fmt→**I**，serde→L2 ✅，async→L1 ✅ |
| 7 | **迭代器协议**：数值区间、`for x in vec`/`for (k, v) in map`/`for x in arr`（数组迭代 J1）可用；自定义迭代器（`next() -> Option<T>` 方法）接入 `for`（J2）；`map`/`filter`/`fold`/`collect`/`take`/`skip` 适配器可用（J3，返回 Vec） | typecheck for 循环分派（range/Vec/HashMap/数组/迭代器），适配器内建 desugar（check_expr.rs）；std-lib.md §2.3 Iterator 规划 | **J** |

### 1.2 实现约束（5 项）

| # | 约束（guide.md §13） | 现状 | 处置 |
|---|---------------------|------|------|
| 1 | std 模块化（core.rl + time/io/net/sync 子模块） | ✅ 已完成 | 说明性，不纳入计划 |
| 2 | net：`tcp_connect` 依赖平台 `sockaddr_in4` 布局（已双布局化）；WASI 下网络不可用 | 平台约束 | ✅ 已解决（L4：WASI 下 net 明确禁用文档化——`net.rl` 网络函数经 `__rlyeh_target_os` 码 5 短路返回，见 §4 执行记录） |
| 3 | Actor 运行时：交叉编译 / WASM 目标下 actor 程序暂不支持 | 平台约束 | ✅ 已解决（L4：Actor 交叉编译 / WASM 支持——driver 注入静态 `rlyeh_actor_resolve` 符号表替代 dlsym + WASI 单线程同步运行时，见 §4 执行记录） |
| 4 | `String::from(s)`：非字面量 Str（运行期内容）长度表达未实现 | typecheck 报 Unsupported | **G**（str 切片一并补齐） |
| 5 | region 选项：`adaptive`/`with_size (N)` 可用；`strategy (bump)` 等其余选项规划中 | `rlyeh-region-alloc` 已就绪（PGO 回灌 F2 已打通） | ✅ 已解决（L3：`strategy (bump)` 语法 + 运行时 C ABI 接线 + PGO 回灌） |

---

## 2. 计划总览（阶段 G–L）

| 阶段 | 主题 | 关键交付 | 依赖 | 状态 |
|------|------|---------|------|------|
| **G** | 引用与借用（L0 完整化） | `&T`/`&mut T`、`*` 解引用、`str` 切片、裸指针、生命周期 `'a` | 无（地基） | G1–G4 ✅（G4 为语法接受 MVP，borrowck 生命周期检查规划中） |
| **H** | 一等函数 | `fn(A) -> B` 函数类型、闭包（捕获 + `move`）、`dyn Trait` | G（引用捕获） | H1–H5 ✅（H3 为 IIFE MVP；H4 为 dyn Trait MVP；H5 为闭包值对象 MVP） |
| **I** | 宏系统与格式化 | `macro_rules!` 声明式宏、`Display`/`Debug`、`println!`/`format!` | 弱（可与 G/H 并行） | ✅ 已完成 |
| **J** | 迭代器与集合协议 | 数组迭代、`Iterator` trait、`map`/`filter`/`fold`/`collect` | H（适配器闭包） | J1–J3 ✅ |
| **K** | 错误传播与所有权层级 | `?` 运算符、`Box<T>`、`Rc<T>`/`Arc<T>`、`Gc<T>` | G（指针操作） | K1–K4 ✅ |
| **L** | 生态模块与平台收尾 | `async fn`/`await`、`serde`、region `strategy (bump)`、WASI net / Actor 交叉编译 | I（serde 宏）、G 等 | L1–L4 ✅ |

> **推荐执行路线**：
> - **保守路线（依赖驱动）**：G → H → J → K → L，I 按需插入（宏展开器在 parse 后、typecheck 前，与 G/H 解耦，可随时并行）。
> - **快赢路线（体验驱动）**：先做 I1/I2（宏 + `println!` 格式化，开发者日常收益最大、不依赖引用系统）与 G 同步推进；J1（数组迭代）独立且极小，可先行交付。
> - 优先级建议：G1–G2（引用地基 + str）> I1–I2（宏 + 格式化）> H1–H2（函数指针 + 无捕获闭包）> K1（`?` 运算符）> J 全阶段 > 其余。

---

## 3. 阶段详情

### 阶段 G — 引用与借用（L0 完整化）

> 现状：`&x`/`&mut x`/`*` 已实现（G1 ✅）：`&T`/`&mut T` 类型、取址/解引用表达式、参数与返回值引用、标量与聚合引用内存模型、字段访问/方法调用的自动 `peel_ref`、借用宽松规则（`&mut T` 兼容 `&T` 参数，严格可变性互斥留给 borrowck）均可用；`&self`/`&mut self` 方法接收者保持可用；`&str` 视图已实现（G2 ✅）：`String::as_str()` 只读借用（瘦指针，运行时 = 指向 String 对象的指针）、`&str` 参数/返回、字节索引、`String::from(&str)` 深拷贝、视图与 String 内容比较互用；`String::from(String)` 运行期长度表达可用；**`str` 值一等类型已实现（G2 补全）**：`let s = "hi"` 绑定的 Str 值（运行时为指向静态字面量数据的 `i8*` 指针，无 String 的 len/cap 槽）参与方法调用 / `+` 拼接 / 内容比较时自动升级为 String 对象（编译期长度展开）；**String 形参位置的字面量实参自动升级**（`m.push_str("!")`/`m.contains("z")`/`map.insert("k", 1)`/`f("hi")` 直接可用，覆盖实例 / 静态方法、普通函数、函数指针、泛型调用与 `dyn Trait` 方法，与 IIFE / 闭包值实参升级语义一致）；运行期 `str` 值（函数参数）不支持升级报 Unsupported。**裸指针已实现（G3 ✅ MVP）**：`*const T`/`*mut T` 类型 + `*p` 读写（标量 load/store，codegen 与引用同为 `i8*` 槽）+ `&T` ↔ `*const T` 互视（FFI 宽松规则）+ `*mut T` 降级 `*const T`；**生命周期标注已实现（G4 ✅ MVP 语法接受）**：`<'a>` 泛型参数与 `&'a T`/`&'a mut T` 类型解析后丢弃（宽松检查，`'a` 绑定验证规划中）。**严格借用检查已实现（G1 收尾，rlyeh-borrowck）**：NLL 近似的借用排他性规则启用（此前为防御性变体）——`&mut` 与任何活跃借用互斥、多个 `&mut` 互斥、活跃可变借用期间写入被借用变量报 `BorrowConflict`（Rust E0502/E0499 对应）；`&mut` 要求 `let mut` 绑定报 `BorrowMutImmutable`（E0596）；局部引用逃逸函数（尾表达式 / `return` 返回 `&x` 或绑定引用变量）报 `DanglingReference`（E0597，参数来源引用允许返回）；借用存活范围 = 语句粒度 `born..=last_use`（NLL 近似，`last_use` 由预扫描阶段按同一遍历顺序预先收集）；语义有意宽松——读取被借用变量与经 `*p` 写入允许（裸指针别名合法），共享借用（多个 `&`）可共存，仅直接赋值被借用变量触发冲突；`print`/`println` 参数为引用时自动剥层打印解引用值（`println(r)` ≡ `println(*r)`）。**`ref` / `ref mut` 模式已实现（G1 收尾）**：`match` 臂与 `let ref x = e;` 绑定变量为对匹配值的引用（`&T` / `&mut T`）而非值拷贝（枚举子模式 / struct 字段 / 解引用写均可用；MVP 注意：`match` 先拷贝匹配值，`ref` 绑定指向拷贝，`ref mut` 写与原变量无关，见 §4 执行记录）。严格生命周期验证（`'a` 绑定推断）仍规划中。

| 任务 | 内容 | 状态 |
|------|------|------|
| G1 | **引用类型与表达式**：`&T`/`&mut T` 类型、`&x`/`&mut x` 表达式、`*` 解引用；函数参数 `x: &T`、返回 `&T`；`ref` 模式。typecheck 类型规则 + borrowck 借用规则（可变性、悬垂、别名）接线 | ✅（`ref`/`ref mut` 模式与 borrowck 严格借用检查 2026-08 收尾实现：`BorrowConflict`/`BorrowMutImmutable`/`DanglingReference` 三规则 + 7 compile-fail 用例 + `borrow_pass.rl` 10 输出全绿） |
| G2 | **`str` 切片与 String 补齐**：`&str` 引用切片（新增 `as_str()` 只读借用视图，`substring` 保持拷贝返回以免破坏现有 API）；`String::from(s)` 支持运行期 String/str 内容（长度表达），消除 §13 约束 4 | ✅ |
| G3 | **裸指针**：`*const T`/`*mut T` 类型 + `*p` 读写（FFI 场景；codegen 直通） | ✅（MVP：`*const T`/`*mut T` 类型注解、`*p` 读写、`&T` ↔ `*const T` 互视、`*mut` 降级 `*const`；codegen 与引用同构） |
| G4 | **生命周期标注**：`'a` 参数（grammar.md 已定义）、省略规则；borrowck 生命周期检查 | ✅（MVP 语法接受：`<'a>` 与 `&'a T` 解析后丢弃；borrowck 生命周期检查规划中） |

**风险与对策**：借用检查是全语言最复杂的子系统，建议分四步走——先允许（宽松借用，typecheck 报错兜底）→ 严格借用检查 → 生命周期 → 指针；每步以 `cargo test --workspace` + clippy 0 警告为准入。

### 阶段 H — 一等函数（闭包 + 函数指针）

> 现状：`fn(A) -> B` 函数类型 + 函数一等值已实现（H1 ✅：`let f = add` 函数值绑定、`f(args)` 间接调用、作实参/返回值/重新绑定/类型注解；codegen 函数指针 `i8*` 槽 + 按签名 `bitcast` + 间接 `call`）；无捕获闭包已实现（H2 ✅：`|x, y| expr` desugar 为匿名函数 `__closure_N` + 函数指针，需 fn 类型上下文驱动参数推断，捕获外部变量报错）；**捕获闭包已实现（H3 ✅ IIFE MVP）**：`(|x| body)(args)` 立即调用时 body 中引用的外层变量按值捕获（迭代收集 + 类型快照），desugar 为匿名函数 `__closure_N(cap1, cap2, x)`（捕获变量作前置参数、参数名保留原名）+ 普通函数调用，零新增 IR 节点；字符串字面量实参自动升级为 String 语义（与 `let s = "..."` 绑定一致）；`move` 关键字 MVP 忽略（所有权宽松）；**闭包值对象已实现（H5 ✅ MVP）**：`let f = |x: i64| body; f(..)` 绑定后反复调用（参数须全带类型注解，按值捕获，desugar 为捕获聚合对象 + 调用点字段读取展开，仅局部变量环境）；按引用捕获规划中；**`dyn Trait` 已实现（H4 ✅ trait 对象 MVP）**：`dyn Trait` 类型 + `&T` 强制转换（vtable 运行时构造 + 2 槽胖指针）+ 方法调用经 vtable 间接分派；trait/impl 泛型单态化已实现，可为函数类型复用。

| 任务 | 内容 | 状态 |
|------|------|------|
| H1 | **函数类型与函数指针**：`fn(A) -> B` 类型（含泛型实例化）、函数作为值传递与调用（codegen 函数指针） | ✅ |
| H2 | **无捕获闭包**：`|x| expr` desugar 为普通函数 + 函数指针（零开销、无依赖 G） | ✅ |
| H3 | **捕获闭包**：按值捕获 desugar 为匿名结构体（捕获字段 + `call` 方法）；`move` 语义；按引用捕获依赖 G | ✅（IIFE MVP：立即调用 + 按值捕获；闭包值对象见 H5） |
| H4 | **`dyn Trait`**：trait 对象（数据指针 + vtable：drop/size/align/方法表），`dyn Trait` 类型 + 强制转换 | ✅（MVP：非泛型 trait/impl；vtable 槽 0–2 置 0；方法按 trait 声明序入表） |
| H5 | **闭包值对象**：`let f = \|x: i64\| ..; f(..)` 绑定后反复调用（匿名结构体 + `call` 方法的 MVP 形态） | ✅（MVP：参数全注解、按值捕获、仅局部变量环境） |

### 阶段 I — 宏系统与格式化

> 现状：**I1/I2/I3 ✅ 已完成**——新 crate `rlyeh-macro`：`macro_rules!` 声明式宏（`$x:expr`/`$x:ident`/`$x:ty`/`$x:tt` 四类元变量 + `$(`...`)` 重复 `*`/`+`/`?`，matcher/transcriber token 解析，parse 期递归展开为 AST，`MAX_MACRO_DEPTH=64` 防爆栈）——**`$x:expr` 捕获完整表达式 token 序列**（token 级优先级爬升 `consume_expr`：前缀一元 `-`/`!`/`not`/`&`/`*`、二元中缀、后缀调用/索引/成员/`?`/`as` 转换、嵌套宏调用 `m!(x)`；`a > b`/`-1`/`f(x) + 1`/`s.len()` 均可，按原样展开优先级由调用方负责）+ 内置格式化宏 `println!`/`print!`/`format!`/`dbg!`（`{}` 值占位、`{:?}` 同构、`{{`/`}}` 转义、多参数可变长度；typecheck `parse_format_string` + `value_to_string_for_ty` desugar 为 String 拼接 + 内建打印）+ 集合宏 `arr!`/`vec!`/`map!`（parse 期 desugar：`arr!` → `ArrayLit`；`vec!`/`map!` → 块表达式 `Vec::with_capacity(n)`/`HashMap::with_capacity(n)` + 逐元素 `push`/`insert`，空 → `new()`，元素经子 Parser 解析支持嵌套宏调用，零新增 IR 节点）；lexer 新增 `$`/`?` token，`!` 保留 `not` 一元运算符语义；**`r#"..."#` 带哈希原始字符串已实现**（`read_raw_hash_string`：任意 `#` 定界、无转义，与 `r#ident` 原始标识符区分）。限制：无卫生宏（hygiene）。`Display`/`Debug` trait 定义仍为规划 API（std-lib.md §8）。

| 任务 | 内容 | 状态 |
|------|------|------|
| I1 | **宏调用语法区分**：词法/语法层区分 `name!`（宏调用）与 `!`（not 一元运算符）；`macro_rules!` 声明式宏（matcher → transcriber，token 流展开于 parse 后、typecheck 前） | ✅（`rlyeh-macro` crate + lexer `Dollar`/`Question` token + parse 期递归展开；`$x:expr` 捕获完整表达式 token 序列（`consume_expr` 优先级爬升）；限制：无卫生宏） |
| I2 | **`Display`/`Debug` trait + 格式化宏**：`{}` 占位符格式化引擎；`println!`/`print!`/`format!`/`dbg!` 宏；String 拼接语义（`{}` 插入 `to_string`） | ✅（内置格式化宏已实现，typecheck desugar 为 String 拼接 + 内建打印；`Display`/`Debug` trait 定义仍规划） |
| I3 | **集合宏**：`vec!`/`map!`/`arr!`（`arr!` → 数组字面量；`vec!`/`map!` → `with_capacity` + push / `insert` 序列） | ✅（parser `parse_collection_macro` desugar 为 `ArrayLit` / 块表达式，零新增 IR；元素经子 Parser 解析支持嵌套宏调用；空集合 → `new()`） |

### 阶段 J — 迭代器与集合协议

> 现状：J1–J3 ✅ 已完成——for 循环分派（数值区间 / Vec / HashMap / 数组 / 自定义迭代器）已齐；适配器 `map`/`filter`/`fold`/`collect`/`take`/`skip` 经内建 desugar 可用（返回 `Vec<T>`，可链式：适配器结果 Vec 亦可作下一适配器源）。std-lib.md §2.3 `Iterator` trait 仍为规划 API（适配器为编译器内建，非 trait 实现）。

| 任务 | 内容 | 状态 |
|------|------|------|
| J1 | **数组迭代**：`for x in arr`（desugar 为数组下标循环，数组切片模式可复用） | ✅ |
| J2 | **`Iterator` trait**：`next() -> Option<Item>` 核心方法；自定义迭代器接入 `for`（经 trait 方法调用） | ✅ |
| J3 | **适配器**：`map`/`filter`/`fold`/`collect`/`take`/`skip`（依赖 H 闭包） | ✅ |

### 阶段 K — 错误传播与所有权层级

> 现状：`Option`/`Result` + `expect`/`unwrap_or` 已实现；`?` 运算符已实现（K1 ✅：Option/Result 早返回 desugar 为 `match` + `return`，零新增 HIR 节点）；裸无参变体值表达式（`return None;`）已支持（infer_expr Ident 分支补 split_variant_path 兜底）；`Box<T>` 已实现（K2 ✅：`Box::new` 堆分配 + `*` 解引用 + 字段/方法/索引自动剥层）；`Rc<T>`/`Arc<T>` 已实现（K3 ✅：引用计数内建 + 弱引用 + try_unwrap + 自动剥层，布局见 memory-model.md §4 MVP 注记）；`Gc<T>` 已实现（K4 ✅：MVP 保守标记-清除 + `gc_region` 生命周期 + 逃逸对象 + 嵌套块存活提升，布局与生命周期协议见 memory-model.md §5 MVP 注记），见 §4 执行记录。

| 任务 | 内容 | 状态 |
|------|------|------|
| K1 | **`?` 运算符**：Result/Option 早返回 desugar（`match` + `return`），要求函数返回兼容类型 | ✅ |
| K2 | **`Box<T>`**：堆分配目标 API（`Box::new`；解引用依赖 G） | ✅ |
| K3 | **`Rc<T>`/`Arc<T>`**：原子/非原子引用计数（memory-model.md §4 布局；`Rc::new`/`clone`/`try_unwrap`；`Deref` 特判或依赖 G） | ✅ |
| K4 | **`Gc<T>`（插件式）**：标记-清除 GC + `gc_region`（memory-model.md §5；write barrier、逃逸限制） | ✅（MVP 保守标记-清除；write barrier / 增量 / 多线程规划） |

### 阶段 L — 生态模块与平台收尾

| 任务 | 内容 | 状态 |
|------|------|------|
| L1 | **`async fn`/`await`**：普通函数异步支持（actor `async` 方法已有独立机制，抽象复用） | ✅（MVP 同步语义：`async fn` ≡ 同步函数、`expr.await` ≡ 直接值；语法经 parser/typecheck 全程接受，零新增 IR） |
| L2 | **`serde` 序列化模块**：`Serialize`/`Deserialize` trait + `#[derive]` 风格宏（依赖 I） | ✅（MVP：`json::stringify` / `json::parse::<T>` 编译器内建，turbofish 泛型实参 `::<T>`，typecheck 期 desugar 零新增 IR；i64/bool/String/&str/数组/struct/Vec/**HashMap** 序列化（L2f）+ i64/bool/String/**HashMap** 反序列化（L2g）；自定义 `Serialize` trait / `#[derive]` 宏仍规划，见 §4 执行记录） |
| L3 | **region 选项接线**：`strategy (bump)` 等其余选项（`rlyeh-region-alloc` 已就绪，PGO 预测可直接回灌 `adaptive` 初始容量） | ✅（语言级 `strategy (bump)` 语法 + region 指令接线 `rlyeh-region-alloc` C ABI 运行时 + PGO 画像回灌 `adaptive` 初始容量 + driver 链接 region 运行时静态库，见 §4 执行记录） |
| L4 | **平台加固**：WASI 下 net 支持（或明确禁用文档化）；Actor 交叉编译 / WASM 支持（消除 §13 约束 2/3） | ✅（WASI 下 net 明确禁用文档化 + Actor 交叉编译 / WASM 支持，见 §4 执行记录） |

---

## 3b. 标准库深度完善计划（阶段 M–T）

> **需求来源**：[`std-lib.md`](./std-lib.md) 状态总览中标记为 📋 规划 / 🔧 部分的标准库章节（§2.3 Iterator、§4 File/标准输入输出/Path/fs/NIO/sendfile、§5 TCP 对象化/HTTP、§6.2 Channel、§8 `Display`/`Debug`、§9 `Serialize`/`Deserialize`、§10 异步运行时、§11 智能指针目标 API、§12 错误处理）。
> **前置**：阶段 G–L 已全部完成，提供能力地基——G（引用/`&str`/裸指针/严格借用）、H（函数指针/闭包/`dyn Trait`）、I（宏/格式化宏/集合宏）、J（迭代器）、K（`?`/Box/Rc/Arc/Gc）、L（async 同步语义/json/region 指令/WASI）。
> **Rust 绑定层策略**：延续 rlyeh-std「绑定层阶段」——每个新 std 模块先在 `crates/rlyeh-std/src/` 用 Rust 实现 C ABI 绑定（`#[no_mangle] extern "C"`），语言侧 `crates/rlyeh-std/rlyeh/*.rl` 经 FFI 调用封装；**NIO/sendfile 绑定层已就绪**（`rlyeh-std/src/nio/`：poller.rs/sendfile.rs/nonblocking.rs，三平台 epoll/kqueue/poll），阶段 R 为纯语言侧封装。
> **现状修正（std-lib.md 过时标注，规划时以实际为准）**：§8 内置格式化宏已实现（I2：`println!`/`print!`/`format!`/`dbg!` + N4 `eprintln!`/`eprint!`（stderr），`{}`/`{:?}` 占位）；§12 `?` 运算符已实现（K1）；§9 `json::stringify`/`json::parse::<T>` 已实现（L2，trait/derive 仍规划）。

### 3b.1 计划总览（阶段 M–T）

| 阶段 | 主题 | 子任务（关键交付，按序） | 子任务数 | 依赖 | 状态 |
|------|------|---------|:---:|------|------|
| **M** | 错误处理基底 | `IoErrorKind`/`IoError`（M1a/M1b）、`Error`/`From`/`Into` trait（M2a/M2b）、std 错误约定 Result 化（M3a/M3b/M3c） | 7 | K1（`?`）、H4（`dyn Trait`）、G1 | ✅ 已完成 |
| **N** | 文件系统与 IO 对象化 | `File`/`OpenMode`（N1a/N1b/N1c）、stdin/stdout/stderr（N2a/N2b）、`Path`/`fs`（N3a/N3b/N3c）、`eprintln!`/`eprint!`（N4） | 9 | M、G1（`&str`）、I2（宏） | ✅ 已完成 |
| **O** | 网络对象化 | `SocketAddr`/`TcpListener`/`TcpStream`（O1a/O1b/O1c）、字节读写（O2）、HTTP 同步 MVP（O3a/O3b） | 6 | M、L2（json）、G1 | ✅ 已完成 |
| **P** | 并发通道与同步 | Channel 绑定/对象化/收尾（P1a/P1b/P1c）、锁 guard 语义（P2a/P2b）、`Condvar`/`Barrier`（P3） | 6 | K3（Arc）、Mutex ✅；P3 依赖 S0 线程 | ✅ 已完成（P1a–P1c、P2a/P2b、P3） |
| **Q** | 序列化与格式化 trait | serde trait/derive（Q1a/Q1b/Q1c）、json 泛型 API（Q2a/Q2b）、`Display`/`Debug` + Formatter（Q3a/Q3b）、TOML（Q4） | 8 | I（宏）、L2（json）、H4 | ✅ 已完成（Q1–Q4） |
| **R** | 高性能 IO | `Interest`/`Event`/`Poller`（R1a/R1b）、非阻塞（R2）、sendfile（R3） | 4 | O（TcpStream）、rlyeh-std nio ✅ | ✅ 已完成 |
| **S** | 异步运行时 | S0 线程支持（S0a–S0e）、`Future`/`Poll`/`block_on`（S1a/S1b/S1c）、`join_all`/`timeout`/`sleep`（S2a/S2b/S2c）、async channel/http（S3a/S3b） | 13 | P、R、S0 | ✅ 已完成（S0a–S0e、S1a–S1c、S2a–S2c、S3a/S3b） |
| **T** | 集合与迭代器收尾 | Vec/String/HashMap API 补齐（T1a/T1b/T1c）、`Iterator` trait（T2）、智能指针收尾（T3a/T3b） | 6 | J、K、G | ✅ 已完成（T1a/T1b/T1c、T2、T3a/T3b） |

> **任务粒度**：全部任务已拆分为「可独立实现 + 独立验收」的子任务（共 **59 个**），字母后缀（a/b/c）子任务须按序完成（后者依赖前者）；每阶段验收用例按子任务分开（见 3b.2–3b.9 各阶段验收行）。
> **推荐执行路线**：
> - **快赢线**（绑定已就绪 / 独立性强，可先行交付）：M1a/M1b（错误类型）→ M2a（`Error` trait）→ Q1a（serde trait 定义）→ R1a/R1b（Poller 封装）→ P1a/P1b（Channel 绑定 + 对象化）。
> - **主线（依赖驱动）**：M（M1a→M1b→M2a→M2b→M3a→M3b→M3c）→ N（N1a→N1b→N1c→N2a→N2b→N3a→N3b→N3c→N4）→ O（O1a→O1b→O1c→O2→O3a→O3b）→ R（R1a→R1b→R2→R3）→ S0（S0a→…→S0e）→ S1–S3（S1a/S1b → S2a/S2b/S2c → S3a/S3b，S1c 状态机独立排期）。P、Q 与主线并行（依赖交集小），T 随时插入。
> - **优先级建议**：M1a/M1b（错误类型，全部 std 的地基）> N1a（`File` 绑定层，日常收益大）> P1a（Channel 绑定层）> Q1a（serde trait 定义）> R1a（Interest/Event 类型）> O1a（SocketAddr）> S0a（线程绑定层）> 其余。

### 3b.2 阶段 M — 错误处理基底

> 现状：`Option`/`Result` + `expect`/`unwrap_or` 已实现（K1 `?` 已支持）；但 std 自由函数（`read_file`/`write_file`/`append_file`/网络函数）错误约定为"空串 / -1 返回值"，无统一错误类型；`Error`/`From`/`Into` trait 未定义（std-lib.md §12 🔧）。
> 目标：建立贯穿全部 std 模块的错误体系，为 File/fs/TCP 对象化提供前置基底。

| 任务 | 内容 | 状态 |
|------|------|------|
| M1a | **`IoErrorKind` 枚举**：`enum IoErrorKind { NotFound, PermissionDenied, AlreadyExists, InvalidInput, WouldBlock, TimedOut, Other }`（复用 enum/match ✅，独立可验收） | ✅ 已完成 |
| M1b | **`IoError` 结构**：`struct IoError { kind, message: String }` + 构造器/`kind()`/`message()` 访问器（正式 `Display` trait 随 Q3，MVP 先用 `message()`） | ✅ 已完成 |
| M2a | **`Error` trait**：`trait Error { fn message(&self) -> String; }` + `impl Error for IoError` + `describe(e: &dyn Error)` 验证（H4 dyn Trait ✅） | ✅ 已完成（2026-08，`rlyeh-std/rlyeh/io/error.rl`：`trait Error { fn message(&self) -> String; }` + `impl Error for IoError`（`message()` 读 `self.message` 字段）；H4 限制——dyn Trait 仅可作局部变量绑定，`describe` 类调用于函数体内构造 `let d: dyn Error = ...`；验收 `tests/run-pass/error_trait.rl`） |
| M2b | **错误转换约定**（`From`/`Into` 泛型 trait 验证）：验证结论——泛型 trait 声明可解析（`trait From<T>`），但 trait 方法返回 `Self` 未支持（typecheck `undefined type Self`），且 parser 无 where 子句（blanket impl `impl<T, U> Into<U> for T where U: From<T>` 不可行）。**MVP 回退**：`IoError::from_kind(kind)` 窄化转换入口（kind → 默认 message），语义等同 `From::from` | 已完成 |
| M3a | **io 自由函数 Result 化**：`read_file`/`write_file`/`append_file`/`read_line` 从"空串/-1"升级为 `Result<T, IoError>`（io.rl + 绑定层返回码映射） | ✅ 已完成 |
| M3b | **net 自由函数 Result 化**：`tcp_connect`/`send_all`/`recv_some`/`hostname` 同步升级（net.rl） | ✅ 已完成 |
| M3c | **测试与示例迁移**：破坏性变更——同步更新全部受影响的 run-pass/compile-fail 用例与 guide.md/std-lib.md 示例；`?` 运算符在 std 内部使用 | ✅ 已完成 |

**验收**：`tests/run-pass/io_error_type.{rlyeh,out}`（M1：构造/访问器/kind 匹配）+ `error_trait.{rlyeh,out}`（M2：Error impl + dyn 调用）+ `io_result.{rlyeh,out}`（M3：成功路径 Result 解包 + `?` 传播）+ `tests/compile-fail/`（错误路径断言）+ 全量回归。

### 3b.3 阶段 N — 文件系统与 IO 对象化

> 现状：仅 4 个自由函数（`read_file`/`write_file`/`append_file`/`read_line`），无 File 对象、无 Path/fs、无 stdout/stderr（std-lib.md §4.1/§4.2/§4.3 📋/🔧）。

| 任务 | 内容 | 状态 |
|------|------|------|
| N1a | **`OpenMode` + Rust 绑定层**：`enum OpenMode { Read, Write, Append, ReadWrite, Create }` + 新增 `rlyeh-std/src/file.rs`（`std::fs::OpenOptions`/`File` 封装 C ABI：open/create/read/write/flush/metadata/close） | ✅ 已完成（绑定层修订：验证 rlyeh-driver 不链接 rlyeh-std crate，Rust 绑定层无法接线，改为 io.rl 直接 libc extern（fopen 族）+ `open_mode_str` 模式字符串映射，跨平台无 `O_*` flags 差异） |
| N1b | **`File` 对象化**：`File::open_with(path, mode)`/`File::create` + 句柄封装 + `close`（Rlyeh 无 Drop，MVP 显式 `close()` 语义；Y1 起 `File::open(path)` 为默认只读兼容壳） | ✅ 已完成 |
| N1c | **读写与元数据方法**：`read_to_string`/`read(&mut [u8])`/`write(&[u8])`/`write_all`/`flush`/`metadata`/`size` | ✅ 已完成（切片实参降级：`read(cap)`/`write(String)`，`&[u8]` 留待切片借用成熟；`metadata` MVP 返回文件大小） |
| N2a | **stdout/stderr 模块**：`stdout`/`stderr`（`write`/`writeln`/`flush`）+ 绑定层 | ✅ 已完成（新增 extern `write(fd,..)`；`flush` MVP no-op） |
| N2b | **stdin 增强**：`read_to_string`/`lines`（迭代行读取，复用 J1 循环形态） | ✅ 已完成（测试套件无 stdin 注入，`stdin_enhance.rl` 覆盖空 stdin 路径；多行/跨 256B 缓冲逻辑经 `rlyeh build` 产物 + shell 管道手动验证） |
| N3a | **`Path` 对象**：`Path::new`/`join`/`parent`/`file_name`/`extension`/`exists`/`is_file`/`is_dir` | ✅ 已完成（`crates/rlyeh-std/rlyeh/fs.rl`：Path 结构体 + 路径拼接/父目录/文件名/扩展名 + 存在性/文件/目录判断，经 `path_ops.rl` 验收） |
| N3b | **`fs` 核心读写**：`fs::read_to_string`/`fs::write`/`fs::copy` | ✅ 已完成（`fs_ops.rl` 验收：读写往返 + copy） |
| N3c | **`fs` 目录操作**：`fs::remove_file`/`remove_dir_all`/`rename`/`create_dir`/`create_dir_all`/`read_dir`（目录条目迭代） | ✅ 已完成（`fs_dir.rl` 验收：目录创建/遍历/删除；`rename` 经 `r#rename` 根命名空间显式引用修复模块内遮蔽——lexer 保留 `r#` 前缀 + parser 声明名归一化 + typecheck `resolve_callable` 跳过模块内优先，见 §4 执行记录） |
| N4 | **`eprintln!`/`eprint!` 宏**：内置格式化宏扩展 stderr 输出（typecheck 新增分支，desugar 复用格式化引擎 + 内建 stderr 打印，与 `println!` 同构） | ✅ 已完成（parser `is_builtin_macro` + typecheck `builtin_signature`/`check_call` String 分支/宏分发 + LIR/IR 内建注册 + codegen 内建 `eprint`/`eprintln`/`eprint_string`/`eprintln_string`，POSIX `dprintf(2, ...)` 直写 stderr（不依赖 `stderr` 符号，跨平台），经 `eprintln.rl` 验收：占位符/String 变量/非 String 参数/无参空行） |

**验收**：`tests/run-pass/file_io.{rlyeh,out}`（N1：open/读写/flush/metadata）+ `stdout_stderr.{rlyeh,out}`（N2）+ `fs_ops.{rlyeh,out}`（N3：读写/复制/目录遍历）+ `eprintln` 用例（N4）+ 全量回归。

### 3b.4 阶段 O — 网络对象化

> 现状：已从仅自由函数（`tcp_connect`/`socketpair_stream`/`send_all`/`recv_some`/`hostname`）升级为完整 TCP 对象 + SocketAddr + HTTP 同步 MVP（std-lib.md §5.1 ✅ / §5.2 ✅）。

| 任务 | 内容 | 状态 |
|------|------|------|
| O1a | **`SocketAddr`**：`SocketAddr::new(ip, port)` + `ip()`/`port()` 访问器（字符串 IP 解析 + 端口打包） | ✅ 已完成 |
| O1b | **`TcpListener`**：`TcpListener::bind(addr)`/`accept`/`local_addr`；Rust 绑定层 `rlyeh-std/src/net/` 扩展（现有自由函数保留兼容） | ✅ 已完成 |
| O1c | **`TcpStream`**：`TcpStream::connect`/`peer_addr`/`shutdown` + 现有 `tcp_connect` 升级为返回 `TcpStream`（自由函数兼容壳） | ✅ 已完成 |
| O2 | **字节读写**：`read(&mut [u8])`/`write(&[u8])`（数组切片实参，复用 G1 切片；TcpStream 与 File 共用实现）+ 逐行读取 helper | ✅ 已完成 |
| O3a | **HTTP 基础**：`HttpClient`（`get`/`post`：URL 解析 + 请求头构造 + 连接复用） | ✅ 已完成 |
| O3b | **HTTP `Response`**：`text()`/`status` + `json::<T>()` 反序列化（依赖 L2 json） | ✅ 已完成 |

**验收**：`tests/run-pass/tcp_addr.{rlyeh,out}`（O1a：SocketAddr/IPv4 解析，`127.0.0.1:8080` → `10.1.2.3:9999` → 端口打包）+ `tcp_echo.{rlyeh,out}`（O1b/O1c/O2：自连接 echo——bind→local_addr→connect→accept→read/write→read_line→shutdown）+ `crates/rlyeh-driver/tests/net_http_test.rs`（O3：本地 mock 服务器免外网，`http_get_json`（200 + JSON + `json::parse::<i64>`）/ `http_post_echo`（Content-Length + 服务器端 echo）/ `http_not_found_status`（404）3/3 通过）+ 全量回归。

**执行记录（2026-08）**：
- **无 Rust 绑定层**：`net.rl` 直接 libc extern FFI（`socket`/`bind`/`listen`/`accept`/`connect`/`getsockname`/`send`/`recv`/`setsockopt`/`shutdown`），`socklen_t` 以 8 字节小端缓冲传递，未扩展 `rlyeh-std/src/net/` Rust 侧（计划表中"Rust 绑定层扩展"一项以 Rlyeh 侧直绑落地）。
- **平台自适应 sockaddr_in 布局**：`__rlyeh_target_os()` 区分 macOS（`sin_len` 头字节）与 Linux，`parse_sockaddr`/`to_sockaddr` 双布局读写；WASI（码 5）网络函数短路返回 `Err`。
- **`$` 结果化**：所有网络 API 返回 `Result<T, IoError>`，`IoErrorKind` 分类（M 阶段基底复用）。
- **HTTP 连接模型**：每次请求新建连接 + `Connection: close`（无连接复用，MVP 语义；`_unit` 占位字段规避空结构体构造限制）；`HttpClient::get/post` 经 `tcp_connect` 自由函数 + 手工结构体字面量构造（模块限定关联函数调用不可用约束）。
- **json::parse 语义确认**：返回裸 `T` 非 `Result`（非法输入给默认值），测试直接用 `println(json::parse::<i64>(r.text()))`。

### 3b.5 阶段 P — 并发通道与同步

> 现状：`Mutex`/`RwLock` 裸 `lock/unlock/try_*` 已实现（§6.1 ✅）；`Channel` 已实现（§6.2 ✅）；`Condvar`/`Barrier` 已实现；guard 语义（`lock_guard` + 块尾自动解锁注入）已实现。**实现方式修正**：P1/P3 未采用 Rust 绑定层（`rlyeh-std/src/channel.rs`），而是与 sync/io/net 一致——纯 Rlyeh 侧 pthread extern FFI（`sync/module.rl`），MVP 元素限 `i64`、无界队列、queue 只增（head 单调推进）；`S0 线程已支持`（driver 注入 `__rlyeh_thread_spawn`），Condvar/Barrier 可在多线程下验证。

| 任务 | 内容 | 状态 |
|------|------|------|
| P1a | **无界队列 Channel**：`sync::Channel`（Mutex + Condvar 队列，`Rc<Channel>` 共享；`send`/`recv`/`try_send`/`try_recv`/`close`；无界队列 send 恒不阻塞，recv 空挂起） | ✅ 已完成 |
| P1b | **`Sender`/`Receiver` 对象化**：`channel()` → `ChannelPair { tx, rx }` + `send`/`recv` + 多 Sender/Receiver 共享（`Rc` clone，K3 ✅；目标 API 为 `Arc` 泛型版，规划） | ✅ 已完成 |
| P1c | **`try_*`/`close`/`iter`**：`try_send`（恒 true）/`try_recv`（空 → None）+ `close`（recv 耗尽返回 None）+ `iter`（`next()` 接入 for 循环，J2 形态） | ✅ 已完成 |
| P2a | **注入机制验证**：编译器对 guard 局部变量作用域结束自动 `unlock` 注入（`rlyeh-desugar/src/guard.rs` 对方法名 `lock_guard` 特判，块尾注入；if/match 分支内提前 return/break 不注入） | ✅ 已完成 |
| P2b | **`MutexGuard` 接线**：`Mutex::lock_guard()` 返回 `MutexGuard`（`p` 裸指针承载 pthread_mutex_t*）+ 作用域结束自动解锁注入；`RwLockWriteGuard` 目标 API 规划 | ✅ 已完成 |
| P3 | **`Condvar`/`Barrier`**：`Condvar::wait/notify_one/notify_all`（与 Mutex 配对）+ `Barrier::new(count)/wait`（`PTHREAD_BARRIER_SERIAL_THREAD` 领头线程语义简化） | ✅ 已完成 |

**验收**：`tests/run-pass/channel.{rlyeh,out}`（P1 ✅：同线程 send/recv + 多 Sender 共享 + try_* + close 耗尽 None + iter 接入 for）+ `tests/run-pass/mutex_guard.{rlyeh,out}`（P2 ✅：guard 持锁期 try_lock=false、块尾/if 分支/多守卫/while 循环体自动解锁、嵌套块语句须加分号）+ 全量回归（P1/P2 均纳入 `suite_test`）。

### 3b.6 阶段 Q — 序列化与格式化 trait

> 现状：`json::stringify`/`json::parse::<T>` 编译器内建已实现（L2 ✅，含 HashMap，turbofish 泛型实参）；`Serialize` trait + `#[derive]` 标记 + struct 反序列化已实现（Q1 ✅，`serde/module.rl` + `json_parse_ast` struct 分支）；`Display`/`Debug` trait 定义仍规划（§8 🔧，内置格式化宏 I2 已实现）。

| 任务 | 内容 | 状态 |
|------|------|------|
| Q1a | **`Serialize`/`Deserialize` trait 定义**：`trait Serialize { fn to_json(&self) -> String; }` + `trait Deserialize { fn from_json(s: String) -> Self; }`（**风险验证结论**：`-> Self` 返回自身类型未支持——typecheck `undefined type Self`，见 M2b） + 内建类型（标量/数组/Vec/HashMap/String）默认 impl | ✅ 已完成（`Deserialize` 按预案退化：`from_json` 未定义，解析统一走编译器内建 `json::parse::<T>`；内建类型默认 impl 为声明性文档——MVP 内建类型方法调用不走 trait impl 查找，序列化经 `json::stringify` 特判；自定义类型可手写 `impl Serialize for T` 并经 `to_json()` 调用） |
| Q1b | **derive 宏语法**：`#[derive(Serialize, Deserialize)]` 解析（lexer 新增 `Pound` token + parser `parse_attributes` 特判，`AstStructDecl.derive` 存储 trait 名列表；其它 attribute 名报错、非 struct 项宽松忽略） | ✅ 已完成 |
| Q1c | **derive 生成接线**：struct 派生 impl——序列化复用 L2 内建 stringify（字段序 = 定义序）；反序列化 `json_parse_ast` 新增 struct 分支（desugar 为块表达式：`String::from` → `substring` 剥离 `{}` → `split(",")` → `for` 遍历 → 字段名匹配 if-else 链逐字段 `Assign`，值递归 `json_parse_ast`；字段顺序任意、缺失字段保持零值、未知字段忽略、嵌套 struct 支持；嵌套 struct/Vec/HashMap 值含逗号经 split 分段错误的 MVP 限制；泛型 struct 不支持；空 struct 报 Unsupported） | ✅ 已完成 |
| Q2a | **泛型 API 入口**：`json::to_string<T: Serialize>`/`from_str<T: Deserialize>`（turbofish 泛型实参；保留既有内建 `stringify`/`parse` 兼容） | ✅ 已完成（`T: Serialize` / `T: Deserialize` trait bound 未支持——MVP 无泛型 trait 约束，签名退化为无 bound turbofish 形式；typecheck 内建别名接线：`json::to_string(v)` ≡ `json::stringify(v)`、`json::from_str::<T>(s)` ≡ `json::parse::<T>(s)`） |
| Q2b | **流式 writer/reader**：`to_writer`/`from_reader`（目标 File/TcpStream，N/O 完成后接线） | ✅ 已完成（`json::to_writer(w, v)` → `w.write_all(json::stringify(v))`，返回 `Result<i64, io::error::IoError>`；`json::from_reader::<T>(r)` → `json::parse::<T>(r.read_to_string().unwrap())`，读失败经 `unwrap` 死循环 MVP 语义；首参须 `File`/`&File`/`&mut File`（短名或 `io::file::File` 路径后缀匹配），TcpStream 留待流式 read_all 方法化） |
| Q3a | **`Display`/`Debug` trait + `Formatter` 类型**：`impl Display for T { fn fmt(&self, f: &mut Formatter) }`（`Formatter` 为 `&mut` 引用参数——G1 ✅ 已支持） | ✅ 已完成（std `fmt/module.rl`：`struct Formatter { buf: String }` + `impl Formatter { pub fn new() }`；`trait Display { fn fmt(&self, f: &mut Formatter) -> String }`、`trait Debug { fn fmt_debug(&self, f: &mut Formatter) -> String }`；core.rl 注入 `mod fmt;` + `use fmt::{Display, Debug, Formatter}`。MVP 签名降级：fmt 直接返回显示字符串（与 `to_json() -> String` 同构；目标 API `Result<(), FmtError>` 未支持）；`Debug` 方法名用 `fmt_debug` 避免与 Display::fmt 同名冲突（方法调用按名查找 impl）；`Formatter` 为约定占位类型。配套修复：模块内 trait/impl 方法签名在收集阶段 `resolve_ast_type` 时 use 段未注册——`resolve_full_name` 加当前模块前缀回退，`collect_item_decls`/`collect_mod_types_inner`（增量哈希路径）/`check_item` 的 ModDecl 分支设置 `module_prefix`） |
| Q3b | **格式化引擎接入**：`{}` 占位解析优先查 `Display` impl（内建类型保持现状，自定义类型走 trait 方法），`{:?}` 走 `Debug`（I2 收尾） | ✅ 已完成（`parse_format_string` 区分 `{}`/`{:?}`（`FormatSeg.is_debug`）；`value_to_string_for_ty` 对自定义类型查 `fmt`/`fmt_debug` 方法，生成 `{ let mut __fmt_q3 = Formatter::new(); x.fmt(&mut __fmt_q3) }` 块表达式（`&mut` 仅支持变量目标，临时值不可用）；`dbg!` 走 Debug 路径；内建类型行为不变；无 impl 报 Unsupported 提示 `impl Display`/`impl Debug`） |
| Q4 | **TOML 模块**（轻量）：基础标量/嵌套表/数组 stringify/parse | ✅ 已完成（`toml::to_string(v)` ≡ `toml::stringify(v)`（序列化：i64/bool/String/&str 标量、数组 `[T; N]`/`Vec<T>`（`[e1,e2]`）、嵌套表——顶层 struct 多行 `f1=v1\nf2=v2` + 嵌套字段内联表 `{x=1,y=2}`、HashMap 内联表 `{"k"=v,...}`（键限 i64/String，键带引号））；`toml::from_str::<T>(s)` ≡ `toml::parse::<T>(s)`（反序列化 round-trip 对齐 stringify 的紧凑输出：标量引号剥离/转义还原（复用 core.rl `json_escape`/`json_unescape`，TOML 基本转义与 JSON 一致）、`Vec<T>` 数组剥 `[ ]` + `split(",")` 逐元素（元素限标量）、struct 行式 `split("\n")` 字段名匹配 if-else 链（字段序无关、缺失零值、未知忽略；嵌套字段内联表剥 `{ }`）、HashMap 内联表剥 `{ }` + `find("=")` 键值分段（键先 json_unescape 再 string_to_int，值限标量））。MVP 签名降级：无泛型 trait 约束（`T: Serialize`/`T: Deserialize` bound 不支持），退化为无 bound turbofish 形式（同 Q2）；数组类型 `[T; N]` 反序列化不支持（用 Vec）；f64 不支持；紧凑输出（TOML 语法合法）——标准 TOML 的 `key = value` 空格形式、`[section]` 行式子表、注释、多行字符串规划中；stringify/parse 均无 std 模块文件（typecheck 内建接线，同 json） |

**验收**：`tests/run-pass/json_derive.{rlyeh,out}`（Q1 ✅：derive 标记 struct 序列化/反序列化 round-trip + 字段顺序任意 + 缺失字段零值 + String/bool 字段 + 嵌套 struct + 手写 `impl Serialize` 的 `to_json()` 调用）+ `tests/run-pass/json_api.{rlyeh,out}`（Q2 ✅：`to_string`/`from_str` 别名兼容 + `to_writer` 写文件返回写入字节数 + `from_reader` 读文件 round-trip）+ `tests/run-pass/display_fmt.{rlyeh,out}`（Q3 ✅：手写 `impl Display`/`impl Debug` 的 `{}`/`{:?}` 占位 + `format!` + `dbg!` Debug 格式 + 内建类型兼容）+ `tests/run-pass/toml_io.{rlyeh,out}`（Q4 ✅：`toml::to_string`/`from_str` 的标量/数组/Vec/嵌套表/HashMap stringify/parse + round-trip + 字段序无关/缺失零值 + i64 键 HashMap）+ 全量回归（Q1–Q4 已纳入 `suite_test`）。

### 3b.7 阶段 R — 高性能 IO（NIO + sendfile）

> 现状：**已完成**（2026-08，R 阶段收官）。实际实现路线：语言侧 `io/nio.rl` 基于 poll(2) 封装（POSIX 统一，替代规划中的 epoll/kqueue 绑定层路线）+ fcntl O_NONBLOCK + driver 注入平台内建 `__rlyeh_sendfile`（macOS sendfile(2) 6 参签名，Linux/Windows/WASI 差异在注入层屏蔽）；`Result<T, IoError>` 化；验收为 `crates/rlyeh-driver/tests/nio_test.rs` 集成测试（7 用例，含非阻塞往返、Poller 就绪/reregister/deregister/错误路径、sendfile 自由函数与 `File::sendfile_to`），std-lib.md §4.4/§4.5 已标 ✅。

| 任务 | 内容 | 状态 |
|------|------|------|
| R1a | **`Interest`/`Event` 类型**：`Interest`（读/写/读写）+ `Event`（token + interest，`is_readable`/`is_writable`，revents 解析） | ✅ 已完成（nio.rl：POLLIN=1/POLLOUT=4 掩码映射，POLLERR/POLLHUP 按可读报告） |
| R1b | **`Poller` 封装**：`Poller::new`/`register`/`reregister`/`deregister`/`poll(timeout_ms)`——poll(2) 注册表状态容器（fds/events/tokens 三数组），重复 register→AlreadyExists、未注册 deregister→NotFound | ✅ 已完成（nio.rl） |
| R2 | **非阻塞**：`set_nonblocking(fd, bool)`/`is_nonblocking(fd)`（fcntl F_GETFL/F_SETFL + O_NONBLOCK=0x4；WASI 下短路 Err 禁用文档化） | ✅ 已完成（nio.rl） |
| R3 | **`sendfile` 模块 + `File::sendfile_to(sock_fd, offset)`**：零拷贝传输（driver 注入 `__rlyeh_sendfile` 平台内建；count=0 发到 EOF；**in_fd 须为只读 fd**，O_WRONLY 句柄报 EBADF，需 reopen Read 后调用） | ✅ 已完成（io/sendfile.rl + io/file.rl） |

**验收**：`crates/rlyeh-driver/tests/nio_test.rs`（R1/R2/R3 集成测试 7 用例全绿：非阻塞往返、Poller 就绪/改向/注销/错误路径、sendfile 自由函数与 `File::sendfile_to` 零拷贝传输）+ 全量回归。

### 3b.8 阶段 S — 异步运行时（线程前置）

> 现状：普通函数 `async fn`/`.await` 为 MVP 同步语义（L1 ✅，`async fn` ≡ 同步函数、`.await` ≡ 直接求值）；`Future`/executor/`block_on`/`join_all`/`timeout` 为规划 API（std-lib.md §10 🔧）。**线程支持（S0a–S0d）已完成（2026-08）**：实际实现走「driver 注入平台内建 + 语言侧 extern」路线（与 sendfile 同架构），替代计划中的 Rust 绑定层——`__rlyeh_thread_spawn/join/self`（pthread 绑定，`thread_builtin_ir`）+ `rlyeh-std/rlyeh/thread/module.rl`（`Thread::start/join/current`）。

| 任务 | 内容 | 状态 |
|------|------|------|
| S0a | **线程绑定**：driver 注入 `__rlyeh_thread_spawn`/`__rlyeh_thread_join`/`__rlyeh_thread_self`（pthread_create/join/self C ABI 封装，线程入口 `i64 (i8*)*`；替代原计划 `rlyeh-std/src/thread.rs` Rust 绑定层） | ✅ 已完成 |
| S0b | **`Thread::start(f: fn() -> i64)`**：语言侧 `thread` 模块（`spawn` 为保留关键字，方法名取 `start`）；函数指针值按地址整数经 extern i64 形参传递（typecheck 放宽 Fn→I64 + codegen ptrtoint） | ✅ 已完成 |
| S0c | **`join` + 返回值传递**：`Thread::join` 返回值槽读取（pthread_join i64*）；启动失败映射 `IoError`（M1b） | ✅ 已完成 |
| S0d | **WASI/Windows 短路**：注入层 os 码 4/5 返回 -1 的 stub，`Thread::start` 返回 `Err`（Unsupported） | ✅ 已完成 |
| S0e | **加固**：栈分配（默认栈大小确认）、线程局部状态确认、内存模型文档化（MVP 无 TLS 需求） | ✅ 已完成（pthread_create attr=NULL 系统默认栈：Linux glibc 约 8MB、macOS 约 512KB；stack_size 定制已由 Y8 ✅ 落地——`thread::Builder` pthread_attr_setstacksize；MVP 无 TLS 已确认；内存模型——共享数据经同步原语、数据竞争 UB 调用方负责，与 C 并发模型一致；thread_builtin_ir 注释 + thread/module.rl 头注释文档化） |
| S1a | **`Future`/`Poll` trait 定义**：`enum Poll<T> { Ready(T), Pending }` + `trait Future { fn poll(&mut self) -> Poll<i64>; }`——关联类型 `type Output` / `Pin<&mut Self>` / `Context` 验证不可行（parser 无 trait `type` 成员、dyn 不可作函数参数），按计划退化指示 Output 固定 i64；`rlyeh-std/rlyeh/future.rl` + core.rl 重导出 | ✅ 已完成 |
| S1b | **`block_on` 手动轮询 MVP**：`block_on<T>(f: &mut T) -> i64` 泛型形态（loop+match 轮询，实例化时按具体类型解析 poll，无约束宽松语义） | ✅ 已完成 |
| S1c | **`async fn` 状态机**：L1 同步语义改造为真实状态机（`await` 挂起/恢复）——desugar（`rlyeh-desugar`：analyze 段切分 + generate 结构体/impl/构造器生成），状态编号 `2k`/`2k+1`（首轮询/恢复），跨 await 变量提升，`block_on` 轮询驱动 | ✅ 已完成 |
| S2a | **`sleep`**：`thread::sleep(Duration)`——注入 `__rlyeh_thread_sleep`（usleep 绑定，micros 截断 u32 上限约 71 分钟）；补 `Duration::seconds/milliseconds` 构造器；`Instant` 基准 clock()（CPU 时钟）睡眠期间不推进（墙钟随 S2b 接入） | ✅ 已完成 |
| S2b | **`join_all` + 墙钟**：线程版 `thread::join_all(Vec<Thread>) -> Vec<i64>`（并发等待多线程，按传入顺序收集返回值）；墙钟 `__rlyeh_clock_monotonic`（clock_gettime CLOCK_MONOTONIC，注意 macOS 常量 =6 与 Linux =1 差异）接入，`Instant::now/elapsed` 睡眠期间推进（不支持平台退回 clock()）。Future 泛型版规划（依赖 S1c + 泛型方法） | ✅ 已完成 |
| S2c | **`timeout`**：Future 超时包装——`timeout<T>(duration, &mut fut) -> Result<i64, i64>`（MVP 退化：`TimeoutError` 规划中，超时 `Err(-1)`；参数顺序对齐规划 API；超时判定经墙钟 `__rlyeh_clock_monotonic`，返回 -1 退回 clock()——MVP 静态方法调用不支持模块路径前缀故直用 extern；忙等轮询，事件驱动规划随 R1 Poller）。Future 版 `Result<F::Output, TimeoutError>` 规划（依赖 S1c） | ✅ 已完成 |
| S3a | **Channel `recv_async`**：异步接收（依赖 P1 + S1/S2 事件循环） | ✅ 已完成（MVP 退化：阻塞语义，等价 `recv`——非空立即返回 / close 后空返回 None；事件驱动版规划随 R1 Poller + 事件循环，`sync/module.rl` `Receiver::recv_async` + channel.rl 用例） |
| S3b | **HTTP async 方法**：`HttpClient` 异步接口（依赖 O3 + S2） | ✅ 已完成（MVP 退化：同步语义，等价 `get`/`post`——`HttpClient::get_async`/`post_async`，`net/http.rl` + net_http_test 2 用例；事件驱动版规划随 S3 事件循环 + §4.4 NIO） |

**验收**：`crates/rlyeh-driver/tests/thread_test.rs`（S0 ✅：start/join 返回值、双线程并行求和、current 正数断言 3 用例全绿；S2a ✅：sleep 返回值 0 断言；S2b ✅：join_all 顺序收集 [10,20,30] 求和 60 + 墙钟 sleep 30ms 后 elapsed ≥ 30ms 断言 2 用例，共 6 用例全绿）+ `crates/rlyeh-driver/tests/time_test.rs`（S2c ✅：timeout 成功路径 Ok(3) + 超时路径 Err(-1) 2 用例，共 5 用例全绿）+ `tests/run-pass/block_on.{rlyeh,out}`（S1a/S1b ✅：Poll 构造/解构 + Future impl 三轮轮询 + block_on 手动轮询，输出 42/3）+ `tests/run-pass/sleep_join.{rlyeh,out}`（S2a ✅：sleep 返回值 0 + 线程存活断言，输出 sleep-ok）+ `tests/run-pass/join_all.{rlyeh,out}`（S2b ✅：3 线程各 sleep 20ms + join_all 收集求和 60 + 墙钟 elapsed ≥ 20ms，输出 join-all-ok）+ `tests/run-pass/timeout.{rlyeh,out}`（S2c ✅：3 轮后 Ready → Ok(3) + 恒 Pending 50ms → Err(-1)，输出 3/-1）+ `tests/run-pass/async-fns.{rlyeh,out}`（S1c ✅：无 await 单尾段 42、嵌套 await 21、多 await 链 + 跨段变量 18、语句形式 await + return await 40、手动 Pending 恢复跨 await 199）+ `tests/run-pass/async_await.{rlyeh,out}`（S1c ✅：手写状态机与 desugar 同构对照，输出 43）+ `tests/run-pass/channel.rl`（S3a ✅：recv_async 非空立即返回 7 + close 后空返回 None，输出 7/-1；阻塞等待语义与 recv 共享路径，MVP 单线程不可测跨线程数据流——线程入口零参数 + Rc 非线程安全，文档化）+ `crates/rlyeh-driver/tests/net_http_test.rs`（S3b ✅：get_async/post_async 与 get/post 同步语义一致 2 用例）+ S0e ✅ 文档化（thread_builtin_ir 注释 + thread/module.rl 头注释，见上）+ 全量回归（zio/net/nio/fs 系列 18 测试集全绿）。**风险**：线程是编译器 + 运行时最大新子系统（FFI 签名、栈布局、WASM 隔离），MVP 建议"block_on 轮询 + 后台线程池"；S1c 状态机已落地（MVP 支持直线 await、Pending/Ready 恢复），控制流块内 await 与表达式嵌套 await 规划中。

### 3b.9 阶段 T — 集合与迭代器收尾

> 现状：Vec/HashMap/String 核心 API 已实现（§3 ✅），适配器内建 desugar 可用（J3 ✅）；`Iterator` trait 定义仍规划（§2.3 📋）；智能指针目标 API 部分缺（§11 🔧，`Box::leak` 等）。

| 任务 | 内容 | 状态 |
|------|------|------|
| T1a | **Vec 目标 API 补齐**：`sort`/`binary_search`/`iter`（按 std-lib.md 目标 API 清单核对） | ✅ 已完成（`sort`/`binary_search` 已有 ✅；新增 `iter`/`iter_mut`（2026-08 V1 瘦指针迭代器——`Iter<T> { data: *const T, len }` / `IterMut<T> { data: *mut T, cur, len }`，零分配零拷贝，`next() -> Option<T>` 值拷贝读取 + `IterMut::write(x)` 经 DerefSet 写回最近 next 读取的元素；编译器特判构造 `Iter::new`/`IterMut::new`；接入 for 与 J3 适配器；目标 `Iter<'_, T>` 借用迭代器（引用元素 `Option<&T>`）仍规划）、`get_mut`（值拷贝，目标 `&mut T` 引用规划）、`sort_by`（比较器闭包 `fn(T, T) -> i64` 三态语义，选择排序）。**附带修复**：typecheck `substitute`/`unify` 补 `Type::Fn` 递归分支 + 实例方法实参循环补闭包特判——泛型方法 `fn(T, T)` 形参此前不替换/不反推，函数名与闭包实参均报错；修复后 `Vec::sort_by` 等泛型 fn 形参方法可用（全量回归无破坏）） |
| T1b | **String 目标 API 补齐**：`chars`/`lines`/`split`（部分已有）/`replace`/`to_uppercase`/`to_lowercase`/`trim` | ✅ 已完成（`split`/`replace`/`trim` 已有 ✅；新增 `chars`（MVP 字节级——逐字节 i64 列表，目标 `Chars` 迭代器 + UTF-8 码点解码规划）、`lines`（委托 `split("\n")`，目标 `Lines` 迭代器规划；`\r\n` 行尾 `\r` 保留）、`to_uppercase`/`to_lowercase`（`to_upper`/`to_lower` 的 API 别名，ASCII 语义）） |
| T1c | **HashMap 目标 API 补齐**：`iter`/`keys`/`values` 等（按 std-lib.md 目标 API 清单核对） | ✅ 已完成（`keys`/`values`/`clear` 等已有 ✅；新增 `iter`（MVP 退化——返回键缓冲，与 `keys` 同构，目标 `Iter<'_, K, V>` 键值对迭代器规划）、`get_mut`（值拷贝，目标 `&mut V` 引用规划）） |
| T2 | **`Iterator` trait 定义**：`trait Iterator { type Item; fn next(&mut self) -> Option<Item>; }`——**风险**：先做关联类型 `type Item` 最小验证（H4 仅验证过非泛型 dyn）；适配器**保持内建 desugar 不迁移**（避免重构风险），trait 定义先行供自定义迭代器接入（J2 已支持方法式 `next() -> Option<T>`，trait 化为可选演进） | ✅ 已完成（MVP 退化：关联类型 `type Item` 验证不可行——parser/typecheck 无 trait `type` 成员载体（S1a 已记录），`trait Iterator { fn next(&mut self) -> Option<i64>; }` 元素固定 i64（core.rl 顶部定义）。自定义迭代器 `impl Iterator for T` 后经 for 接入 ✓（check_for_iterator 检测 next() 方法，inherent 或 trait impl 均可）；适配器保持内建 desugar 不迁移 ✓。泛型元素迭代器（如 `StdinLines` 返回 `Option<String>`）仍走方法式接入，注释文档化） |
| T3a | **`Box::leak`**：泄漏堆对象返回裸指针 | ✅ 已完成（`Box::leak(b)` → `*mut T` 裸指针（G3 语义，`*p` 读写可用），读取 Box 槽 0 指针值不释放。MVP 退化：目标签名 `fn leak(self) -> &'static mut T`——`&*b` 堆地址取引用需 MIR `AddrOf` 支持任意目标表达式（当前仅变量取址），退化为裸指针值语义等价，`'static` 宽松丢弃（G4）） |
| T3b | **Rc/Arc 目标 API + `Weak::upgrade` 完善**（std-lib.md §11 目标 API 清单逐项核对） | ✅ 已完成（逐项核对：`Rc::new`/`strong_count`/`weak_count`/`downgrade`/`try_unwrap`、`Arc::new`/`strong_count`/`weak_count`、`Weak::new`/`upgrade` 已全覆盖（rc_new.rl 验收），无新增缺口） |

**验收**：`tests/run-pass/vec_api.{rlyeh,out}`（T1a ✅：iter 快照 + for 求和 15、get_mut 值拷贝 8、sort_by 闭包降序 `5 4 3 1 1` 与升序 `1 1 3 4 5`）+ `string_api.{rlyeh,out}`（T1b ✅：chars 字节级 3/65/33、lines 3 行含尾空行段、to_uppercase/to_lowercase 别名与原名一致 true）+ `hashmap_api.{rlyeh,out}`（T1c ✅：iter 键求和 3、iter/keys 同构 3、get_mut 200/-1/300）+ `iterator_trait.{rlyeh,out}`（T2 ✅：Counter/Step 自定义迭代器 `impl Iterator` 经 for 接入，输出 `10/0/12` + 直接 next 调用 `1/2/3/-1`）+ `box_leak.{rlyeh,out}`（T3a ✅：`*mut i64` 读写 42/99、Box\<String\>/Box\<Point\> 解引用 4/3、类型注解 `*mut i64` 7）+ T3b ✅（rc_new.rl 已全覆盖核对）+ 全量回归（30+ 测试集 + suite_test 全绿）。

> **T 阶段已知限制**（实测发现）：typecheck 变量环境 `variables: HashMap<String, Type>` 按名全局索引、**无作用域隔离**——同名遮蔽（如 match 臂绑定与后续 let 绑定同名）时类型互相覆盖，后续按类型分支的输出可能异常（实测 i64 200 被以 `%p` 打印为 `0xc8`）；~~当前建议避免同名变量遮蔽，作用域栈重构（按作用域分层存储变量类型）规划中~~。✅ 已解决（U1 作用域栈重构完成：按作用域分层 + 块级遮蔽存储槽 mangle，同名变量互不覆盖，见 §4 执行记录）。

---

## 3c. 标准库后续完善计划（阶段 U–Z：目标 API 对齐与编译器能力补齐）

> **需求来源**：[`std-lib.md`](./std-lib.md) 各章节「目标 API（规划）」「MVP 退化」「规划中」标注，以及阶段 M–T 完成后遗留的**已知退化项**（M2b/Q1a/S1a/T1a/T1b/T1c/T2/T3a 的 MVP 降级实现）。
> **前置**：阶段 G–T 已全部完成（2026-08-24），MVP 标准库完整可用（错误体系 / IO 对象化 / 网络 / 并发 / 序列化 / NIO / 线程 / 异步状态机 / 集合收尾）。
> **定位**：本计划不做新语言特性，聚焦 ① 消除已实测验证的编译器能力缺口（阶段 U，全部为 std 完整化的硬前置），② 把 MVP 退化项升级为 std-lib.md 目标 API（阶段 V–Y）。
>
> **编译器能力缺口盘点（阶段 U 集中解决）**：
>
> | # | 缺口 | 实测依据 | 影响面 |
> |---|------|---------|--------|
> | 1 | typecheck 变量环境**按名全局索引、无作用域栈**，同名遮蔽互相覆盖（后续按类型分支输出异常） | T 阶段已知限制（§3b.9 / std-lib.md 顶部） | `get_mut` 引用语义、借用迭代器、引用返回值 |
> | 2 | trait **关联类型** `type Item` / `type Output` 无载体 | S1a/T2 已验证（parser/typecheck 无 trait `type` 成员） | `Iterator` 泛型元素、`Future` 泛型 Output |
> | 3 | **泛型 trait 约束** `T: Bound`（where 子句 / bound）不可用 | M2b 已验证（blanket impl 不可行） | `json::to_string<T: Serialize>`、`timeout<F: Future>` |
> | 4 | trait 方法返回 **`-> Self`** 未支持（typecheck `undefined type Self`） | M2b/Q1a 已验证 | `Deserialize::from_json`、`From::from`、`Into::into` |
> | 5 | **MIR `AddrOf` 仅支持变量取址** | T3a 已验证（`&*b` 堆地址取引用不可行） | `Box::leak` 目标签名 `&'static mut T` |

### 3c.1 计划总览（阶段 U–Z）

| 阶段 | 主题 | 子任务（关键交付，按序） | 子任务数 | 依赖 | 状态 |
|------|------|---------|:---:|------|------|
| **U** | 编译器地基（std 完整化前置） | U1 作用域栈重构、U2 trait 关联类型、U3 泛型约束 where、U4 `-> Self` 返回、U5 MIR AddrOf 任意目标、U6 数值转换 Cast IR、**U7 方法级泛型参数**、**U8 泛型结构体构造 + 泛型 trait**（2026-08-25 深化） | 8 | G、T 已知限制 | ✅ 已完成（U1–U6 全部落地 + U7 方法级泛型参数 + U8 泛型结构体构造 `Foo<T>{..}` / 泛型 trait `impl<T> Trait<T> for X`，见 §4 执行记录） |
| **V** | 集合与迭代器完整化 | V1 借用迭代器（`Iter`/`IterMut`/`Iter<'_, K, V>`）、V2 String 码点迭代器（`Chars`/`Lines`）、V3 Iterator 默认方法 + 适配器迁移、V4 `get_mut` 引用语义、V5 新集合（HashSet/BTreeMap/VecDeque） | 5 | U1/U2/U3 | 🔧 进行中（V1 瘦指针迭代器 MVP ✅、V4 `get_mut` 引用语义 ✅ 2026-08-25；V2/V3/V5 规划，依赖 U2/U3 已就绪） |
| **W** | 异步运行时完整化 | W1 Future 泛型化（Output/Pin/Context）、W2 await 状态机扩展（控制流/表达式嵌套/引用跨 await）、W3 事件驱动 executor（epoll/kqueue/io_uring + Future 挂起）、W4 `join_all`/`timeout` Future 版 + `TimeoutError`、W5 `recv_async`/HTTP async 真异步、W6 async 泛型/递归 + 闭包跨线程捕获 | 6 | U、R（Poller）、S1c | ✅ **全部完成（W1–W6，2026-08-26）**（W1 Future 泛型化 `type Output` + `cx: &mut Context`；W2 await 控制流图展开；W4 `join_all`/`timeout` Future 版 + `TimeoutError` + `F::Output` 投影；W3 事件驱动 executor——定时器 `future::sleep` + fd 唤醒 `future::wait_fd` + `block_on` 集成 Poller；W5 `recv_async`/`get_async`/`post_async` 真异步；W6 泛型 async fn + 递归 async fn + 闭包值跨线程捕获。W 收尾验证：修复递归函数多子 future 槽零值缺字段漏洞，全量 130 用例 + W 系列 driver 测试全绿） |
| **X** | 序列化/格式化/时间完整化 | X1 Duration/Instant/SystemTime 完整 API、X2 标准 TOML（空格形式/`[section]`/注释/多行字符串）+ 解析鲁棒性、X3 `Deserialize` trait + Serializer/Deserializer 框架 + `JsonError`/`TomlError`、X4 Formatter 完整化（`Result<(), FmtError>`） | 4 | U3/U4、Q | 🔧 进行中（X1 ✅ 已完成；X2/X3/X4 规划，依赖 U3/U4 已就绪） |
| **Y** | IO/网络/并发/智能指针收尾 | Y1 File `open_with`/`read(&mut [u8])`/`write(&[u8])`/Metadata、Y2 NIO 高性能后端（epoll/kqueue）、Y3 HTTP 连接复用 + sendfile Windows `TransmitFile`、Y4 Mutex/RwLock guard 完整 + Channel 泛型化/bounded、Y5 `Box::leak` 目标签名、Y6 `Error::source` + `Into::into` 自动转换、Y7 UDP（`net/udp.rl`）、Y8 `thread::Builder::stack_size` | 8 | U、O/R/P | 📋 规划 |

> **推荐执行路线**：
> - **快赢线**（独立性强、不依赖 U 全量，可先行交付）：X1（Duration 构造器/读取器补齐，纯 std）→ Y8（stack_size，driver 注入扩展）→ Y1（`File::open_with`，libc extern 扩展）→ Y3（sendfile Windows 平台分支）。
> - **主线（依赖驱动）**：U1（作用域栈，解锁引用语义）→ U2（关联类型）→ U3（泛型约束）→ U4（`Self` 返回）→ V（V1→V4→V3→V2→V5，随 U 逐项解锁）→ X3/X4（依赖 U3/U4）→ W（W1→W2→W4→W3→W5→W6，W3 事件驱动为最大单点）→ Y 收尾。
> - **优先级建议**：U1（消除已知限制、风险最低收益最广）> V4（`get_mut` 引用语义，集合 API 高频）> X1（时间 API 补全，零依赖）> U2（解锁 Iterator/Future 泛型化）> W2（await 控制流块，日常编写收益大）> W3（事件驱动 executor，收益最大但风险最高）> 其余。

### 3c.2 阶段 U — 编译器地基（std 完整化前置）

> 现状：MVP 已验证 5 个能力缺口（见上表）。此阶段为纯编译器工程，无 std 侧产出，但解锁全部后续阶段；每项独立可验收（编译/运行用例），改动面集中在 `rlyeh-typecheck`（U1–U4）与 `rlyeh-mir`/`rlyeh-lir`/`rlyeh-codegen`（U5）。

| 任务 | 内容 | 状态 |
|------|------|------|
| U1 | **作用域栈重构**：typecheck 变量环境 `variables: HashMap<String, Type>` → 按作用域分层（`Vec<HashMap>` 栈，块/函数/method/闭包/for 循环体各自压栈弹出），同名遮蔽按最近作用域解析；`local_inits` 同步分层；消灭 T 阶段已知限制（match 臂绑定与后续 let 同名互不覆盖）。**风险**：变量环境是全 typecheck 的共享状态（check_expr/check_item/check_stmt/closures 均读写），分层后所有读写点需走作用域 API；回归面大，须全量回归 | ✅ 已完成（作用域栈 `Vec<Scope>` + 块级遮蔽 `name$N` 存储槽 mangle + 函数体隔离；消灭 T 阶段同名遮蔽类型覆盖限制；`ref` 模式注册引用类型修复，见 §4 执行记录） |
| U2 | **trait 关联类型**：parser 支持 `trait T { type Item; ... }`（`TypeMember` AST 节点，多条 type 声明允许）；typecheck 绑定 `Type::Assoc(name, trait)` 解析（`Self::Item`/`Self::Output` 在 impl 实例化时替换为具体类型，unify 分支）；`Iterator::Item`/`Future::Output` 泛型化（§2.3/§10 目标 API 的前置）。**风险**：关联类型的解析/替换走通 trait/impl 全链路，涉及 `substitute`/`unify` 递归；MVP 可先支持非泛型 trait（复用 H4 约束） | ✅ 已完成（parser `type Item;` 声明 + `type Item = Concrete;` 定义；typecheck 经 `ctx.assoc_types` 映射在 impl 方法签名收集/实例化检查期替换 `Self::Item`，trait 声明期退化为 `Generic("Self::Item")` 占位；`Iterator::Item` 泛型化留待 V 阶段（std `Iterator` trait 仍为规划 API）；附带修复 codegen `fn_llvm_type` 对 void 返回函数指针的支持，见 §4 执行记录） |
| U3 | **泛型 trait 约束（where / bound）**：parser 支持 `fn f<T: Bound>(...)` / `impl<K, V> HashMap<K, V> where K: Hash + Eq { ... }`；typecheck 记录约束并在调用点/实例化点校验；`T: Serialize`/`F: Future`/`B: FromIterator<T>` 签名落地。**风险**：约束推导（impl 的 where 与调用点约束联动）是核心复杂度，MVP 可先支持「声明 + 单 bound + 调用点宽松校验」（不推导） | ✅ 已完成（AST `AstTypeParam { name, bounds }` 升级 fn/struct/enum/trait/impl 五处泛型字段；parser `<T: B1 + B2>` 内联 bound + impl `where K: B1 + B2` 子句；typecheck `FnTemplate.bounds`/`ImplDef.bounds` 记录 + `check_generic_bounds` 调用点宽松校验——单态化实参确定后查 `impl Trait for Type`（基本类型按 Display 名匹配），新增 `GenericBoundMismatch` 错误；泛型函数体内 `t.area()` 经单态化替换自然解析。`T: Serialize` 等签名落地待 X 阶段 std trait 声明（语言能力已就绪）；impl where 记录不校验、约束推导规划中，见 §4 执行记录） |
| U4 | **`-> Self` 返回**：typecheck 支持 trait 方法签名返回 `Self`（解析 `Self` 为当前 impl 的具体类型，`substitute` 替换）；落地 `Deserialize::from_json(s) -> Self`、`From::from(v) -> Self`、`Into::into() -> Self`。**风险**：`Self` 在参数位置（关联返回）与 dyn 场景（H4 限制）保持禁止；仅实现返回位置 | ✅ 已完成（`TypeContext.self_type: Option<Type>` 上下文字段——`collect_impl` 方法签名解析时设为 impl 目标类型、`instantiate_impl_method` body 检查时设为 `substitute(self_type, subst)`（恢复机制与 type_params/generic_subst/assoc_types 一致）；`resolve_named_type` 对裸 `Self` 优先解析为 self_type、trait 上下文（未设置）退化为占位 `Generic("Self")`（与 `self` 参数占位一致）。覆盖 trait 方法 / inherent 方法 / static 方法（`Point::zero() -> Self`）签名与 body 内 `Self` 类型注解（`let t: Self = ...`），调用点返回具体类型支持链式调用。`Self` 参数位置与 dyn 场景保持既有禁止。`Deserialize::from_json`/`From::from`/`Into::into` 签名落地待 std trait 声明（语言能力已就绪），见 §4 执行记录） |
| U5 | **MIR `AddrOf` 任意目标表达式**：`AddrOf` 从「仅变量」扩展为任意表达式（先求值到临时槽再取址，与 MIR `lower_expr` 既有兜底同构）；落地 `Box::leak` 目标签名 `&'static mut T`（`&*b` 堆地址取引用）。**风险**：取临时地址的活跃性/DCE 正确性（H1 CallIndirect 曾踩坑），须补 MIR 测试 | ✅ 已完成（typecheck `&`/`&mut` 目标放宽为三类：变量 / 解引用 `&*p`（MIR 折叠）/ 不可变表达式（临时取址，读语义）；MIR `Ref{Deref{..}}` 折叠为直接透传内部指针——消除 DerefRead 拷贝临时再取址的语义错误（拷贝地址 ≠ 原地址，`&mut` 写回不生效）；`&obj.field`/`&arr[i]` 字段索引目标与 `&mut` 非左值目标保持禁止（拷贝取址语义错误，待 MIR place 概念）、`&&T` 引用再取引用保持既有禁止；`Box::leak` 维持 FieldGet 读槽 0 裸指针（值语义等价、规避 borrowck 引用逃逸）。补 MIR 测试 ×2 验证折叠指令序列，见 §4 执行记录）。**后 V1（2026-08）字段/索引真实取址**：`&obj.field` / `&arr[i]` 生成真实槽地址（GEP）——MIR `AddrOfField`（标量字段取槽地址；聚合 `FieldScalar::Ptr` 字段直接透传指针值）+ `PtrAdd`（base 指针 + 索引，步长 8，索引可为运行期变量）指令；codegen `FieldAddr`/`PtrAdd` 发射（inttoptr + `%p + %index`）；`&mut arr[i] = v` / `&mut obj.field = v` 经 DerefWrite 写回原字段 / 原元素（替代 U5 拷贝取址语义错误路径）。配套：MIR DCE / 内联 pass 识别新指令（内联缺失曾致迭代器内联副本读未初始化栈值）、borrowck/regionck 新 HIR 节点分支、typecheck `substitute` 补 `RawPtr` 分支 + 字段访问按接收者实例类型参数替换（`StructDef.type_params` 新增）——后者修复用户代码 `Vec<Infer>.data` 字段类型保留 `Generic T` 的错配。验收：`tests/run-pass/addr_of_field_index.rl`（`&arr[i]`/`&mut arr[i]`/`&mut pt.y`/指针算术/索引变量/聚合字段透传），见 §4 执行记录 |
| U6 | **数值转换 Cast IR**：`as` 转换在 typecheck 被**静默擦除**（`check_expr` Cast 分支直接返回 inner hir、类型改标 target，MIR/LIR/codegen 无转换指令——benchmark_report 已列为后续任务）；实现 HIR/MIR/LIR Cast 节点 + codegen 转换指令（`f64→i64` = `fptosi`、`i64→f64` = `sitofp`、整数截断/扩展 = `trunc`/`sext`/`zext`）；解锁 `Duration::from_secs_f64`（X1 注记项）+ mandelbrot 基准。**风险**：涉及 HIR→MIR→LIR→codegen 全链 lower + LLVM 指令映射，回归面大；HIR 仅对「数值→数值」Cast 产出节点（其余保持擦除，如指针/引用转换） | ✅ 已完成（2026-08-25：typecheck Cast 分支仅对「数值→数值」且源/目标不同产出 HIR `Cast { expr, to }` 节点——`to` = 目标类型 Display 名，可转换标量 = ≤64 位整族 + 浮点 + bool + char（i128/u128 存储非 64 位槽、指针/引用/聚合转换保持擦除）；MIR/LIR `MirStmt/LirStmt::Cast { target, value, to }`（源存储类型由 LIR 类型表查询 I64/F64/Bool/Char，目标语义由 `to` 名解析位宽/符号/存储槽）；codegen 按源存储与目标语义发射——浮点↔整数 `fptosi`/`fptoui`/`sitofp`/`uitofp`（bool 目标源规整一律 fptosi 避免负值 UB）、整数截断/扩展 `trunc`/`sext`/`zext`（窄化后 sext/zext 回 64 位槽保持符号扩展不变量）、整↔bool `icmp ne 0`/`zext i1`、整↔char `trunc`/`zext`、同存储恒等 `add/fadd 0` 制造裸寄存器；补 DCE/内联/`touches_any`/lir 类型推断/names 收集/borrowck/regionck/mir_lower_test 全链分支；解锁 `Duration::from_secs_f64`（X1 剔除项恢复，`(secs * 1e6) as i64` fptosi 向零截断）。验收：`tests/run-pass/cast.rl`（14 输出：f64↔i64/整型截断扩展/整↔bool/整↔char/同类型擦除）+ duration_full 增 from_secs_f64 + 全量回归，见 §4 执行记录） |

**验收**：`crates/rlyeh-driver/tests/` 新增 `scoping_test.rs`（U1：同名遮蔽独立类型、嵌套块、match 臂/let 互不覆盖、for 循环体遮蔽）+ `assoc_type_test.rs`（U2：trait `type Item` 声明/解析/实例化替换）+ `bound_test.rs`（U3：泛型约束声明 + 调用点校验）+ `self_ret_test.rs`（U4：`-> Self` 返回的 trait/impl + 调用）+ `addr_of_test.rs`（U5：`&*p`/`&(e.field)` 取址）；每项全量回归 + clippy 0 警告。

### 3c.3 阶段 V — 集合与迭代器完整化

> 现状：T 阶段已实现 MVP 退化版（`get_mut` 值拷贝、`chars` 字节级、`Iterator` 元素固定 i64、适配器内建 desugar）；V 阶段 2026-08 已实现**瘦指针迭代器 MVP**（`iter`/`iter_mut` 返回 `Iter<T>`/`IterMut<T>`：裸指针 + 剩余长度，零拷贝零分配，`next()` 值拷贝读取、`write()` 写回真实原槽；接入 for 与 J3 适配器）与 **`get_mut` 引用语义**（V4 ✅ 2026-08-25：`Vec::get_mut -> Option<&mut T>` / `HashMap::get_mut -> Option<&mut V>`，经 `*` 写回真实槽，编译器确认支持嵌套引用类型返回）——目标 API（§2.3/§3）仍为借用迭代器（引用元素 `Option<&T>`）+ 泛型元素 + trait 默认方法。

| 任务 | 内容 | 状态 |
|------|------|------|
| V1 | **借用迭代器**：`Vec::iter(&self) -> Iter<'_, T>` / `iter_mut(&mut self) -> IterMut<'_, T>`、`HashMap::iter(&self) -> Iter<'_, K, V>`——迭代器结构体（持有容器瘦指针 + 游标槽）+ `next() -> Option<&T>` / `Option<(&K, &V)>`（J2 方法式接入，元素为引用值，U1 解锁返回引用）；`for x in v.iter()` 解引用迭代（自动剥层） | ✅ 部分完成（2026-08 **瘦指针迭代器 MVP**：`Iter<T>`/`IterMut<T>` 已实现并接入 for 与适配器——`data`/`cur` 裸指针字段（V1 真实 GEP 取址 `&self.data[0]` 支持）、`next()` 值拷贝读取、`IterMut::write` 写回真实原槽；编译器特判构造 + `StructDef.type_params` 字段访问替换修复；待办：**引用元素** `Option<&T>`/`Option<&mut T>`（需 U1 引用返回 + 生命周期）、`HashMap::iter` 借用迭代器） |
| V2 | **String 码点迭代器**：`chars(&self) -> Chars`（UTF-8 码点解码——首字节定宽 + 连续字节校验，返回 `Option<char>` 迭代器）替代 MVP 字节级；`lines(&self) -> Lines`（按 `\n`/`\r\n` 分行迭代器，替代 `split("\n")` 的 Vec 拷贝）；`trim(&self) -> &str` 引用视图（目标签名，替代字符串拷贝） | 🔧 进行中（**chars_iter/lines_iter 迭代器 ✅ 2026-08-26**——新增 `Chars`/`Lines` 迭代器结构体（持有 `&String` 引用 + 游标；Rlyeh 裸指针 `*const u8` 按 i64 元素解引用无法逐字节，故用引用字段 `self.s.data[pos]` 按字节索引）；`chars_iter -> Chars` `next() -> Option<i64>`（**返回码点数值而非 char**——Rlyeh `char` 类型 codegen 仅支持 ASCII，非 ASCII `as char` 报错，用 i64 表达全 Unicode 码点；首字节定宽 1-4 + 连续字节校验，实证 `"A中!"` → 65/20013/33）；`lines_iter -> Lines` `next() -> Option<String>`（按 `\n`/`\r\n` 分行并剥 `\r`，尾随换行空行段对齐 split 语义，实证 `"a\nbb\r\nccc"` → 1/2/3）；保留旧 `chars()->Vec<i64>`/`lines()->Vec<String>` 兼容；**`trim -> &str` 规划**——需编译器跨层改造：当前 `&str` 是"指向整个 String 对象的瘦指针"（无偏移/长度），无法表达 data 子区间；borrowck 拒绝返回局部 String 引用；`char` 类型仅 ASCII；需 &str 胖指针（data+len）跨层改造（typecheck/MIR/LIR/codegen 双槽）解锁） |
| V3 | **Iterator 默认方法 + 适配器迁移**：`Iterator` trait 补默认方法 `count`/`sum`/`chain`/`enumerate`/`find`/`any`/`all`（依赖 U2 关联类型 + U3 约束）；内置适配器 `map`/`filter`/`fold`/`collect`/`take`/`skip` 从「typecheck 内建 desugar」迁移为 trait 默认方法（目标签名 `-> Map<Self, B>` 等包装迭代器类型；**风险**：迁移重构面大（J3 全部调用点），可保留内建 + 新增默认方法双轨，逐一迁移后删内建） | 📋 规划 |
| V4 | **`get_mut` 引用语义**：`Vec::get_mut(&mut self, i) -> Option<&mut T>`、`HashMap::get_mut(&mut self, k) -> Option<&mut V>`（U1 解锁返回引用；经 `*` 写回原容器，取代 MVP 值拷贝） | ✅ 已完成（2026-08-25：std `Vec::get_mut -> Option<&mut T>`（越界 None，命中 `Some(&mut self.data[i])` 原槽可变引用）与 `HashMap::get_mut -> Option<&mut V>`（`find` 缺失 None，命中 `Some(&mut self.vals[idx])`）全部落地；调用方 `match { Some(r) => *r = x }` 经解引用**写回真实槽**（非拷贝）；`get_mut_ref.rl` 验收 11 断言（vec 写回 100/连续写回 35→70、越界/负索引 None、HashMap 写回 999 + get 读回 + len 不变 + 他键不受影响、引用剥层读）；实测确认编译器已支持嵌套引用类型返回（`Option<&mut T>` 无新增 IR）） |
| V5 | **新集合**（std-lib.md §1 目标架构目录）：`HashSet<T>`（`collections/hashset.rl`，复用 HashMap 7 槽存储 + djb2/Knuth 散列，`insert`/`contains`/`remove`/`iter`/`union`/`intersection`/`difference`）；`BTreeMap<K, V>`（`btree.rl`，有序键——数组二分插入 + 移动，`first`/`last`/`range` 规划 MVP 限 `i64` 键）；`VecDeque<T>`（`deque.rl`，环形缓冲，`push_front`/`push_back`/`pop_front`/`pop_back`） | 📋 规划 |

**验收**：`tests/run-pass/iter_ref.{rlyeh,out}`（V1：`iter` 求和/`iter_mut` 写回/`for (k, v) in m.iter()`）+ `chars_lines.{rlyeh,out}`（V2：UTF-8 多字节码点、`\r\n` 分行、`trim` 视图）+ `iterator_defaults.{rlyeh,out}`（V3：count/sum/chain/enumerate/find/any/all + 适配器迁移回归）+ `get_mut_ref.{rlyeh,out}`（V4 ✅ 2026-08-25：vec 写回 100 / 连续写回 35→70 / 越界与负索引 None / HashMap 写回 999 + get 读回 + len 不变 + 他键不受影响 / 引用剥层读）+ `hashset_btree_deque.{rlyeh,out}`（V5）+ 全量回归。

### 3c.4 阶段 W — 异步运行时完整化

> 现状：S 阶段已实现线程、`Future`/`Poll`/`block_on`、async fn 状态机（S1c）、线程版 `join_all`、`timeout`（MVP 退化）、`recv_async`/HTTP async（阻塞语义退化）；目标 API（§10）为泛型 Future + 事件驱动 executor。

| 任务 | 内容 | 状态 |
|------|------|------|
| W1 | **Future 泛型化**：`trait Future { type Output; fn poll(&mut self, cx: &mut Context) -> Poll<Self::Output>; }`（依赖 U2 关联类型；`Context`/`Pin<&mut Self>` 为占位类型——`Pin` 语义 MVP 可退化 `&mut Self`）；async fn desugar 输出 `Output` 泛型化（当前固定 i64） | ✅ 已完成（2026-08-25：`future.rl` trait 签名升级 `type Output` + `cx: &mut Context`；新增 `struct Context { _unit: i64 }` 占位；`block_on`/`timeout` 内 `let mut cx = Context { _unit: 0 };` + `f.poll(&mut cx)`；desugar `gen_impl` poll 签名/impl types/`Poll<Self::Output>` + `poll_call` `&mut *cx` 透传；core.rl `import future::Context`；10 处手写 impl（tests+examples）同步新签名；suite_test 全绿） |
| W2 | **await 状态机扩展**：await 位置扩展到控制流块内（if/match/loop 体，状态机段切分按控制流图而非直线）、表达式中间嵌套 await（`a.await + b.await`）；跨 await 变量从 `i64` 扩展到引用类型（依赖 U1）；消除 S1c 显式报错限制 | ✅ 已完成（2026-08-25 控制流图展开 + 2026-08-26 验收修复）：`analyze.rs` 状态机从「直线切段」升级为「控制流图展开」——if/match/loop/for/while 子块按 await 点递归展开为扁平段列表（段带显式后继 `next` 实现跳转/回跳、`CONT_PENDING` 汇合占位回填）；表达式嵌套 await 经 `extract_expr_awaits` 临时变量提取为独立段；跨 await 变量类型扩展到 f64/bool/char/String（需类型注解）。**验收修复（2026-08-26）**：① `pattern_has_bindings` 误判 `_`（通配符）为绑定变量——改为 Wildcard 返回 false，解锁 match 臂 `_` 内 await；② 函数尾表达式「`let r = ..; r`」被拆为跳转段+独立收尾段致局部变量跨段不可见（undefined）——新增 `flush_tail` 把语句+尾表达式合并为收尾段，段内局部尾表达式可见；③ `zero_for_type` 对 String 字段零值改用 `String::from("")`（裸字面量类型 `string` 与字段 `String` 不匹配）。验收 `tests/run-pass/w2_probe.rl`（match 内 await + 跨 await String/bool + let r 尾表达式）+ `async_if_await`/`async_loop_await`/`async_while_min`） |
| W3 | **事件驱动 executor**：`rlyeh-async-runtime`（或语言侧 `async/executor.rl` + `task.rl`）——基于 R1 Poller（poll(2)，后续接 epoll/kqueue，见 Y2）的事件循环 + 任务队列 + Waker/唤醒注册：`block_on` 从忙等轮询改为「poll Pending → 注册 fd/定时器 → 事件就绪唤醒」；为 W5 提供真异步底座。**风险**：单线程事件循环 + Future 状态机 + fd 注册表三者联动是 MVP 后最大子系统，建议分两步——先「定时器唤醒」（sleep/超时事件驱动），再「fd 事件唤醒」 | ✅ 两步完成（2026-08-25 定时器唤醒：`Context` 携带 `deadline` 槽，`block_on`/`timeout` 据此 `thread::sleep` 到唤醒时刻再轮询非忙等；`future::sleep` 定时器 future，await 走带注解变量，实证 sleep(20ms) 墙钟 23ms / CPU 37us；**fd 事件唤醒**：`Context` 携带 `fd`/`interest` 槽，`block_on` `Pending` 且 `fd>0` 时构造 `Poller`（poll(2)）注册并等待就绪，`future::wait_fd(fd, interest)` 落地，实证 TcpListener + 线程延迟 connect 墙钟 35ms / CPU 0.3ms；为 W5 提供真异步底座；验收 `tests/run-pass/async_sleep.{rl,out}` + `crates/rlyeh-driver/tests/executor_test.rs` 3 用例全绿） |
| W4 | **`join_all`/`timeout` Future 版**：`join_all<F: Future>(futures: Vec<F>) -> Vec<F::Output>`（依赖 W1 + 泛型集合；并发轮询，非线程版）；`timeout<F: Future>(d, fut) -> Result<F::Output, TimeoutError>` + `TimeoutError` 类型（§12 错误体系扩展），替代 `Err(-1)` 退化 | ✅ 完成（2026-08-25：`TimeoutError` 类型 + `timeout` 返回 `Result<F::Output, TimeoutError>` + `future::join_all<F: Future>(Vec<F>) -> Vec<F::Output>`（并发轮询直至全部 Ready，结果用 `HashMap<i64, F::Output>` 按序收集支持任意 Output）落地；**`F::Output` 关联类型投影完整实现**——`Type::AssocProjection` + `resolve_ast_type` 识别 `F::Output` + 实例化求值（查该类型 trait impl 的 `assoc_types`）；前置编译器修复 `check_generic_bounds` 泛型 bound 裸名解析（`F: Future` 可解析 std import 提升 trait）；验收 `tests/run-pass/join_all_fut.{rl,out}` + `join_all_any.{rl,out}`（Vec<String>）+ `join_all_fut_test.rs` 4 用例（含 String Output）+ `time_test` 5/5 全绿） |
| W5 | **`recv_async`/HTTP async 真异步**：`Receiver::recv_async` 挂起直到数据/close（事件驱动，替代阻塞等价语义）；`HttpClient::get_async`/`post_async` 经 NIO 非阻塞连接 + 事件驱动读写（依赖 W3 + Y2） | ✅ 完成（2026-08-25：`recv_async` 真异步——`Channel` 携带 socketpair 唤醒 fd + `RecvAsync` future（try_recv + `cx.fd` 挂起）；`get_async`/`post_async` 真异步——`GetAsync` future（connect/写同步 + 非阻塞读响应 wait_fd 挂起，`extract_body` 解析；POST 带 body + Content-Length）`block_on` 驱动返回 `Response`（前置 `block_on` 泛型化返回 `F::Output`）；验收 `net_http_test.rs` `http_get_async`/`http_post_async` 5/5 + `channel.rl`/`recv_async.{rl,out}` 全绿） |
| W6 | **async 泛型/递归 + 闭包跨线程捕获**：async fn 泛型参数（依赖 U3）、递归 async fn（状态机字段自引用，防无限结构体——指针槽）；闭包值跨线程捕获（`Thread::start` 接收有捕获闭包，§10.1 约束消除，依赖 H5 扩展） | ✅ 已完成（泛型 async fn Part 1a；递归 async fn Part 1b——struct 前向声明地基 + desugar 拓扑打破依赖环 + 递归子 future 槽 `Box<__Fut_>`；**闭包值跨线程捕获**——`Thread::start(f, arg)` typecheck 特判生成线程入口 thunk + 线程输入对象，`thread::__start_with_input` 复用 std Result 构造，MVP 限单参数闭包；**W 收尾验证 ✅ 2026-08-26**：修复递归函数含多个子 future 槽（非递归 + 递归槽并存）的零值构造缺字段漏洞——`gen_fields` 分两遍（先非递归槽入 spec，递归槽零值 `Box::new(__Fut_X{<除自身外所有字段零值>})`）；边界回归 `tests/run-pass/w_edge_probe.rl`（递归+嵌套+多函数环+递归跨线程+fn/闭包双入口）、`async_depth_probe.rl`（深度 1000）、`compile-fail/thread-closure-multiparam.rl`（跨线程闭包多参数报错）） |

**验收**：`crates/rlyeh-driver/tests/` 新增 `async_full_test.rs`（W1/W2：控制流内 await、嵌套 await、引用跨 await、泛型 async fn）+ `executor_test.rs`（W3：定时器事件驱动 `sleep` 不忙等、fd 事件驱动）+ `join_all_fut_test.rs`（W4）+ `recv_async_http_test.rs`（W5）+ 现有 `async-fns`/`timeout` 用例保持全绿；**所有运行时测试带超时保护**（§5 约定）。

### 3c.5 阶段 X — 序列化/格式化/时间完整化

> 现状：Q 阶段已实现 json 内建 + Serialize trait（退化）+ TOML 轻量 MVP + `Display`/`Debug`（签名降级）；时间仅 `seconds`/`milliseconds` 构造器 + `secs`/`millis`/`micros`/`nanos` 读方法 + 墙钟 `Instant`。

| 任务 | 内容 | 状态 |
|------|------|------|
| X1 | **时间 API 完整**：`Duration` 补 `microseconds`/`nanoseconds` 构造器、`as_secs`/`as_millis`/`as_nanos`（u128 返回——`as_nanos` 可退化为 i64 注记）、布局对齐目标 `nanos: u64`（内部 micros: i64 迁移）；`Instant::duration_since(&self, earlier)`；新增 `time/system.rl`：`SystemTime`（墙钟纪元，`UNIX_EPOCH`/`now`/`duration_since`，`__rlyeh_clock_realtime` 注入，Windows 差异屏蔽） | ✅ 已完成（2026-08-25：构造器 `microseconds`/`nanoseconds` + 读方法 `as_secs`/`as_millis`/`as_nanos` + `Instant::duration_since` + `time/system.rl` `SystemTime`（`now`/`unix_epoch`/`duration_since`，`__rlyeh_clock_realtime` = CLOCK_REALTIME）全部落地；`from_secs_f64` 经 U6 恢复；`nanos: u64` 布局迁移留注记，见 §4 执行记录） |
| X2 | **标准 TOML + 解析鲁棒性**：`toml::to_string` 输出标准形式——`key = value` 空格、`[section]` 行式子表（替代内联表 `{...}`）、`#` 注释、多行字符串（`"""`）；`toml::from_str` 接受注释/空格/子表；修复 §9.4 已知限制——值含逗号/嵌套容器分段不可靠（替换 `split(",")` 为逐字符/引号感知解析）、`[T; N]` 数组反序列化、f64 支持 | 📋 规划 |
| X3 | **`Deserialize` trait + Serializer/Deserializer 框架**：`trait Deserialize { fn from_json(s: String) -> Self; }`（依赖 U4 `-> Self`，替换「解析统一走内建」退化）；`Serializer`/`Deserializer` 访问器框架（目标签名 `serialize(&self, &mut Serializer) -> Result<(), SerError>`，U3 约束泛型化 `to_string<T: Serialize>` 落地）；`JsonError`/`TomlError` 类型（§12，替代非 Result 包装/死循环语义）；`json::parse` 非法输入改返回 `Err` | 📋 规划 |
| X4 | **Formatter 完整化**：`Formatter` 从占位类型升级为真实格式化器（对齐/宽度/精度/填充状态字段）；`trait Display { fn fmt(&self, f: &mut Formatter) -> Result<(), FmtError>; }`（目标签名，依赖 U3/U4，替换 `-> String` 降级；`Debug::fmt_debug` 改名对齐 `Debug::fmt`——方法同名冲突的 MVP 限制随 impl 查找改进消除）；`FmtError` 类型 | 📋 规划 |

**验收**：`tests/run-pass/duration_full.{rlyeh,out}`（X1：micros/nanos 构造 + as_secs/as_millis 读取 + from_secs_f64 + duration_since + SystemTime 前后关系）+ `toml_std.{rlyeh,out}`（X2：标准空格形式 round-trip + `[section]` + 注释 + 嵌套容器值）+ `deserialize_trait.{rlyeh,out}`（X3：手写 `impl Deserialize` + `from_json` 调用 + `Result` 化错误）+ `fmt_result.{rlyeh,out}`（X4：`fmt -> Result` 对齐/宽度）+ 全量回归。

### 3c.6 阶段 Y — IO/网络/并发/智能指针收尾

> 现状：N/O/P/R 阶段已实现 File/Path/fs、TCP/HTTP、Mutex/RwLock/Condvar/Barrier/Channel、NIO（poll 版）、sendfile（macOS）；T 阶段 `Box::leak` 退化为裸指针。本阶段逐项对齐目标 API + 平台补全。

| 任务 | 内容 | 状态 |
|------|------|------|
| Y1 | **File 目标 API**：`open_with(path, OpenMode)`（`open(path)` 保留兼容壳）；`read(&mut [u8])`/`write(&[u8])` 切片实参（§4.1 目标签名，替代 `read(cap)`/`write(String)` 降级——依赖切片借用成熟 + 数组切片参数化）；`metadata` 返回完整 `Metadata`（size/mtime/is_file/is_dir，替代仅返回大小的 MVP） | ✅ 部分完成（2026-08：**open_with + open 兼容壳 + 完整 Metadata 已落地**——`io/file.rl`：`open(path)` 默认只读兼容壳 ≡ `open_with(path, Read)`，`open_with(path, mode)` 保留原双参逻辑；`struct Metadata{size, mtime, is_file, is_dir}` + `File::metadata() -> Result<Metadata, IoError>`（4 槽非按值 calloc 堆对象）；driver 注入 `__rlyeh_file_size/mtime/mode` 平台内建（POSIX stat(2) 直读 `struct stat` 字段——Linux x86_64 mode@24/size@48/mtime@88、macOS mode@4/size@96/mtime@48（偏移经本机 clang offsetof 实测）、其余平台 -1 stub；extern String 实参经 codegen 自动取 data 指针，语言侧统一 `(path: String) -> i64` 签名；S_IFMT=0xF000 掩码判定 is_file(S_IFREG=0x8000)/is_dir(S_IFDIR=0x4000)）。**`read(&mut [u8])`/`write(&[u8])` 切片实参挂 U1**（动态切片借用 + 数组切片参数化，MVP 保留 `read(cap)`/`write(String)` 降级）；验收：`tests/run-pass/file_open_with.{rlyeh,out}` + `io_file_test.rs` 新增 `file_open_with_modes`/`file_metadata_complete`（11/11 全绿）） |
| Y2 | **NIO 高性能后端**：`io/nio.rl` 的 poll(2) 后端之上增加 `Interest` 平台后端抽象——Linux `epoll`（`epoll_create1`/`epoll_ctl`/`epoll_wait`，O(1) 事件）、macOS `kqueue`（`kqueue`/`kevent`，`EVFILT_READ`/`EVFILT_WRITE`）；`Poller` 内部按 `__rlyeh_target_os()` 分派；为 W3 事件驱动 executor 提供平台后端 | 📋 规划 |
| Y3 | **HTTP 连接复用 + sendfile 平台补全**：`HttpClient` 连接池（keep-alive：`Connection: keep-alive` + 复用空闲连接，替代每请求新建 + close）；sendfile Windows `TransmitFile` 分支（§4.5「非 Unix 返回 Unsupported」消除） | 🔧 部分完成（2026-08：**HTTP 连接复用已落地**——`io/file.rl` 无涉、`net/http.rl` 重构：`HttpClient` 加池字段 `{conn_fd, conn_host, conn_port}`（`_unit` 占位保留），`get/post`/`get_async`/`post_async` 改**实例方法**（`&mut self`），新增 `request_one`/`read_response`/`find_header_end`/`parse_content_length` 内部函数：请求带 `Connection: keep-alive`、body 优先按 Content-Length 精确读（无则回退 EOF）、同 host:port 复用 + 失效自动丢弃重建重试一次；**sendfile Windows `TransmitFile` 分支保留**——本机（macOS）无法编译验证 Windows 代码，避免交付未经验证的分支，`__rlyeh_sendfile` 非 Unix 仍返回 Unsupported（§4.5）；验收：新增 `http_keepalive_test.rs` 5 用例（复用/双实例/失效重试/POST 复用/无 CL 回退，mock 服务器每连接一线程 + 连接计数断言，net_http_test.rs 5 用例同步迁移实例调用，10/10 全绿）） |
| Y4 | **锁 guard 完整 + Channel 泛型化**：`Mutex<T>`/`RwLock<T>` 泛型化（目标签名 `lock(&self) -> MutexGuard<T>`，替代裸 `lock/unlock` + `lock_guard()` 命名特判——方法名对齐目标 API）；`RwLockWriteGuard`/`RwLockReadGuard`（P2b 规划）；`Deref`/`DerefMut` 语义（`*guard` 解引用访问数据，替代 MVP 独立访问器）；`Channel<T>` 泛型化（元素不再限 i64）、`bounded_channel(capacity)` 有界队列（send 满阻塞）、`SendError<T>`/`RecvError`/`TryRecvError` 错误类型（替代 `Option` 退化）、`Arc<LockFreeQueue>`（跨线程 Sender/Receiver，依赖 U3） | 📋 规划 |
| Y5 | **`Box::leak` 目标签名**：`fn leak(self) -> &'static mut T`（依赖 U5 AddrOf 任意目标，替代 `*mut T` 裸指针退化）；`'static` 宽松丢弃（G4 现状） | 📋 规划 |
| Y6 | **错误体系完整化**：`trait Error { fn message(&self) -> String; fn source(&self) -> Option<&dyn Error>; }`（补 `source` 链，M2a 现状仅 message）；`Into::into()` 自动转换 + `?` 运算符的 From 自动转换（目标：`?` 在 `E: Into<F>` 时自动转——依赖 U3/U4，`io::Error` → 自定义错误；MVP 无 where 约束时退化显式 `into()` 调用） | 📋 规划 |
| Y7 | **UDP**：`net/udp.rl`（§1 目标架构目录）——`UdpSocket::bind`/`send_to`/`recv_from`/`local_addr`（libc `socket(AF_INET, SOCK_DGRAM)` + `sendto`/`recvfrom`，平台 sockaddr_in 双布局复用 O1a，WASI 短路） | ✅ 已完成（2026-08：`net/udp.rl` 新建——`UdpPacket{data, from}`/`UdpSocket{fd, addr}`，`bind`（socket SOCK_DGRAM + `bind` + 端口 0 经 getsockname 读实际端口）、`local_addr`、`send_to`/`recv_from`（sendto/recvfrom extern，sockaddr 构造/解析复用 net::byteorder，recv 回填解析源地址 `from`）、`set_nonblocking`/`is_nonblocking`（R2 fcntl 复用）；core.rl 增加 `extern fn sendto/recvfrom` 声明 + `import net::udp::UdpSocket/UdpPacket` 提升（std 预置符号须经 core.rl import 才能裸名访问，TcpListener/SocketAddr 同模式）；WASI 短路；验收：`crates/rlyeh-driver/tests/udp_test.rs` 3 用例（自回环/双 socket 互发源端口/多包按序，bind(0) 免端口冲突）+ run-pass `udp_echo.rl`（输出确定 9/rlyeh-udp/true），suite 全量回归全绿） |
| Y8 | **`thread::Builder::stack_size`**：`Builder::new()/stack_size(bytes)/spawn(f)`——`pthread_attr_setstacksize`（S0e 规划项，替代 attr=NULL 默认栈：Linux 8MB/macOS 512KB 的定制手段） | ✅ 已完成（2026-08：`thread/module.rl` 增加 `Builder`（`new`/`stack_size(&mut self, n)`/`start(&self, f)`）+ driver 注入 `__rlyeh_thread_spawn_stack`（attr 定制，n<=0 → attr=NULL 等价 `Thread::start`）；修复 codegen by_value 逃逸 bug——聚合对象地址经 FieldSet 嵌入堆对象（如 `Result::Ok(Thread{..})`）时栈槽悬垂，预扫描逃逸诊断回退 calloc；验收：`crates/rlyeh-driver/tests/thread_test.rs` 新增 `builder_default_stack_spawn`/`builder_stack_size_deep_recursion`（64MB 栈 100000 层深递归，默认栈 SIGSEGV 反证定制生效）） |

**验收**：`tests/run-pass/file_open_with.{rlyeh,out}`（Y1 ✅：open_with + Metadata；切片 read/write 挂 U1）+ `crates/rlyeh-driver/tests/nio_epoll_test.rs`（Y2，Linux）/`nio_kqueue_test.rs`（Y3 macOS 或 CI 矩阵）+ `http_keepalive_test.rs`（Y3 ✅：连接复用 + 失效重试 + 无 CL 回退；sendfile Windows TransmitFile 分支未验证保留）+ `mutex_generic.{rlyeh,out}`（Y4：泛型 Mutex + Deref guard + bounded Channel + 错误类型）+ `box_leak_ref.{rlyeh,out}`（Y5）+ `error_source.{rlyeh,out}`（Y6）+ `udp_echo.{rlyeh,out}`（Y7 ✅：UDP 自回环）+ `udp_test.rs`（Y7 ✅：自回环/双 socket 互发源端口/多包按序）+ `thread_test.rs::builder_*`（Y8：`builder_default_stack_spawn`/`builder_stack_size_deep_recursion`）+ 全量回归；**运行时测试带超时保护**（§5 约定）。

> **总依赖关系**：U（全部）→ V（V1/V3/V4 依赖 U1–U3；V2/V5 依赖 V1）+ X3/X4（依赖 U3/U4）→ W（W1 依赖 U2；W2 依赖 U1；W3 依赖 R + Y2 可选；W4/W6 依赖 U3）→ Y（Y1 依赖 U1 切片成熟；Y4/Y6 依赖 U3/U4；Y2 独立；Y3/Y7/Y8 独立）。
> **跟踪与验收约定**：沿用 §5——每阶段 `cargo test --workspace` 全绿 + clippy 0 警告；运行时/并发类测试带超时保护；完成后同步更新 std-lib.md 状态总览（勾销对应规划章节）+ 本文档状态标识 + CODEBUDDY.md §5.5。

---

## 4. 执行记录

- [x] **U5 MIR `AddrOf` 任意目标表达式**（2026-08-25）：`&*p` 引用折叠 + `&`/`&mut` 目标放宽。**实现**：① typecheck（check_expr.rs UnaryOp::AddrOf 分支）——目标放宽为三类：变量（既有）、解引用 `&*p`/`&mut *p`（`ExprKind::Unary{Deref}`）、不可变 `&expr`（任意表达式求值到临时槽取址，读语义正确）；字段/索引目标（`&obj.field`/`&arr[i]`）与 `&mut` 非左值目标继续报 Unsupported（FieldGet/IndexGet 经拷贝临时再取址语义错误——写回不生效，待 MIR place 概念扩展），`&&T` 引用再取引用保持既有禁止；② MIR（lower.rs `HirExpr::Ref` 分支）——`Ref{Deref{inner}}` 折叠为 `lower_expr(inner).map(MirValue::Place)`，直接透传内部指针，消除「DerefRead 拷贝临时 → AddrOf 取临时地址」的语义错误（拷贝地址 ≠ 原地址，`&mut *b` 写回不生效）；③ `Box::leak`（T3a）维持 FieldGet 读槽 0 裸指针实现（`&*b` 折叠值等价、规避 borrowck 引用逃逸检查），更新注释说明。**验收**：`tests/run-pass/addr_of_fold.rl`（`&*b` 读 / `&mut *b` 写回 Box 堆 42→99、`&*r` 同一地址语义（`*r=8` 后 `*s` 可见 8）、`&(3*4)` 表达式取址 12、`Box::leak` 99→100）+ `crates/rlyeh-mir/tests/mir_lower_test.rs` 新增 `test_lower_addr_of_deref_fold`/`test_lower_addr_of_deref_mut_fold`（断言无 AddrOf 指令、DerefRead/DerefWrite 直写 base=p）+ 全量 suite_test / workspace 全绿。文档：mvp-gaps-plan.md U5 状态 + CODEBUDDY.md §5.5。
- [x] **U5 后置 V1：`&obj.field` / `&arr[i]` 真实取址（GEP）**（2026-08-25）：字段/索引目标生成真实槽地址，写回原字段/原元素。**实现**：① typecheck（check_expr.rs UnaryOp::AddrOf 分支）——V1 解锁 `FieldAccess`/`Index` 目标；`&T` ↔ `*const T` 互视已支持，指针算术 `*const T + n` → `HirExpr::PtrAdd`（元素步长）；② MIR（lower.rs `addr_of`）——`FieldGet` 目标：标量字段发射 `MirStmt::AddrOfField`（GEP 槽地址），聚合字段（`FieldScalar::Ptr`，如 `Vec::data: [T; 0]`）直接透传字段值（地址即指针值）；`Index` 目标：`AddrOf(base)` + `lower_expr(index)` + 发射 `MirStmt::PtrAdd { elem, is_str }`；③ LIR/codegen——`PtrAdd` 直通（lower.rs）+ `FieldAddr`/`PtrAdd` 发射（llvm.rs：inttoptr + `%p + %index` 计算地址，DerefWrite 经 store 写回）；④ 配套修复四连：**MIR DCE**（passes/dce.rs）`collect_used` 识别 `AddrOfField`/`PtrAdd`（否则常量 offset `Assign(_t5, Int(1))` 被误删，`%_t5.addr` 槽未赋值）；**MIR 内联**（passes/inline.rs）`inline_stmt` 补两指令分支（此前 `_ => None` 丢弃——`Vec::iter()` 内联后 `&self.data[0]` 读到未初始化栈值，野指针崩溃）；**typecheck `substitute`** 补 `Type::RawPtr` 递归分支（泛型方法实例化 `*mut T → *mut i64`，此前原样保留导致 `*self.cur = x` 报 `expected T, found i64`）；**字段访问替换**——`StructDef` 新增 `type_params`（types.rs），`check_field_access` 按接收者实例类型参数替换字段类型（用户代码 `Vec<Infer>.data` → `[Infer; 0]`，此前保留 `[Generic T; 0]` 致 DerefSet 严格兼容检查错配）；borrowck/regionck 补 `HirExpr::PtrAdd` 递归检查分支（non-exhaustive）。**验收**：`tests/run-pass/addr_of_field_index.rl`（`&arr[1]`→20 / `&mut arr2[2]=99` / `&mut pt.y=7` / 指针算术 `base+1` / 索引变量 `&arr4[i]` / 聚合字段透传 `&v.data[0]`→11 / `&mut v2.data[1]=55`）+ suite_test 全绿。文档：guide.md §8.1 + mvp-gaps-plan.md U5 状态 + CODEBUDDY.md §3.2/§3.9。
- [x] **V1 瘦指针迭代器 MVP（Iter/IterMut）**（2026-08-25）：`Vec::iter()` 从「元素值拷贝缓冲」（T1a 退化）升级为零分配零拷贝视图。**实现**：① std（core.rl）——`struct Iter<T> { data: *const T, len }` / `struct IterMut<T> { data: *mut T, cur, len }`；`iter(&self)` 经 `let p: *const T = &self.data[0]`（V1 聚合字段透传 + PtrAdd）取首元素地址，`iter_mut(&mut self)` 经 `&mut self.data[0]`；`Iter::next(&mut self) -> Option<T>`（`*self.data` 值拷贝读 + `self.data + 1` 指针推进）、`len`/`is_empty`；`IterMut::next`（读并推进，记录 `cur` 写回目标）+ `write(x)`（`*self.cur = x` 经 DerefSet 写回最近 next 读取的元素）；② typecheck 特判构造——`Iter::new(data, len)` / `IterMut::new(data, cur, len)`（check_iter_construct，与 Vec/HashMap 同模式：Alloc + FieldSet，Iter 2 槽 by_value、IterMut 3 槽堆分配，元素类型从 data 裸指针内项反推），`check_method_call` 触发；③ 接入 for（next() 检测）与 J3 适配器（map/filter/fold/collect 等检测 next() + Option 内项）；`CORE_TYPES`（driver std_test.rs）不含 Vec 无需同步。**验收**：`tests/run-pass/vec_api.rl` 重写（iter for 求和 / 手动 next+len / iter_mut write 写回 100、7 / 适配器链 `v.iter().map(|x| x*10).collect()`）+ examples 副本同步 + suite_test 全绿。文档：guide.md §10.3 + std-lib.md §3.1 + mvp-gaps-plan.md T1a/V1 状态。
- [x] **U4 `-> Self` 返回**（2026-08-25）：`Self` 类型名解析为当前 impl 目标类型。**实现**：① `TypeContext` 新增 `self_type: Option<Type>` 上下文字段（`Default` 默认 None，无需改初始化）；② `resolve_named_type` 对裸 `Self` 优先返回 `self_type`，未设置（trait 收集上下文）退化为占位 `Generic("Self")`（与既有 `self` 参数占位、`Self::Item` 关联类型占位语义一致）；③ `collect_impl` 方法签名解析前设 `ctx.self_type = Some(self_type)`、解析后恢复；④ `instantiate_impl_method` body 检查前设 `ctx.self_type = Some(substitute(&impl_def.self_type, subst))`（泛型 impl 单态化后 Self → 具体类型）、检查后恢复（与 type_params/generic_subst/assoc_types 保存恢复并列）。**覆盖**：trait 方法（`fn clone(&self) -> Self`）、inherent 方法（`fn doubled(&self) -> Self`）、static 方法（`fn zero() -> Self` / `Point::moved(dx, dy) -> Self`）签名返回位置 + body 内 `Self` 类型注解（`let t: Self = ...`）；调用点返回具体类型，支持链式调用（`p.doubled().sum()`）。**MVP 边界**：`Self` 参数位置（关联返回）保持禁止；dyn 场景含 `Self` 签名方法不可经 vtable 调用（H4 既有限制）；`Deserialize::from_json(s) -> Self` / `From::from(v) -> Self` / `Into::into() -> Self` 落地待 X/W/V 阶段 std trait 声明（语言能力已就绪）。**验收**：`tests/run-pass/self_return.rl`（trait / inherent / static 方法 + body 内 Self 注解 + 链式调用，输出 7/14/0/200/11/22）+ 全量 suite_test 全绿。文档：mvp-gaps-plan.md U4 状态 + CODEBUDDY.md §5.5。
- [x] **U3 泛型 trait 约束（bound / where）**（2026-08-25）：`fn f<T: Bound1 + Bound2>(...)` 内联 bound + `impl<K, V> Trait for Type where K: B1 + B2 { }` where 子句；调用点宽松校验（不推导）。**实现**：① AST——新增 `AstTypeParam { name, bounds }`，`AstFnDecl`/`AstStructDecl`/`AstEnumDecl`/`AstTraitDecl`/`AstImplBlock` 五处 `generics: Vec<String>` 升级为 `Vec<AstTypeParam>`；② parser——`parse_generics` 解析 `T: B1 + B2`（ident + `+` 连接；生命周期参数保持丢弃），新增 `parse_where_clause` 在 `parse_impl` 中被实现类型后消费 `where K: B1 + B2` 并合并进泛型参数 bounds；③ typecheck——`FnTemplate.bounds`（函数泛型）与 `ImplDef.bounds`（impl 泛型，`#[allow(dead_code)]` 记录不校验）保存约束；`check_generic_bounds` 在 `check_generic_call` 实参推断 subst 完成后校验——对已确定的类型参数逐 bound 检查 `trait_defs` 已声明 + 存在 `impl Trait for Concrete`（`type_implements_trait`：impl `trait_name` 匹配 + 目标类型匹配，基本类型按 Display 名与 impl `Named` 目标比较，如 `impl PartialEq for i64`；未确定参数跳过不推导）；新增 `TypeError::GenericBoundMismatch`（`type `X` does not implement trait `Y` (bound on generic parameter `T`)`）；④ 泛型函数体内调用 bound 方法（`t.area()`）经单态化实例化（generic_subst 替换）自然解析，无需特判。**同步适配**：rlyeh-fmt / rlyeh-doc 各 7 处泛型打印改 `fmt_generics`（`T: B1 + B2` 格式）。**MVP 边界**：impl where 仅记录不校验（impl 泛型实例化方法调用校验规划中）；约束推导（不变量关系）不支持；`T: Serialize`/`F: Future`/`B: FromIterator<T>` 签名落地待 X/W/V 阶段 std trait 声明（语言能力已就绪）；泛型 struct 字面量推断仍受限（U2 已注明）。**验收**：`tests/run-pass/bound_test.rl`（单 bound / 多 bound `Named + HasArea` / 混合 `T: HasArea, U` / 多次单态化实例化，输出 0.0/25.12/Point/Circle/0.0）+ `tests/compile-fail/bound-mismatch.rl`（i64 不满足 HasArea 报错）+ where 子句声明编译验证 + 全量 suite_test 全绿。文档：mvp-gaps-plan.md U3 状态 + CODEBUDDY.md §5.5。
- [x] **U2 trait 关联类型（assoc type）**（2026-08-25）：`trait T { type Item; ... }` 声明 + `impl T for X { type Item = Concrete; }` 定义，impl 方法签名中 `Self::Item` 收集期替换为具体类型。**实现**：① AST——`AstTraitDecl.types: Vec<String>`（声明名）、`AstImplBlock.types: Vec<(String, AstType)>`（名 → 类型 AST）；② parser——trait/impl body 支持 `type` 引导的成员（`Token::Type`，trait 侧 `;` 结尾、impl 侧 `= Type;`），`parse_type` 的 `Self` 分支支持 `Self::Item` 路径；③ typecheck——`TraitDef.assoc_types`/`ImplDef.assoc_types` 记录，`TypeContext.assoc_types: HashMap<String, Type>` 保存/恢复式填充：collect_impl 先解析关联类型定义再填映射，方法签名中的 `Self::Item` 经 `resolve_ast_type` 查表替换；trait 声明收集期（无 impl 上下文）退化为占位 `Type::Generic("Self::Item")`，`type_mentions_self` 扩展识别 `Self::` 前缀（dyn vtable 路径正确拦截关联类型方法）；`instantiate_impl_method` 方法体惰性检查时按 `impl_def.assoc_types`（经 `substitute`）填充。**附带修复 codegen bug**：`fn_llvm_type` 对 `Unit` 返回未处理（`llvm_type(Unit)` 报「单元类型不能作为存储」）——返回 `()` 的 trait 方法（如 `fn set(&mut self, v: Self::Item)`）在 dyn vtable 构造（`FnPtr(Box::set)` → `LirStmt::Move` 全局函数地址）或 `CallIndirect` 时触发；修复为 `void` 返回。**验收**：`tests/run-pass/assoc_type.rl`（`i64`/`String` 两种关联类型具体化、返回/参数/多关联类型、H4 dyn 去虚拟化关联类型方法）+ 全量 suite_test 全绿。文档：mvp-gaps-plan.md U2 状态 + CODEBUDDY.md §5.5。
- [x] **U1 作用域栈重构 + 块级变量遮蔽**（2026-08-25）：typecheck 变量环境从单层 `HashMap<String, Type>` 重构为作用域栈 `Vec<Scope>`（`Scope { vars, inits, dyn_concrete, is_fn }`），块级遮蔽 `let x` 语义落地。**核心设计**：① `push_scope(is_fn)`——`true` = 函数/闭包边界（变量查找不穿透，函数体隔离）、`false` = 块/循环/match 臂（查找穿透，块内可见外层）；② 存储槽 mangle——块内同名绑定 `insert_variable` 返回 `name$N`（`shadow_seq` 递增），下游 HIR 按槽名区分（块内引用指新绑定、块后原变量恢复）；③ `insert_variable`/`insert_local_init` 返回存储槽名，Let/引用/for desugar/match 臂绑定全链路改用槽名；④ `new()` 自动推入根作用域。**三处关键 bug 修复**：① **fn 边界隔离顺序错误**——`stored_name`/`resolve_variable`/`lookup_local_init`/`get_dyn_concrete`/`remove_dyn_concrete` 遍历先查 `vars` 后查 `is_fn`，嵌套编译（impl 方法体实例化）时外层 fn 层同名参数（如 `self`）触发误 mangle，参数槽 `self$0` 与 codegen 入口槽 `self` 错位（IR 读未初始化槽 → 运行期 SIGTRAP/垃圾输出，15 个 run-pass 崩溃全因于此）；改为携带 `crossed_fn` 标志——起始层为 fn 边界查完即止、块层穿透但越过外层 fn 边界后停止；② **match 臂 `ref` 绑定类型注册错误**——`check_pattern` Ident 分支内部 `insert_variable` 先以值类型（`i64`）注册，`AstPattern::Ref` 分支的引用化类型只进 `bound_tys`（调用方丢弃），臂内 `*r` 解引用报「发现 i64」；改为内层为简单标识符时直接以 `&T` 注册（`Type::Ref(pat_ty)`）；③ 函数体从 `check_block` 改 `check_block_inner`（不额外开块层，尾表达式延迟闭包固化仍可访问局部变量）。**验收**：`tests/run-pass/shadowing.rl`（7 组输出：基础块遮蔽/多层嵌套/遮蔽初始化引用外层/match 臂遮蔽/while 体遮蔽/for 变量遮蔽/闭包捕获遮蔽变量）+ 全量 suite_test + 全工作区 cargo test 全绿 + clippy 0 警告。文档：mvp-gaps-plan.md U1 状态 + CODEBUDDY.md（§3.9）+ guide.md §13。
- [x] **L4 平台加固：WASI net 禁用文档化 + Actor 交叉编译 / WASM 支持**（2026-08）：消除 §13 约束 2/3。**L4a net**：`net.rl` 网络函数（`socketpair_stream`/`send_all`/`recv_some`/`tcp_connect`/`hostname`）统一加 `if __rlyeh_target_os() == 5 { return …; }` WASI 短路（`__rlyeh_target_os` 码 5 = WASI，driver `target_os_code` 新增 wasi 分支——wasm 目标由 0 改为 5，`platform_builtin_test.rs` 断言同步更新）。**L4b Actor WASM 支持**（wasm32-wasip1，Rust 旧目标名 wasm32-wasi 已废弃）：① **符号解析替代**——dlsym 在 WASI 不可用，driver 从 HIR `fn_signatures` 收集 actor 符号（`escape_llvm_c_string`/`llvm_quoted_name`/`actor_symbols_from_ir`）注入静态表 `actor_resolve_ir`：`rlyeh_actor_resolve(name, nlen)` 逐条 `strcmp` 命中 → `ptrtoint` 取函数地址；**外部链接**（internal 链接对静态库不可见，wasm-ld 报 undefined symbol）；② **WASI 单线程同步运行时**（`rlyeh-actor-runtime/src/sync.rs`，`#[cfg(target_os = "wasi")]`）：`Entry { handler, state: u64, factory: Option<RlyehFactory>, running }` + `TABLE: Mutex<Vec<Option<Entry>>>` + `NEXT: AtomicU64`（句柄 = 索引 + 1）；`spawn`/`spawn_supervised`/`dispatch`（回调前取 entry 快照防 Mutex 重入；handle 返回 `u64::MAX` 视为崩溃 → `factory` 重建初始状态并重启）/`stop`/`status`/`shutdown`；③ **ABI 修正**——`CallbackActor.state: u64`（原 `*mut c_void`）、`rlyeh_actor_spawn(handler: *const c_char, state: u64)`、`RlyehFactory = unsafe extern "C" fn() -> *mut c_void`（匹配 Rlyeh 侧 `__state_new` 的 `i8*` 返回）；④ **driver 链接**——`wasm_actor_runtime_lib_path` + `assemble_wasm` 探测并链接 wasm actor 运行时（缺失且源码用了 actor 则显式报错）；`ffi.rs` 的 `resolve_symbol` cfg 分支（wasi 查注入表）。**关键教训**：① clang 拒绝 `declare void @strdup(i8*)`（wasi-libc 无 strdup）——`resolve_symbol` 改 cfg dlsym / 静态表双分支；② ABI 不匹配触发 wasm `signature_mismatch` trap——Rlyeh 侧 `declare i64 @rlyeh_actor_spawn(i8*, i64)` 的 `i64` 与 32 位指针同宽但 wasm-ld 按真实类型区分，state/factory 统一为 `u64`/`*mut c_void`；③ `Vec::resize` E0599（Entry 非 Clone）→ `#[derive(Clone, Copy)]`。产出 `wasm_target_test.rs` 新增 `wasm_actor_runtime_ready`/`wasm_actor_runs`（actor-ping-pong ask/send，输出 `11\n12\n2\n4\n`）/`wasm_actor_supervised_runs`（受监督崩溃重启，输出 `50\n0\n10\n`）+ wasm 码断言 0→5 + 全量 cargo test 全绿。文档：mvp-gaps-plan.md 状态标识 + CODEBUDDY.md（§3.9 并发行）+ guide.md §11.3/§13 + grammar.md 顶部。
- [x] **L3 region 选项接线**（2026-08）：语言级 `region` 选项完整语法 + region 指令从"LLVM 后端忽略"转变为调用 `rlyeh-region-alloc` C ABI 运行时。**语法**：`strategy (bump)` 选项（AST `RegionStrategy::Bump` + `RegionOptions.strategy`，parser `region.rs` 解析）；`region 'r adaptive { .. }`（`adaptive`）与 `region 'r with_size (N) { .. }`（`with_size`）原有语义保留。**IR 传递链**：`HirRegionOptions`（strategy）/`MirRegionOptions`/`LirRegionOptions` 沿 HIR→MIR→LIR 传递；`InRegion.size`（typecheck 期 = `type_slot_count × 8`，聚合对象 bump 分配 + memcpy 值镜像，MVP 浅拷贝）；`AllocInRegion{size}`/`RegionExit{name}` 节点（MIR/LIR）；匿名区域赋唯一名 `__anon_region_N`。**运行时接线**（`rlyeh-region-alloc` 新增 `crate-type = ["rlib", "staticlib"]` + `cabi.rs` C ABI 层）：`rlyeh_region_enter(name, nlen, initial, allow_growth, factor, exact, adaptive, strategy)` 按 options 创建 `Region`（Exact / Adaptive / Multiply）→ `rlyeh_region_alloc(handle, size, align)`（`allocate_bytes` 裸 bump 分配，不注册析构）→ `rlyeh_region_transfer(handle, ptr)`（`mark_transferred`）→ `rlyeh_region_exit(handle)`（销毁批量释放）。**codegen**：4 个 `declare` + 入口块按区域名预分配句柄槽 `%{key}.rh` + RegionEnter/AllocInRegion/RegionExit/Transfer 四条指令接线（AllocInRegion 仅 Ptr 槽且 size > 0 时接线）。**PGO 回灌**：`--profile` 读取 `.rl_profile` → `PgoAdvisor::recommend_size` p95×1.1 建议 → typecheck `region_hints` 注入 `region 'r adaptive` 初始容量。**driver**：`region_hints_from_profile` + `full_pipeline_with_hints` + 链接列表追加 `region_runtime_lib_path()`。**关键教训**：① `cargo build` 增量缓存命中会复用旧 LLVM IR（源码哈希未变），改 codegen 后须 `--force` 或 touch 源文件；② **Apple clang 21 不接受 `%r = getelementptr inbounds ([N x i8], [N x i8]* @.str.N, i64 0, i64 0)` 独立指令形式**（报 `expected type`，但同一 GEP 内联在 `store` 右值中合法）——需独立取地址值的场景改用 `emit_string_global_ptr`（bitcast 指令 `%r = bitcast [N x i8]* @.str.N to i8*`）。产出 `tests/run-pass/region_alloc.{rlyeh,out}`（10 输出：标量/聚合 `in 'r` 分配、`with_size`/`adaptive`/`strategy (bump)` 区域、transfer 移出、跨区域引用）+ 全量用例全绿 + cargo test 全绿。文档：mvp-gaps-plan.md 状态标识 + CODEBUDDY.md（§3.4/§3.9）+ guide.md §8.2/§13 + memory-model.md §3 实现状态。
- [x] **H4 `dyn Trait` trait 对象（MVP）**（2026-08）：`dyn Trait` 类型 + `&T` 强制转换 + 方法调用 vtable 间接分派全链路（parser → typecheck → HIR → MIR/LIR → LLVM）。**类型与布局**：`AstType::Dyn(Box<AstType>)`（parser `ty.rs` `dyn` 关键字分支）+ typecheck `Type::Dyn(String)`（完整 trait 名；use 别名解析）；布局 = **2 槽胖指针**（槽 0 = 数据指针、槽 1 = vtable 指针，`field_scalar_of` Ptr）。**转换 `coerce_to_dyn`**：`let d: dyn Shape = &c;` / 函数实参 `&T` → `dyn Trait`（`compatible_with` 允许）；desugar 为 HIR 块——`Alloc(3+N)` vtable（槽 0–2 = drop/size/align，MVP 置 0）+ N 方法槽（`FnPtr("Trait::method")` 按 trait 声明序）+ `Alloc(2)` 胖指针（槽 0 = 数据指针、槽 1 = vtable 指针）。**调用 `check_method_call` Dyn 分支**：`let __data = FieldGet(obj,0); let __vtp = FieldGet(obj,1); let __m = Index(__vtp, 3+idx); CallIndirect(__m, [__data, ...实参])`——同一签名分派到不同 impl（多态）。MVP 限制：trait/impl 均须非泛型（泛型实例化报 Unsupported）；trait 方法签名含 `Self`（关联返回类型/参数）不支持经 dyn 调用；签名参数与返回类型须为具体类型。产出 `tests/run-pass/dyn_trait.{rlyeh,out}`（6 输出：转换/多态分派/胖指针拷贝/带参方法）+ 全量 40 用例全绿 + cargo test 全绿。
- [x] **G3 裸指针 / G4 生命周期标注**（2026-08）：
  - **G3 裸指针**：`*const T`/`*mut T` 类型注解（parser `ty.rs` 新增 `Token::Star` 分支；AST `AstType::RawPtr(Box<AstType>, bool)`；typecheck `Type::RawPtr(Box<Type>, bool)`；`resolve_ast_type` 分支）+ `*p` 读写（Deref 分支扩展 `RawPtr`，标量 load/store 与 `&T` 同构，codegen 同为 `i8*` 槽零改动）+ 引用 ↔ 裸指针**互视宽松规则**（`compatible_with` 双向：`(RawPtr, Ref)` 与 `(Ref, RawPtr)` 内层兼容即可——FFI 传参 `fn f(p: *const T)` 传 `&x`、`let p: *const T = &x;`，codegen 布局一致零成本）+ `*mut T` 降级 `*const T`（反向禁止）。接线：`field_scalar_of` → Ptr、`type_mono_key` `rawptr{const,mut}_`、`Display`、rlyeh-fmt/rlyeh-doc `fmt_type` 补分支。**关键教训**：参数检查 `ty.compatible_with(pty)` 是**实参×形参**方向，与 let 声明 `at.compatible_with(&ty)` 相反——单方向规则（RawPtr 接受 Ref）会在函数调用处失效，MVP 采宽松互视（双向）解决，借用安全性留给 borrowck。聚合裸指针访问字段需显式 `(*p).field`（不自动剥层）。产出 `tests/run-pass/raw_ptr.{rlyeh,out}`（7 输出：读取/写入/降级/传参/聚合显式解引用/函数返回值）。
  - **G4 生命周期标注（MVP 语法接受）**：parser 两处——`parse_generics` 遇 `Token::Lifetime` 跳过（`'a` 及可选 `: 'b` bound），`parse_type` 的 `&` 分支跳过 `&'a T` 生命周期（其后可选 mut）；typecheck 无改动（生命周期信息解析后丢弃，宽松检查）。grammar.md EBNF 早已定义（`Lifetime ::= ''' Ident`）；lexer 早有 `Token::Lifetime`。borrowck 生命周期检查规划中。产出 `tests/run-pass/lifetime.{rlyeh,out}`（4 输出：`fn longest<'a>(&'a i64, &'a i64) -> &'a i64`、`struct Wrapper<'a>`、`&'a mut T` 写入、与裸指针组合）。
- [x] **N 阶段：文件系统与 IO 对象化（N1–N3）+ `eprintln!`/`eprint!` 宏（N4）**（2026-08）：`File`/`OpenMode`（N1a/N1b/N1c：`File::open`/`create` + `read_to_string`/`read`/`write`/`write_all`/`flush`/`metadata`/`size`，io.rl 直接 libc extern + `open_mode_str` 模式映射）、stdin/stdout/stderr 模块（N2a/N2b：`stderr()`/`stdout()` `write`/`writeln`/`flush` + stdin `read_to_string`/`lines`）、`Path`/`fs`（N3a/N3b/N3c：`Path::new`/`join`/`parent`/`file_name`/`extension`/`exists`/`is_file`/`is_dir` + `fs::read_to_string`/`write`/`copy`/`remove_file`/`remove_dir_all`/`rename`/`create_dir`/`create_dir_all`/`read_dir`，见 `crates/rlyeh-std/rlyeh/fs.rl`）。**N3c 关键修复（`r#rename` 遮蔽）**：`fs.rl` 内裸名 `rename(...)` 被 `resolve_callable` 模块内优先规则绑定到 `fs::rename`（返回 `Result<i64, IoError>`），`if r != 0` 中 Result 与整数比较触发 ChainTypeMismatch——为 `r#` 前缀承载「根命名空间显式引用」语义：lexer `read_raw_ident` 保留前缀产出 `Ident("r#rename")`；parser `parse_fn` 声明名归一化（`fn r#rename` → 注册 "rename"）；typecheck `resolve_callable` 对 `r#xxx` 去前缀并**跳过模块内优先**直接绑定根命名空间；`register_use` 归一化 use 路径段；fs.rl 移除 use 别名 workaround 直接调用 `r#rename`。**N4 `eprintln!`/`eprint!`**：内置格式化宏扩展 stderr 输出，与 `println!` 同构——parser `is_builtin_macro` 加 `eprintln`/`eprint`；typecheck `builtin_signature` 注册 `eprint`/`eprintln`（0–1 参数）+ `check_call` String 分支映射 `eprint_string`/`eprintln_string` + `check_format_macro` 分发（`trim_end_matches('!')` 通用）；LIR `BUILTIN_FUNCTIONS` + codegen `BUILTIN_FUNCTIONS` 注册 4 内建；codegen 打印分支统一 `emit_out` 辅助闭包——stderr 目标用 **POSIX `dprintf(2, fmt, ...)`**（不引用 `stderr` 符号：macOS libc 为 `__stderrp` 不可移植，WASI 亦提供 dprintf），非 String 参数复用 `builtin_fmt`（`eprintln(7)`/空参换行/Char/Bool 全支持）。产出 `tests/run-pass/{file_io,open_mode,stdout_stderr,stdin_enhance,path_ops,fs_ops,fs_dir,eprintln}.{rlyeh,out}` + `tests/compile-fail/io-result-misuse.rl` + 全量 78 用例全绿 + cargo test 全绿 + clippy 0 警告。文档：CODEBUDDY.md（§3.9/§5.5）+ guide.md §13 + std-lib.md §4.2/§8。
- [x] **H3 捕获闭包（IIFE MVP）**（2026-08）：`(|x, y| body)(args)` 立即调用——闭包体引用的外层变量**按值捕获**，desugar 为匿名函数 `__closure_N(cap1, cap2, x, y) -> ret { body }`（捕获变量作前置参数、参数名保留外层原名；闭包参数无类型注解，类型从实参推断）+ 调用点展开 `__closure_N(cap1, cap2, arg1, arg2)`，零新增 IR 节点。实现：`check_call` 的 `_ =>` 分支拦截 `ExprKind::Closure` callee → `check_capture_closure_iife`；**捕获收集采用迭代重查法**（环境 = 已发现捕获变量 + 闭包参数，逐轮把 `UndefinedVariable` 中存在于外层变量环境的名称收为捕获并重查，循环收敛）；字符串字面量实参经 `check_string_from` 升级为 String 语义（与 `let s = "..."` 绑定一致，槽数匹配——否则闭包体内拼接按 `Str` 报 ExpectedNumeric，而字面量绑定变量本就为 `Str` 类型，属语言现状）。MVP 约束：仅 IIFE（`let f = |x| ..; f(..)` 闭包值对象 = 匿名结构体 + `call` 方法，规划中）；`move` 关键字 MVP 忽略（所有权宽松）；嵌套捕获闭包不支持；参数遮蔽捕获（Rust 语义）。H2 无捕获闭包不受影响（IIFE 无捕获时走同路径，结果一致）。产出 `tests/run-pass/capture_closure.{rlyeh,out}`（8 输出：单/多捕获、String 拼接、H2 回归、参数遮蔽、move、数组变量索引、for 循环内 IIFE）+ 全量 39 用例全绿 + cargo test 全绿。
- [x] **H5 闭包值对象（MVP）**（2026-08）：`let f = |x: i64| body; f(..)` 绑定后反复调用——闭包参数支持**类型注解**（parser `parse_closure` 参数循环：`parse_pattern` 后 `eat(Colon)` → `parse_type`；AST `ExprKind::Closure` 新增 `param_types: Vec<Option<AstType>>` 平行字段），全注解闭包在无 fn 上下文时可创建闭包值。**desugar（H3 规划的"匿名结构体 + call 方法"的 MVP 形态，零新增 IR 节点）**：① 闭包匿名函数 `__closure_N(cap1, cap2, x)`（与 IIFE 相同的捕获前置参数形态，`emit_closure_fn` 公共函数）；② 闭包值 = 捕获聚合对象（每捕获一槽）——绑定展开为 `HirExpr::Block`（`Let __cv_N = Alloc{slots}` + 逐槽 `FieldSet`（按值拷贝捕获变量，`field_scalar_of`）+ 尾 `Variable(__cv_N)`），`f` 绑定为对象指针，类型 `Type::Closure { captures, params, ret, fn_name }`；③ 调用点 `f(args)` desugar 为 `__closure_N(FieldGet(f, 0, cap0)..., 实参...)`（`check_closure_value_call`，check_call 函数签名兜底分支前查变量环境 Type::Closure；String 实参升级语义与 IIFE 一致）。**重构**：迭代捕获收集提取为公共函数 `check_closure_body_with_captures`（IIFE 与闭包值共用，行为不变）；匿名函数生成提取 `emit_closure_fn`。**接线**：`Type::Closure` 加 Display（`closure(A) -> B`）、`field_scalar_of` → Ptr、`type_slot_count` → Ok(1)；rlyeh-fmt 支持 `|x: i64|` 注解格式化（zip params/param_types）；rlyeh-check 模式匹配补 `param_types` 字段。MVP 约束：参数须全注解（半注解报 Unsupported 提示）、仅按值捕获、闭包值仅局部变量环境不跨函数边界。产出 `tests/run-pass/closure_value.{rlyeh,out}`（14 输出：绑定/调用、多参数、单/多捕获、无捕获、闭包值别名、复用多次调用、String 捕获 + 方法调用、String 实参升级、块表达式闭包体、捕获拼接、捕获数组索引）+ 2 compile-fail（半注解、实参类型不匹配）+ 全量 122 套件 681 测试 0 失败 + clippy 0 警告。既有行为确认：`let s = "hi"; s.len()` 与 `n.to_string()`（`i64::to_string`）在普通代码同样报 not found（`&str` 视图无 len 方法、to_string 未注册），非闭包值回归。
- [x] **宏系统补全（`$x:expr` 多 token + `r#"..."#` 原始字符串）**（2026-08）：**① `$x:expr` 完整表达式匹配**——rlyeh-macro 新增 token 级优先级爬升 `consume_expr`（替代 `consume_atom`）：`consume_operand`（前缀一元 `-`/`+`/`!`/`not`/`&`/`*` + 原子——标识符/`true`/`false`/数字/字符串/字符/时间字面量/定界组整体 + 后缀——调用 `(...)`/索引 `[...]`/成员 `.x`/`?`/`as` 转换/宏调用 `f!(...)`）+ `consume_ty`（`as` 后的类型：`i64`/`String`/`Vec<i64>`/`mod::Type` 路径 + 尖括号泛型深度配对）+ 中缀二元循环（`||`/`&&`/位运算/比较/`in`/`not in`/加减乘除模/移位/范围 `..<`/`...`）；表达式边界遇 `,`/`)`/`]`/`}`/`;`/`=`/`=>`/`:` 停止；**关键调试教训**：循环末尾 `input.get(pos)?` 在 token 流结束返回 None 使整个匹配失败——须 `None => break`。多 token 表达式按原样展开（`twice!(3 + 4)` → `3 + 4 * 2`，优先级由调用方负责，与 Rust 一致）。**② `r#"..."#` 带哈希原始字符串**——lexer 新增 `read_raw_hash_string`（任意 `#` 定界、内容到 `"` + N 个 `#` 闭合、无转义）；分发分支按 `r"` / `r#..."#`（一个或多个 `#` 后跟 `"`）/ `r#ident` 顺序区分（**关键**：多哈希 `r##"` 的 peek_nth(2) 是 `#` 非 `"`，须扫描 `#` 序列后查 `"`）。**③ 现状修正**：`check_format_macro` 多参数 `{}` 格式化早已实现（`tests/run-pass/macro.rl` 已有 `println!("{} {} {:?}", a, b, true)`），guide.md §13 旧文案过时一并修正。**既有行为确认**：`let s = "hi"`/`r#"..."#` 绑定为 `&str` 视图（无 `len` 方法）、`(2.5 as i64) * 2` clang 层 `fmul` 类型错误均为既有行为（git stash 验证非回归）。产出 `tests/run-pass/macro_expr_multi.{rlyeh,out}`（12 输出：中缀原样展开/括号优先级/多参数比较/一元负/嵌套宏/函数调用/方法调用/r# 无转义/r## 含双引号/String::from 绑定/数组索引）+ rlyeh-macro 单测 `test_expr_multi_token`（中缀/比较/一元/调用/成员/as/范围）+ `test_expr_boundary_stops`（逗号边界）+ rlyeh-lexer 单测 `test_raw_hash_string`（r#/r##/r###/与 r#ident 区分）+ 全量 122 套件 684 测试 0 失败 + clippy 0 警告。剩余限制：无卫生宏（hygiene，全局名称匹配）。
- [x] **K4 `Gc<T>` 追踪 GC（MVP 保守标记-清除）**（2026-08）：编译器内建 + 独立运行时 crate `rlyeh-gc-runtime`。**布局**：`Gc<T>` 栈上 1 槽（Ptr）指向堆 1-槽包装（`Alloc{slots:1}`，槽 0 存 `GcInner` 基址）；对象 = `slot_count(T)` 个 8 字节槽连续堆块，`T` 值区自堆首槽起（与 `Box<T>` 完全同构，解引用/剥层零差异）。**生命周期协议**：`gc_region` 块 desugar 为 `rlyeh_gc_region_begin()`（`epoch += 1`）→ `rlyeh_gc_alloc(n)`（malloc 对象 + 注册块表 + 记录 epoch）→ `rlyeh_gc_escape(ptr)`（块返回值登记逃逸 root，空指针空操作）→ `rlyeh_gc_collect()`（从逃逸 root 标记 → 清除 `epoch` 匹配未标记对象 → 存活对象提升为 root → `epoch -= 1`）；块外对象 epoch 恒小于任何块 → 永不回收（MVP 泄漏语义）；跨块存活引用链经"存活提升"连续保护（内层逃逸对象在后续块 collect 中为 root）。**内建 API**：`Gc::new`（`check_gc_new`：alloc + 值区写 + 1-槽包装 + `FieldSet` 存 base）、`gc_region` 块（`check_gc_region`：body 语句 + final_expr 绑定 `__gc_escape` + escape 调用 + collect 调用 + 块表达式类型）、字段/方法/索引自动剥层（`heap_ptr_hir` 统一 Box/Rc/Arc/Gc）。**关键教训**：① `rlyeh_gc_escape` 参数必须传**对象 base**（`heap_ptr_hir` 解包装槽 0）——初版传 `Variable(esc)`（包装指针）导致 `mark` 线性查找（`header.base == root.base`）失配、对象被误回收（悬垂读取）；② `collect` 存活提升条件须为**所有被标记对象**（`marked`）而非 `marked && epoch == s.epoch`——嵌套块 collect 释放全部旧 root 节点，仅提升本块对象会使外层逃逸对象（`epoch < s.epoch` 存活但未标记可达）失去保护，在后续外层 collect 中因未标记且 epoch 匹配被误回收；③ **调试提醒**：`rlyeh run`/`build` 增量缓存命中会跳过 typecheck（源码哈希未变复用旧 LLVM IR），改编译器代码后必须 `--force`。**运行时加固**：全部动态内存只用 `libc::malloc`/`free`（对象块 + header/root 元数据链表），不用 Rust 堆分配与 `realloc`——macOS C 主程序环境实测 `RawVec`（`Vec::with_capacity`/`push` 扩容）与 `libc::realloc` 在 `libc::malloc` 之后调用触发 `libsystem_malloc` 的 `mfm_alloc` 崩溃（`_os_unfair_lock_unowned_abort` / SIGKILL，复现实验见 `rlyeh-gc-runtime` 模块头注释）；全局状态 `SyncUnsafeCell` 单线程无锁（MVP 编译产物单线程串行调用约定，无 Mutex）。产出 `tests/run-pass/gc_region.{rlyeh,out}`（15 输出：块内分配+解引用、逃逸对象跨块存活、字段/方法/索引剥层、嵌套块存活链式提升、循环重复收集、Gc 字段剥层、块外泄漏语义）+ `tests/compile-fail/gc_bad.rl`（`Gc::new()` 参数个数断言）+ 全量 36 用例全绿 + cargo test 全绿。
- [x] **K3 `Rc<T>`/`Arc<T>` 引用计数装箱**（2026-08）：编译器内建（typecheck 特判，零新增 HIR/MIR/LIR/codegen 节点）。**布局**（MVP 注记，与 memory-model.md §4 顺序差异见该文档）：堆 `RcInner` 的 `T` 值区自堆首槽起（槽 0..`n`，`n = slot_count(T)`，与 `Box<T>` 同构，解引用/剥层零差异），尾部两计数槽：槽 `n` = strong_count、槽 `n + 1` = weak_count；`Rc<T>` 栈上 1 槽（Ptr）指向 RcInner，分配 `(n + 2)` 个 8 字节槽。内建 API：`Rc::new`/`Arc::new`（值区写后 `FieldSet` GEP 写计数 1/0——**关键教训：`DerefSet` base 是"地址"而 `FieldGet` 是 load 槽值，计数/值区写必须用 `FieldSet`（GEP + store），不能经 `FieldGet` 取地址**）、`clone`（强计数 +1 指针共享）、`strong_count`/`weak_count`（读计数槽，返回 `Type::USize`）、`downgrade`（弱计数 +1 → `Weak<T>`）、`try_unwrap`（强计数 == 1 → `Ok(T)` 解出值区 / 否则 `Err(Rc<T>)`，HIR 直接构造 Result 枚举对象）、`Weak::upgrade`（强计数 > 0 → 强计数 +1 返回 `Some(Rc<T>)` 否则 `None`，HIR `If` 构造 Option 枚举对象）。接线：`check_rc_method` 在 receiver 推断后、`heap_ptr_hir` 改写前分派（内建需原始 Rc 对象）；`heap_ptr_hir` 统一 Box/Rc/Arc（槽 0 即值区首槽，Rc 分支与 Box 合并）；`peel_refs_and_heap` 剥层；`Rc::new`/`Arc::new`/`Weak::upgrade` 静态路径特判（check_call `split_once("::")` 块）；Deref 分支扩展 Rc/Arc。调试收获：`rlyeh run` 缓存 key 不含编译器版本，改编译器后必须 `--force`；`try_unwrap` 首版 else 分支漏写 Err payload 槽致读未初始化内存崩溃（match `Err(rc)` 绑定读 payload）。产出 `tests/run-pass/rc_new.{rlyeh,out}`（17 输出：标量/方法/索引/字段剥层、clone 计数、弱引用升级、try_unwrap Err 分支、Arc 同构、Rc<Vec> push）+ `tests/compile-pass/rc_ty.rl` + `tests/compile-fail/rc_bad.rl` + 全量 33 用例全绿 + cargo test 全绿 + clippy 0 警告。
- [x] **K2 `Box<T>` 堆分配装箱**（2026-08）：`Box::new(v)` 编译器内建（无 std 结构体定义，纯 typecheck 特判）+ `*` 解引用 + 字段/方法/索引自动剥层。布局：**`Box<T>` 栈上 1 槽（Ptr）存堆指针，堆上分配 `slot_count(T)` 个 8 字节槽的连续对象区**（与对象槽区布局同构）——`Box::new` desugar 为 `alloc_bytes(8*n)` + 写入（标量 T 经 `HirExpr::DerefSet` 直接写堆首槽，复用 G1 解引用写链路；聚合 T 经 `array_copy` 整槽区 memcpy，浅拷贝与 MVP 结构体赋值一致）+ 1 槽 `Alloc` 对象（槽 0 = 堆指针）。核心新增：`check_box_new`（参数检查 + `type_slot_count` 槽数计算：标量 1 / struct 字段数 / enum `slot_count` / 数组长度 / 元组元素数 / 引用·Fn·内嵌 Box 1 槽 / 动态数组 1 指针槽）+ `peel_box`/`peel_refs_and_boxes`/`box_ptr_hir`（`Box<T>` 表达式 → 堆对象指针 `FieldGet(box, 0, Ptr)`）。接线五处：① `UnaryOp::Deref` 加 `Box<T>` 分支（标量 load / 聚合指针拷贝，与 `&T` 同构）；② 字段访问 `peel_refs_and_boxes` + base 改写；③ 方法调用 receiver 改写（`Box<String>` 的 len/索引、`Box<Vec<i64>>` 的 push 均可，`&self` 收到 `T` 对象指针）；④ 索引访问 base 改写；⑤ `as_str` 特判支持 `Box<String>`。`Vec::with_capacity` 无上下文返回 `Vec<Infer>`，测试经 `Box<Vec<i64>>` 注解统一。嵌套 `Box<Box<i64>>`（`**bb`）与 Box 赋值指针共享（浅拷贝）可用。产出 `tests/run-pass/box_new.{rlyeh,out}`（15 输出：标量/浮点/布尔解引用、类型注解、嵌套、String 方法+索引、struct 字段+聚合解引用、Vec push+索引、指针共享）+ `tests/compile-pass/box_ty.rl`（函数参数/返回值注解）+ `tests/compile-fail/box_bad.rl`（`Box::new()` 参数个数断言）+ 全量 30 用例全绿 + cargo test 全绿 + clippy 0 警告。
- [x] **J1–J3 迭代器与集合协议**（2026-08）：
  - **J1 数组迭代**：`for x in arr`——typecheck 新增 `check_for_array`（数组以指针存储、长度编译期已知，desugar 为 `let __for_arr = <数组>; let mut __for_i = 0; loop { if __for_i >= N { break; } let pat = __for_arr[__for_i]; __for_i += 1; body }`，元素读取复用 `HirExpr::Index` 步长 8 / u8 按字节；元素类型 Infer 报 Unsupported）。`check_for` 分派加 `Type::Array` 分支。
  - **J2 自定义迭代器接入 for**：接收者类型存在 `next() -> Option<Item>` 方法（inherent / trait impl，`find_impl_for_method` + `unify` 替换）时，构造 AST `let mut __for_it = it; loop { match __for_it.next() { Some(__elem) => { let pat = __elem; body }, None => break } }` 交 `check_for_iterator` infer（复用 check_method_call / check_match 全链路）。配套修复：parser `stmt_terminator` 补 `,`（`match { None => break, }` 臂体解析）。测试 `tests/run-pass/iterator_for.{rlyeh,out}`（inherent + trait 迭代器 / 手动 next / 按值绑定，6 输出）。
  - **J3 适配器**：`map`/`filter`/`fold`/`collect`/`take`/`skip` 内建 desugar（`check_method_call` 入口特判，接收者为数组 / `Vec<T>` / 自定义迭代器时启用）：新增 `closure_return_ty`（闭包体实际返回类型预推断，U 用于收集容器类型注解，避免 `Vec<Infer>` 被 for 拒绝）、`ty_to_ast`（Type → AstType 注解）、`try_check_adapter`/`check_iterator_adapter`。desugar 结构：闭包经 `check_closure_expected` 按 `fn(T...) -> U` 签名注入匿名函数（H2，返回函数指针 `__adp_f`）；收集容器 `let mut __out: Vec<U> = Vec::new();`（带注解）；循环复用现有分派——数组/Vec 走 `for __x in ..`（J1/check_for_vec），迭代器走 `loop { match it.next() { Some(__x) => apply, None => break } }`。apply：map `let __u = __f(__x); __out.push(__u);` / filter `if __f(__x) { push }` / fold `__acc = __f(__acc, __x);`（返回 acc）/ take `if __n < n { __n += 1; push } else { break }` / skip `if __n < n { __n += 1 } else { push }` / collect `push`。适配器结果 Vec 亦可作下一适配器源（链式）。产出 `tests/run-pass/adapters.{rlyeh,out}`（13 输出：数组/Vec/迭代器 × 6 适配器 + 链式）+ `tests/compile-fail/adapter-badargs.rl`（非闭包参数断言）+ 全量 27 用例全绿 + cargo test 全绿 + clippy 0 警告。
- [x] **K1 `?` 运算符**（2026-08）：`expr?` 在 Option/Result 上下文 desugar 为 `match` + `return` 早返回（零新增 HIR 节点，复用 check_match 的 if-else 链 + tag 比较）。核心：① 拆分 `check_match_with_scrutinee`（接受调用方已推断的 scrutinee HIR + 类型，避免 inner 内嵌闭包重复 desugar）；② `check_question`（typecheck）——infer inner → `Type::Named("Option"/"Result")` 特判 → 构造 AST MatchArm（成功臂 `Some(__v) => __v` / 失败臂 `None => return Option::None`、`Err(__e) => return Result::Err(__e)`，失败变体经完整路径构造）+ `check_match_with_scrutinee` 执行；非 Option/Result 类型报 Unsupported。③ 裸无参变体值表达式支持：infer_expr Ident 分支补 `split_variant_path` 兜底（`return None;` 可用）。接线：parser 后缀循环 `?`（AST `ExprKind::Question`）；rlyeh-fmt（PREC_POSTFIX 后缀重建）/rlyeh-check（walk_expr 补分支）。MVP 约束：返回类型兼容性检查与现有 `return` 语义一致（宽松）。产出 `tests/run-pass/question.{rlyeh,out}`（5 输出，含表达式中间嵌套双 `?`）+ `tests/compile-pass/question_result.rl`（Result 解包）+ `tests/compile-fail/question-nonoption.rl`（非 Option/Result 报错断言）+ 全量 23 用例全绿 + clippy 0 警告。
- [x] **H2 无捕获闭包**（2026-08）：`|x, y| expr` desugar 为匿名函数（`__closure_N`）+ 函数指针，零运行时开销。核心：`check_closure_expected`（typecheck）——按预期 fn 签名取参数类型、`std::mem::take` 清空变量环境实现无捕获隔离（body 引用外部变量 → `UndefinedVariable` 改写为 Unsupported「闭包捕获外部变量（H3 规划）」）、body 尾部表达式兼容返回类型、注册 `fn_signatures` + `ctx.mono_items` 注入 `HirItem::Fn`（复用 H1 函数指针全链路）。接线三处：`check_call` / `check_indirect_call` 实参循环（形参 `Type::Fn` + 实参闭包 → 预期签名检查）+ `check_stmt` Let 分支（fn 注解 + 闭包 init）。MVP 约束：参数无类型注解需 fn 上下文、仅标识符/`_` 参数模式、不支持返回闭包的函数（`let f: fn(..) = |..| ..; f` 转接）、`move` 语义规划。产出 `tests/run-pass/closure.{rlyeh,out}`（7 输出）+ `tests/compile-pass/closure.rl` + `tests/compile-fail/closure-capture.rl` + 全量 20 用例全绿 + clippy 0 警告。
- [x] **H1 函数类型与函数指针**（2026-08）：`fn(A) -> B` 函数类型 + 函数一等值全链路（parser → typecheck `Type::Fn` 签名 + `infer_expr` 函数值推断 + `check_indirect_call` 兜底 → HIR `HirExpr::FnPtr`/`CallIndirect` → MIR/LIR `CallIndirect` → LLVM：函数指针统一存 `i8*` 槽，存储前按签名 `bitcast i64(...)* @fn to i8*`，调用时 `bitcast` 回 `{ret}({params})*` 后间接 `call`）。函数值支持作实参、作返回值（`choose`）、重新绑定、显式类型注解。**关键修复**：① MIR DCE 活跃性收集漏算 `CallIndirect` 实参（实参临时赋值被误删 → 未初始化栈读取垃圾值）；② MIR inline pass 缺失 `CallIndirect` 分支（内联后整个间接调用被丢弃，返回值临时从未写入）；③ borrowck/regionck 补 `FnPtr`/`CallIndirect` 分支；④ `mir_lower_test.rs` match 补 `call_indirect` arm。产出 `tests/compile-pass/fn_ptr.rl` + `tests/run-pass/fn_ptr.{rlyeh,out}` + 全量 17 用例全绿 + clippy 0 警告。
- [x] **L2 `serde` 序列化模块**（2026-08）：`json::stringify` / `json::parse::<T>` 编译器内建，**零新增 IR 节点**——typecheck 期 desugar 为 String 构建/解析表达式。**turbofish 泛型实参**（`::<T>`）：parser `check_turbofish()`（Colon+Colon+Lt 三 token 前瞻）→ 路径解析循环 break → Call 后缀分支解析 `::<T1, T2>(args)` 类型实参；`ExprKind::Call` 新增 `type_args: Vec<AstType>` 字段（更新全部构造点与模式匹配：parser/typecheck/fmt/check）。**stringify**（`check_json_stringify` + `json_serialize_ast`）：i64→`int_to_string`、bool→if 三元、String/&str→`"`+`json_escape`+`"`（转义 `"`(34)/`\`(92)/换行(10)/制表(9)）、数组→静态展开、struct→`{"f":v,...}`（字段序=定义序，嵌套递归）、Vec→while 循环 push_str。**L2f HashMap 序列化**：`Type::Named("HashMap")` 分支——substitute 键/值类型（非 Infer 校验）→ K 限 i64/String → 构建块：`String::new()` + `{` + `for (k, v) in m`（`check_for_hashmap` 容量扫描、`first` 标志控逗号）→ 键序列化（i64→`"`+int_to_string+`"`、String→json_escape 带引号）+ 值递归 `json_serialize_ast` → push `}`。**关键修复**：`Vec` 本身是 std struct（data/len/cap 字段），struct 分支须置于 Vec 分支之后且 guard 排除（否则 `Vec<i64>` 被误序列化为 `{"data":[],"len":3,"cap":3}`）；`vec![...]` 绑定后元素类型为 `Vec<Infer>`（push 不反向精化接收者），须注解定型（与 `for x in v` 约束一致，`check_for_vec` 同款报错）；**map! 宏 String 键缺陷**（字面量键无 `String::from` 时运行时崩溃）与 **unify 多类型参数错误定型**（substitute 后 `Infer` 被统一为首实参类型）——修复为 `check_method_call` 按 `raw_params`（原始参数类型）逐名绑定（含 Str→String 定型升级，避免 K 被定型为 Str 视图崩溃）。**parse**（`check_json_parse` + `json_parse_ast`）：缺 `::<T>` type_args 报错；i64→`string_to_int`、bool→字节比较、String→`json_unescape`（剥离首尾引号 + 还原转义）；MVP 语义直接返回 `T`（非法输入给默认值 0/false/空串，非 Result 包装）。**L2g HashMap 反序列化**：`Type::Named("HashMap")` 分支——K 限 i64/String、V 限标量（嵌套 HashMap 显式 Unsupported，`split(",")` 无法处理内层逗号）；desugar 为块表达式：`String::from(arg)` → `substring(1, len-1)` 剥离 `{}` → `split(",")` 分段 → `for x in vec` 遍历（`find(":")` 分键值 → 键经 `json_unescape`（i64 再 `string_to_int`）+ 值递归 `json_parse_ast` → `__m.insert(__k, __v)`），`let __m: HashMap<K,V>` 注解定型（空 `{}` 亦定型）；parser turbofish 支持嵌套泛型 `>>` 拆分（`pending_gt` 机制复用 `close_generics`，`::<HashMap<i64, i64>>(s)`）。**std**：core.rl 新增 `json_escape`/`json_unescape`（`out` 为保留字，局部变量用 `buf`）；**LLVM 转义修复**：字符串常量 `\"` 不可靠（clang 误解析为 `[1 x i8]` 长度不匹配）→ 十六进制 `\22`。产出 `tests/run-pass/json_serde.{rlyeh,out}`（28 输出：标量/数组/struct/Vec/HashMap stringify（i64·String 键、空 map、嵌套 Vec 值）+ parse i64·bool·String·HashMap（多键 get/缺失键/空对象/String 键·bool 值/String 值）+ stringify→parse 往返）+ `crates/rlyeh-parser` 单元测试 `test_turbofish_nested_generics_shr_split` + 全量 43 用例全绿 + cargo test 全绿（678 通过 0 失败）。文档：std-lib.md §9 状态标注 + §9.1 JSON 小节；guide.md §9.4 + §13 更新；grammar.md 顶部 + PostfixOp turbofish EBNF；CODEBUDDY.md §5.5 记录。
- [x] **L1 普通函数 `async fn`/`await`**（2026-08）：MVP 同步语义，**零编译器代码改动**——`async fn` 关键字经 parser（`AstFn.is_async`）解析后由 typecheck 忽略（HirItem 无 async 概念，编译为普通同步函数）；`expr.await`（`ExprKind::Await`）在 typecheck 直接 `infer_expr(inner)` 求值（`.await` 为语法标记，同步求值）。**设计**：抽象复用 actor 异步机制（actor `async` 方法 `.await` = ask 同步往返，MVP 同步语义），普通函数同构——`async fn foo(x) -> T { ... }` ≡ `fn foo(x) -> T`（async 丢弃）；`foo().await` ≡ `foo()`；`foo().await + bar().await` 等表达式中间 await 可用；支持嵌套（async fn 内 `.await` 另一 async fn）、String/数组等非标量返回。**验证**：产出 `tests/run-pass/async-fns.rl` + `.out`（6 输出：直接调用 / `.await` / 嵌套 await / String 返回 `.len()` / 数组返回 + 索引 / 表达式中间 await）+ 全量 42 用例全绿。**边界**：actor 类型不可作普通函数参数（undefined type，既有行为）；`Future`/executor/`block_on`/`join_all` 仍为 std-lib §10 规划 API（L1 仅同步语义）。**文档**：guide.md §9.3 新增 async fn 小节 + §13 更新；std-lib.md §10 状态标注；grammar.md 顶部 async 标注实现；CODEBUDDY.md §5.5 记录。
- [x] **I3 集合宏 `arr!`/`vec!`/`map!`**（2026-08）：parse 期 desugar，零新增 IR 节点。**设计**：`arr![a, b, c]` → `ExprKind::ArrayLit`（元素类型统一由 typecheck 校验）；`vec![...]` → 块表达式 `let mut __vec_N = Vec::with_capacity(n); __vec_N.push(e0); ...; __vec_N`（`map!` 同构：`HashMap::with_capacity(n)` + `.insert(k, v)`，元素须 `k => v`）；空集合 → `Vec::new()`/`HashMap::new()`。**实现**：`parse_macro_call` 中 `is_collection_macro` 分支 → `parse_collection_macro`（`crates/rlyeh-parser/src/expr.rs`）；元素 token 流包裹 `(` `)` 后经子 `Parser::from_tokens`（继承宏注册表，支持嵌套宏调用与完整表达式）解析——`arr!` 走 `parse_call_args`，`vec!`/`map!` 先 `expect(LParen)` 再逐元素循环（`parse_expr` + 可选 `FatArrow` 对 + `Comma` 分隔，`check(RParen)` 空集合短路，`expect(RParen)` 收尾）；Parser 新增 `collection_temp_seq` 计数器生成 `__vec_N`/`__map_N` 临时变量。**关键教训**：子 Parser 手动元素循环必须先消费包裹的 `(`——`parse_call_args` 内部自消费，直接照搬循环会在 `vec![]` 处把 `(` 当分组表达式解析触发 UnexpectedEof（空 `( )` 组）。**零下游改动**：`Vec::with_capacity`/`HashMap::with_capacity` 为 typecheck 既有构造器特判（`resolved.split_once("::")`），`push`/`insert` 为 std 普通方法查询，块语义复用现有 desugar。产出 `tests/run-pass/collection_macros.{rlyeh,out}`（10 输出：arr! 索引 / vec! 带元素·空·嵌套宏元素·绑定变量算术元素 / map! 键值对·get·空·键冲突覆盖 / vec! 结果可变 push）+ 全量 41 用例全绿 + cargo test 全绿 + clippy 0 警告。
- [x] **I1 声明式宏 + I2 内置格式化宏**（2026-08，新 crate `crates/rlyeh-macro`）：`macro_rules!` 声明式宏——matcher/transcriber token 解析、匹配 `$x:expr`/`$x:ident`/`$x:ty`/`$x:tt` 四类元变量 + `$(`...`)` 重复 `*`/`+`/`?`，`MacroError(String)`；lexer 新增 `$`/`?` token（`Dollar`/`Question`，`!` 保留 `not` 一元运算符语义）；parser 集成——`macro_rules!` 定义注册（`parse_macro_rules` + `collect_group_content` 深度计数定界组收集，`AstMacroDecl` 占位节点）、`name!` 调用（内置宏 → `MacroCall` AST；用户宏 → `expand_macro` 递归展开 + 子 Parser `from_tokens` 再解析，`MAX_MACRO_DEPTH=64` 防爆栈、多余 token 报错）；AST 新增 `ExprKind::MacroCall`；typecheck 格式化引擎——`parse_format_string`（`{}` 值占位 / `{:?}` 同构 / `{{` `}}` 转义 / 占位符数量检测）+ `value_to_string_for_ty`（i64→`int_to_string`、bool→If 三元、String/&str/Str→`String::from`）+ `check_format_macro`（`+` 拼接复用 A3 clone+push_str 语义 → 内建 `println`）+ `check_dbg_macro`（`let __tmp` + println + 返回 `HirExpr::Block`）。关键修复：① matcher 路径 `expect(LParen)` 提前消费导致 `collect_group_content` 双重 bump 吞掉首个 `$` token（移除预消费）；② `dbg!` 先推断参数类型再按预推断类型构造 String 转换（避免闭包 IIFE 参数上下文丢失）。MVP 限制：`$x:expr` 仅匹配原子 token（`a > b`/`-1` 等多 token 表达式不支持）、无卫生宏（hygiene）、I3 集合宏 `vec!`/`map!`/`arr!` 与 `println!(r#"...")` 未实现；`Display`/`Debug` trait 定义仍为规划 API（std-lib.md §8）。产出 `tests/run-pass/macro.{rlyeh,out}` + `tests/compile-pass/macro.rl` + 全量测试全绿 + clippy 0 警告（详见 CODEBUDDY.md §5.5 宏系统记录）。
- [x] **`ref` / `ref mut` 模式**（2026-08，G1 收尾）：`match` 臂与 `let ref x = e;` 中 `ref x` 将 `x` 绑定为对匹配值的**引用**（`&T`）而非值拷贝，`ref mut x` 绑定 `&mut T` 且可经解引用写。**实现**：parser 早已产出 `AstPattern::Ref(inner, is_mut)`（pattern.rs）；typecheck `check_pattern` 原实现**忽略 ref 前缀**（值拷贝语义）——改为递归检查内层模式后，将每条绑定语句的初始化包装为 `HirExpr::Ref { expr, is_mut, pointee: field_scalar_of(pat_ty) }`（聚合 = 对象指针拷贝，标量 = 存储槽地址；MIR `AddrOf` 经 `lower_expr` 对任意目标兜底求值到临时变量，零 MIR/codegen 改动），绑定变量类型引用化为 `Type::Ref(T, mutability)`，可变性原样传递；`check_stmt` let 分支新增 `let ref [mut] x = e;`（仅 `ref` 包裹标识符，复杂内层模式报 Unsupported）。**语义注意（与 Rust 的差异）**：Rlyeh `match` desugar 先把匹配值**拷贝**到临时槽，`ref` 绑定指向该拷贝——`ref mut` 写作用于拷贝，原变量不受影响；`ref` 的价值在省去绑定时再次拷贝，聚合经 `ref` 为对象指针拷贝、不复制底层数据。**覆盖**：标量 / String / 枚举子模式 `Some(ref v)` / struct 字段访问（引用自动剥层）/ `ref mut` 解引用写 + 读 / `let ref x = e;` / 聚合字段 `Some(String)`。产出 `tests/run-pass/ref_pattern.{rlyeh,out}`（8 输出）+ 全量回归绿 + clippy 0 警告。文档：guide.md §8.1/§13 + CODEBUDDY.md §3.9。
- [x] **Str 值实参自动升级（String 形参位置）**（2026-08，Str 值特性的收尾补全）：`String` 形参位置传入字符串字面量（或绑定字面量的变量）自动构造 String 对象——`m.push_str("!")` / `m.contains("z")` / `map.insert("k", 1)` / `f("hi")` 直接可用，消除 `String::from` 书写负担（与 IIFE / 闭包值实参既有升级语义一致）。**实现**：提取 `upgrade_str_arg(ctx, hir, ty, param_ty, arg)` helper（`check_expr.rs`，`check_string_from` 之后）——实参 `Type::Str` 且形参非 `Type::Str` 时经 `check_string_from` 升级；接入 6 处实参检查点：`check_call`（普通函数）/ `check_indirect_call`（函数指针）/ `check_static_method_call` / `check_method_call`（实例方法，升级在 contains_infer 定型前——`HashMap::new()` 无注解 + `insert("a", 1)` 从"需 String::from"变为自动升级，K 定型结果不变）/ `check_trait_object_call`（dyn Trait 方法）/ `check_generic_call`（保守：仅形参为具体 `String` 时升级，泛型 T 位置保持 Str 推断不改变既有结果）。**关键修复**：`check_generic_call` 第二个类型检查循环（实例化后按具体签名重查）漏升级，补同一升级逻辑。**边界**：`str` 形参保持裸字面量指针直传（不升级）；运行期 `str` 值实参（函数参数，无编译期长度）报 Unsupported；`&str` 形参经升级后的 String 值 + String↔`&str` 兼容规则接收。产出 `tests/run-pass/str_value_arg.{rlyeh,out}`（17 输出：push_str/contains/starts_with/ends_with/find/replace/split/strip_prefix/strip_suffix 方法实参、绑定字面量变量实参、普通函数、函数指针、静态方法、泛型函数混合参数、HashMap 无注解字面量键）+ 全量回归绿 + clippy 0 警告。文档：guide.md §10.2/§10.4/§10.5/§10.6/§13 + CODEBUDDY.md §3.9。
- [x] **`str` 值一等类型（Str 值升级为 String 对象）**（2026-08，G2 后续补全）：`let s = "hi"` 绑定的 `s`（`Type::Str`，运行时为指向静态字面量数据的 `i8*` 指针，无 String 的 len/cap 槽）参与字符串操作时自动**升级为 String 对象**（编译期长度展开，复用 `check_string_from` 字面量/绑定变量分支，`pub(crate)` 化）。**① 方法调用**——`check_method_call` 中 `Type::Str` 值接收者（非 `&str` 引用）升级 `recv_hir`/`recv_ty` 后按 String impl 解析（`s.len()`/`substring`/`contains`/`as_str` 均可用）；`&str` 引用接收者保持直接归一（String 对象瘦指针）。**② 拼接**——`check_expr` 二元 `+` 分支 `l_str`/`r_str` 纳入 `Type::Str` 值（`comparison::is_str_value`）与 `&str` 视图，Str 值操作数升级后走既有 clone+push_str 拼接（`s + "!"`/`"ab" + "cd"`/`s + s`）。**③ 比较**——`comparison.rs` `lhs_str`/`rhs_str` 纳入 Str 值，字典序/相等分支升级操作数后走 `string_lt_hir`/`string_eq_hir` 内容比较（Str 值 ↔ 字面量/String/`&str`）；字符串组合豁免 `check_comparison` 的类型兼容检查（`s == String::from("hi")` 此前报 ChainTypeMismatch）。**④ 关键根因修复**：`check_fn_body_with_self`（check_item.rs）隔离 `ctx.variables` 但**未隔离 `ctx.local_inits`**——std 方法体检查把同名局部变量（如 `s`）insert 进全局 `local_inits`，覆盖调用方 `let s = "hi"` 的字面量绑定追踪（`s` 的 init 被覆盖为 `Variable("start")`），使后续 `String::from(s)` / Str 值升级查不到内容报 Unsupported；补上 `saved_inits` take/恢复（Err 提前返回与正常返回两处）。**边界**：运行期 `str` 值（如 `fn f(s: str)` 函数参数，内容/长度运行期未知）无法升级，方法调用报 Unsupported；`m.push_str("!")` 方法实参字面量不升级（既有行为，须 `String::from("!")`）。产出 `tests/run-pass/str_value.{rlyeh,out}`（17 输出：方法/拼接/比较/视图/升级组合）+ 全量 122 套件 685 测试 0 失败 + clippy 0 警告。
- [x] **G2 `str` 切片与 String 补齐**（2026-08）：`&str` 引用切片 + `String::from` 运行期内容。**块一**：`String::from(runtime_string)` desugar 为 `s.clone()`（标准库深拷贝，运行期读 len 槽——消除 §13 约束 4 的"长度表达未实现"；签名相同的重声明不受影响）。**块二**：`&str` 视图（`&str` 运行时 = 指向 String 对象的瘦指针，复用 G1 聚合引用机制，MIR/LIR/codegen 布局零改动）：`String::as_str()` 特判 desugar 为 `HirExpr::Ref`（对象指针拷贝）返回 `Ref(Str)`；`resolve_ast_type` 识别 `str` 类型关键字；方法调用把 `Ref(Str)` 接收者归一为 String impl（len/substring 等）；`check_index`/`check_slice` 对 `&str` 先取 data 槽（同 String 对象）；`String::from(&str)` 读 data/len 槽深拷贝（alloc_bytes + copy_bytes + 三槽）；`compatible_with` 允许 `&str` ↔ `&String` 互视 + 视图与 String 值比较互用；`comparison.rs` 字符串内容比较纳入 `&str`。**设计决策**：计划原文"substring 返回引用而非拷贝"改为**兼容方案**——`substring` 保持 String 拷贝返回（避免破坏大量现有调用者与文档 API），零拷贝切片以新增 `as_str()` 借用视图体现。产出 `string_from_runtime_test.rs` 6 用例 + `str_ref_test.rs` 9 用例 + 全量 114 套件回归绿 + clippy 0 警告。
- [x] **G1 引用类型与表达式**（2026-08）：`&T`/`&mut T` 类型 + `&x`/`&mut x`/`*` 表达式全链路打通（parser → typecheck → HIR → MIR → LIR → LLVM）。核心设计：**标量引用存 `i8*` 值槽、聚合引用存对象指针拷贝**；`*p` 读取/写入时按标量种类 `bitcast i8*` 转换；字段访问/方法调用自动 `peel_ref`（`p.x`/`v.len()` 免显式解引用）；`&mut T` 兼容 `&T` 参数（宽松规则，严格可变性互斥留给 borrowck）。关键修复：① MIR inline 补 `AddrOf`/`DerefRead`/`DerefWrite` 三指令映射（否则内联丢弃引用指令、标量读取输出垃圾）；② MIR DCE 活跃性分析纳入 Deref* 读操作（`*p = *p + 1` 的临时变量免被删）；③ codegen `operate_value` 与 `store_to` 指针拷贝避免 `%%` 转义；④ **顶层同名遮蔽**：用户顶层 `fn read` 与 std extern `read` 重名时，用户侧声明注册为 `read@shadow<N>` mangle 名（仅签名不同时），std 原名保留——std 模块（如 `io.rl` 内部 `read(0, tmp, 256)`）仍绑定 extern 原版，用户顶层代码经 `fn_shadow_of` 绑定自身版本，下游符号名唯一不冲突；签名相同的重声明（如 `extern fn __rlyeh_target_os()`）视为无害重声明保留原名。产出 `reference_test.rs` 10 用例（标量读写/复合赋值、struct 读写、vec 方法、引用链、别名拷贝、返回引用、`&mut`→`&` 传递）+ 全量 112 套件回归绿。

- [x] **X1 时间 API 完整化（3c 阶段 X 首个快赢项）**（2026-08-25）：`Duration` 补目标 API 构造器 `microseconds`/`nanoseconds` + 读方法 `as_secs`/`as_millis`/`as_nanos`（u64/u128 → i64 实现，溢出未检查；`secs`/`millis`/`micros`/`nanos` 保留兼容别名）+ `Instant::duration_since` + 新增 `time/system.rl` `SystemTime`（`now`/`unix_epoch`/`duration_since`）。**driver**：`time_builtin_ir` 重构为共享 `__rlyeh_clock_now(clk_id)` + 两个内建 `__rlyeh_clock_monotonic`（CLOCK_MONOTONIC：Linux=1/Darwin=6）与 `__rlyeh_clock_realtime`（CLOCK_REALTIME：两平台均 0，`SystemTime` 用），非支持平台均返回 -1（语言侧 `SystemTime::now` 退回 UNIX 纪元）。**关键教训**：① `time/system.rl` 是 time 的**子模块**（`module system;`），完整路径 `time::system::SystemTime`（同 `io::file::File` 双段模式），import 写 `time::SystemTime` 报 undefined type；② **`module system;` 声明须置于 time/module.rl 文件末尾**——module 声明顺序即收集期注册顺序（io 惯例），放顶部会使 system.rl 展开内容插在 `Duration` 定义之前，其签名引用 `time::Duration` 报 undefined（首次全量回归 99 用例失败即此因）。**`from_secs_f64` 剔除 → U6 恢复**：X1 时依赖 as 转换 IR（typecheck `Cast` 分支直接透传 inner hir、类型改标 target，`f64` 位模式被当 `i64` 用），列入 **U6 数值转换 Cast IR**；U6（2026-08-25）落地后 `Duration::from_secs_f64` 恢复（`(secs * 1e6) as i64`，fptosi 向零截断），X1 验收 `duration_full.rl` 增 1 输出（`from_secs_f64(1.5)` = 1500000us）；X1 状态 ✅ 已完成。产出 `tests/run-pass/duration_full.{rlyeh,out}`（9 输出：microseconds/nanoseconds 构造、as_millis/as_secs/as_nanos 读取、duration_since 同刻差 0、SystemTime 距纪元秒数范围断言、先后关系断言）+ 全量套件 99+ 用例全绿（34.15s）+ cargo test 全绿。文档：std-lib.md §7 状态标注（X1 ✅ / U6）+ 头部说明 + mvp-gaps-plan.md §3c.1/§3c.5/§4 + CODEBUDDY.md §2 结构描述。
- [x] **U6 数值转换 Cast IR（阶段 U 收尾）**（2026-08-25）：`as` 转换从**静默擦除**到完整落地——typecheck `Cast` 分支仅对「数值→数值」且源/目标不同的转换产出 HIR `Cast { expr, to }` 节点（`to` = 目标类型 Display 名；可转换标量 = ≤64 位整族 + `f32`/`f64` + `bool` + `char`，i128/u128 存储非 64 位槽与指针/引用/聚合转换保持擦除，同类型返回 inner 无指令）。**IR 链路**：MIR/LIR 各新增 `Cast { target, value, to }` 指令（`to` 类型名透传）；LIR 类型推断 `cast_target_lir_type` 解析（整族→I64 槽、浮点→F64 槽、bool→Bool、char→Char），names 收集补 target/value。**codegen 发射**（按源 LIR 存储类型 + 目标语义位宽）：浮点→整数 `fptosi`/`fptoui`（向零截断）；整数→浮点 `sitofp`/`uitofp`；整数缩窄 `trunc to iN` + `sext`/`zext` 回 64 位槽（保持"槽值 = 语义值符号/零扩展"不变量，窄到宽直接透传）；整↔bool `icmp ne 0`/`zext i1`（bool 目标源规整一律 fptosi 避免 fptoui 负值 UB）；整↔char `trunc`/`zext`；同存储恒等 `add/fadd 0` 制造裸寄存器。**全链分支补齐**：MIR DCE 活跃性（value 使用）、MIR inline 映射、LIR 类型推断/names/主转换、codegen `touches_any` 与使用收集、borrowck/regionck `HirExpr::Cast` 分支、`mir_lower_test.rs` match。**修复过程中两处 IR 生成 bug**：① 恒等透传把带 `%` 操作数文本直接给 `store_to` 导致 `%%r` 双百分号；② `fptosi`/`zext` 分支返回裸寄存器名被后续指令当操作数（缺 `%`）——统一 `i64v` 为带 `%` 操作数文本、`emit_cast` 仅返回裸寄存器名（`bare_reg` 剥 `%` 或 `add/fadd 0` 造寄存器）。**解锁**：`Duration::from_secs_f64`（X1 剔除项恢复）。验收：`tests/run-pass/cast.rl` 14 输出（`3.7 as i64`=3、`(-3.7) as i64`=-3、`5 as f64`=5.000000、`300 as i8`=44、`(-300) as i8`=-44、`300 as u8`=44、`(-1) as u8`=255、`let a: i8 = -1; a as i64`=-1、`5 as bool`=true、`0 as bool`=false、`true as i64`=1、`'a' as i64`=97、`66 as char`=B、`42 as i64`=42）+ `duration_full.rl` 增 `from_secs_f64(1.5)`=1500000 + 全量 722 测试全绿。**附带**：修复历史遗留错误断言（`regionck_test.rs` `test_transfer_out_of_scope_region` 期望 typecheck 通过但 region 块内变量块外不可见——改为块外定义 x 保持测试意图；`string_test.rs`/`string_case_trim_test.rs` 期望值与实现不符的笔误断言，见上轮记录）。
- [x] **U7 方法级泛型参数（泛型系统深化）**（2026-08-25）：方法可自带 `<U>` 泛型参数（`impl Foo { fn map<U>(x: U) -> U }`），不同 `U` 调用正确单态化。**编译器修复两处**：① `collect_impl`（check_item.rs）方法签名收集时把方法泛型 `U` 并入 `type_params`（原只设 impl 泛型 `T`，`fn map<U>(x: U)` 的 `U` 报 `undefined type`）——循环内临时 `tparams = impl.generics + m.generics`，签名解析后恢复；② `instantiate_impl_method`（check_expr.rs）mono 键/后缀从「仅含 impl 泛型」扩展为「impl 泛型 + 方法泛型」，不同 `U` 调用（`map<i64>` vs `map<String>`）不再因键相同复用首次实例（`U` 错误绑定）。**验证/限制**：`method_generic.rl` 同一 `map<U>` 以 U=i64/f64/String 调用输出 `10/3.5/hi` + 嵌套 `m.map(100)+1`；泛型结构体构造仍受限（`Foo<T> { .. }` 语法不支持、`Foo { .. }` 产生无实参 `Foo`、`T` 无法从字段/注解推导），故以「非泛型 impl 的方法泛型」验收；泛型 trait（`impl<T> Trait<T> for X`）与泛型结构体构造完整化规划为后续。**验收**：新增 `tests/run-pass/method_generic.{rl,out}`；全量 120 用例全绿 + `cargo build` 无警告。
- [x] **U8 泛型结构体构造 + 泛型 trait（泛型系统深化）**（2026-08-25）：补齐 U7 遗留的两项泛型能力。① **泛型结构体构造 `Foo<i64> { x: 1, y: 2 }`**——`ExprKind::StructCtor`（rlyeh-ast）新增 `type_args: Vec<AstType>` 字段，parser/desugar/typecheck/fmt/check 全链路同步；parser `parse_ident_prefix`（expr.rs）新增 `looks_like_generic_struct_ctor`（`<类型...>{` 前瞻模式，不消费 token，避免与 `<` 比较歧义）→ 消费 `<T1, T2>` 类型实参列表；typecheck `check_struct_construct` 解析实参、`substitute` 把泛型字段类型（`T`）替换为实参后校验 `compatible_with`、返回 `Named(struct_name, type_args)`；新增 `TypeError::GenericArityMismatch`（error.rs 枚举 + span/Display）。② **泛型 trait `trait Trait<T>` + `impl<T> Trait<T> for X`**——parser `parse_impl`（item.rs）新增 `looks_like_generic_trait_impl`（`<...>for` 前瞻区分 trait impl 与 inherent `impl<T> Foo<T>`，无回溯），支持 `impl<T> Trait<T> for X` 的 trait 泛型实参消费；typecheck 方法解析无需改动（`find_impl_for_method` 按 self 类型 + `unify` 绑定 impl 泛型，trait 实参由 impl generics 承载，self_type = `Named("X", [Generic("T")])`）。**验证**：`Pair<i64> { a: 100 }` 泛型构造 + 泛型 impl `get` + 方法泛型 `map<U>`（U=i64/String）+ 泛型 trait `impl<T> Wrap<T> for Pair<T>` 输出 `100/7/hi/100`；`Pair<String>` 构造 `yo/yo`。**验收**：新增 `tests/run-pass/generic_ctor_trait.{rl,out}` 6 断言；全量 121 用例全绿 + `cargo build` 无警告。
- [x] **W 阶段全量验收 + 组合测试**（2026-08-25）：对 W1–W5 + U7/U8 整体验收。① `async_combo.rl` 组合验证（泛型结构体 Output future + join_all `Vec<String>` + block_on 泛型化 String + timeout `Result<String, TimeoutError>` + recv_async block_on 驱动，输出 `a/b/zz/tt/42`）；② **`F::Output` 投影对泛型 impl 求值修复**（typecheck `eval_assoc_projection`）——原按 `compatible_with` 匹配泛型 impl self_type（`Generic("T")` vs `String` 不相等）失败返回未替换投影，改按 self_type 名匹配 + base 具体实参替换 assoc_types 占位求值，解锁泛型结构体作为 `Future::Output`；③ 清理无探针/孤儿文件，文档一致性核对。**验收**：新增 `tests/run-pass/async_combo.{rl,out}`；全量 122 用例全绿。
- [x] **V4 `get_mut` 引用语义（V 阶段第 2 项）**（2026-08-25）：std `Vec::get_mut` 与 `HashMap::get_mut` 从值拷贝退化升级为 `Option<&mut T>` / `Option<&mut V>` 引用语义——命中返回对**原槽**的可变引用、越界/缺失键返回 `None`，调用方 `match { Some(r) => *r = x }` 经解引用写回真实槽（非拷贝）。**关键前提（实测确认）**：编译器已支持嵌套引用类型返回（`Option<&mut T>` 返回类型 + `Some(&mut self.data[i])` 构造 + `*r` 写回，零新增 IR——G1 引用全链路 + U 阶段作用域/引用返回能力的自然组合，typecheck `substitute` 对 `Ref` 泛型实参已可解析）。**std 改动**：`Vec::get_mut`（越界检查 `i >= 0 && i < self.len`，命中 `Some(&mut self.data[i])`——V1 GEP 真实取址复用）+ `HashMap::get_mut`（`self.find(k)` 缺失 `idx < 0` 返 None，命中 `Some(&mut self.vals[idx])`）；两处均更新头注释（值拷贝注记 → V4 引用语义）。**调用方迁移**：`vec_api.rl` 第 3 段从 `println(v3.get_mut(1))`（直接打印值）改为 match 写回 `*r = 88` + 越界 None 双断言（`v3[1]` 读回 88 证明真实写回）；`hashmap_api.rl` 第 3/4 段更新注释 + 第 4 段改为 `*r = 333` 写回后 `get(3)` 读回 333（原值拷贝段只验证"原表不受影响"，现验证写回真实槽）；tests/run-pass 与 examples/std-demos/02-collections 副本同步。**验收**：新增 `tests/run-pass/get_mut_ref.rl` 11 断言（vec 命中写回 `v[0]`=100、越界/负索引 None、连续写回 `w[2]` 30→35→70、HashMap 写回 999 后 `get(2)` 读回 999 + `len()` 仍 3 + 他键 `get(1)`=100 不受影响、引用剥层读 `println(*r)`=6）+ suite_test 全绿（`vec_api`/`hashmap_api`/`get_mut_ref` 均通过）。

---

## 5. 跟踪与验收约定

1. 每个阶段/任务完成须满足：`cargo test --workspace` 全绿 + `cargo clippy --workspace --all-targets` 0 警告。
2. 涉及运行时/并发类测试（Actor、通道、锁、join、GC 周期）须按 `design/00_项目总览.md` 硬性规则带超时保护，挂起即视为失败。
3. 任务完成后同步更新三处：本文档状态标识 + `CODEBUDDY.md`（§3.9 限制速览勾销 / §5.5 执行记录）+ `guide.md` §13（勾销对应限制条目，并更新 `grammar.md` 顶部实现状态标注）。
4. 每个阶段产出对应的集成测试（仿 development-plan.md 各阶段 `*_test.rs` 用例并全量回归）。
5. 优先交付顺序建议：G1/G2（引用地基 + str）→ I1/I2（宏 + 格式化）→ H1/H2（函数指针 + 无捕获闭包）→ K1（`?`）→ J 全阶段 → 其余。

---

- [x] **T 阶段：集合与迭代器收尾**（2026-08-24）：**T1a Vec**：新增 `iter`（MVP 退化元素拷贝缓冲）/`get_mut`（值拷贝）/`sort_by`（比较器闭包，选择排序）（`sort`/`binary_search` 已有）。**T1b String**：新增 `chars`（字节级）/`lines`（委托 split）/`to_uppercase`/`to_lowercase`（别名）（`split`/`replace`/`trim` 已有）。**T1c HashMap**：新增 `iter`（退化键缓冲）/`get_mut`（值拷贝）（`keys`/`values` 已有）。**T2 Iterator trait**：`trait Iterator { fn next(&mut self) -> Option<i64>; }`——关联类型 `type Item` MVP 退化固定 i64（parser/typecheck 无 trait `type` 成员载体）；自定义迭代器 `impl Iterator for T` 经 for 接入 ✓；适配器保持内建 desugar。**T3a Box::leak**：返回 `*mut T` 裸指针（G3，`*p` 读写），目标 `&'static mut T` 退化（`&*b` 取址需 MIR AddrOf 支持任意目标，规划）。**T3b**：Rc/Arc/Weak 目标 API 逐项核对完成（rc_new.rl 已全覆盖，无缺口）。**附带修复**：① typecheck `substitute`/`unify` 补 `Type::Fn` 递归分支 + 实例方法实参循环补闭包特判（泛型方法 `fn(T, T)` 形参此前不替换/不反推，`Vec::sort_by` 等泛型 fn 形参方法函数名/闭包实参均报错；修复后可用，全量回归无破坏）；② 实测发现已知限制——typecheck 变量环境 `variables: HashMap<String, Type>` 按名全局索引无作用域隔离，同名遮蔽（match 臂绑定 vs let 绑定）类型互相覆盖，println 按类型分支输出异常（i64 200 → `0xc8`），文档化为已知限制（作用域栈重构规划）。**验收**：`vec_api`/`string_api`/`hashmap_api`/`iterator_trait`/`box_leak` 5 个 run-pass 用例 + 全量回归（30+ 测试集 + suite_test 全绿）。文档：mvp-gaps-plan.md T 阶段 + std-lib.md §2.3/§3/§11 状态与差异注记。

> **维护者**：Rlyeh Language Team
> **最后更新**：2026-08-25
