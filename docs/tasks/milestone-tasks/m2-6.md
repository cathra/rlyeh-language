# M2.6 — 交叉编译

> **所属里程碑**：Milestone 2 — 生产可用（Month 4-8）
> **里程碑范围**：M2.1–M2.8 + 已完成语言特性
> **状态**：✅ 已完成
> **所属任务树**：[任务文档导航](../README.md) → 里程碑 M：[`milestone-2.md`](../milestone-2.md)

## 目标

交叉编译

## 执行情况 / 技术细节

交叉编译：`rlyeh build --target <triple>` 全链路；macOS 双架构已验证（`arm64-apple-macosx` / `x86_64-apple-macosx` 均以本机 clang 直链 SDK 编译运行）；`__rlyeh_target_os` 平台内建（linux=1/macos=2/windows=3/freebsd=4）驱动 `sockaddr_in4` 双布局（macOS `sin_len` 头 vs Linux 无）；driver 编译目标归一化（`arm64` 前缀统一为 `arm64-apple-macosx`）；Windows/ARM 工具链待环境

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-08-26 | 由里程碑任务列表细化为单任务文档 |
