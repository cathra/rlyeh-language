name: CI

on:
  push:
    branches: [ main ]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-latest ]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      # WASM 工具链：wasm 相关测试（wasm_target_test.rs）在工具缺失时优雅跳过，
      # 因此安装尽可能多的组件即可；缺失部分不影响其余测试。
      - name: Install WASM toolchain (macOS)
        if: runner.os == 'macOS'
        run: brew install lld wasmtime wasi-libc

      - name: Install WASM toolchain (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y lld clang wasmtime || true
          sudo apt-get install -y libwasi-libc-dev || true

      - name: Build
        run: cargo build --workspace

      - name: Test
        # 串行测试线程：driver 集成测试会 spawn clang 编译，并行时进程资源
        # 耗尽（os error 35，EMFILE/EPROCLIM）导致偶发失败，故限制为 1
        run: cargo test --workspace -- --test-threads=1

      # E1：macOS 双架构交叉编译冒烟（本机 clang 直链 SDK，已在本机验证）
      - name: Cross-compile smoke (macOS dual-arch)
        if: runner.os == 'macOS'
        run: |
          cargo run -p zeta-driver -- build examples/hello-world.zeta --target arm64-apple-macosx -o /tmp/hello-arm64
          cargo run -p zeta-driver -- build examples/hello-world.zeta --target x86_64-apple-macosx -o /tmp/hello-x86_64
          /tmp/hello-arm64 | grep -q "Hello, Zeta!"
          /tmp/hello-x86_64 | grep -q "Hello, Zeta!"

      # E2：WASM 编译 + wasmtime 运行冒烟（工具缺失时跳过）
      - name: WASM smoke
        run: |
          if ! command -v wasmtime >/dev/null; then echo "wasmtime missing, skipping"; exit 0; fi
          cargo run -p zeta-driver -- build examples/hello-world.zeta --target wasm32-wasi -o /tmp/hello.wasm
          wasmtime /tmp/hello.wasm | grep -q "Hello, Zeta!"
