// SH-P0-7 P-M1：守卫只对**可反驳模式**有意义。标识符 / `_` 兜底模式没有
// 匹配条件可与之 And 合并（其绑定位于臂体内、晚于条件求值），须显式报错。
// expect: 对兜底模式使用守卫条件
fn main() {
    let n = 1;
    match n {
        k if k > 0 => println(1),
        _ => println(0),
    }
}
