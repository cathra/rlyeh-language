// 模块 + use 导入：编译必须成功
mod math {
    pub const PI: f64 = 3.14159;

    pub fn square(x: i64) -> i64 {
        x * x
    }
}

use math::PI;
use math::square as sq;

fn main() {
    println(sq(9));
    println(PI);
}
